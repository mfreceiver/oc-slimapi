# Delivery Report — oc-slimapi v1 B0+B1

- **Run:** ocmar `v1-b0-b1` · **Finished:** 2026-07-18 · **Mode:** SERIAL
- **Base:** `324a2d2ed593d1367326cdd1964812420e6085f8` (clean commit: spec + plan)
- **Head:** working-tree (uncommitted) · **Owner (released):** `ocmar-v1-b0-b1`
- **Final state:** `resume-check` = **COMPLETED** (ledger `release --finished` → FINISHED)

## 1. Requirement (one line → shipped)
Implement oc-slimapi v1 **B0+B1** as strictly-additive changes (no `X-Slimapi-Version` bump): G2 `status` 404/503 split, G8 `full/{mid}` streaming cap, G7-soft messages directory allowlist, shell/PTY deny-list, and structured error codes (`{"code":...}`) across thin routes — all per the jointly-authored `docs/v1-impl-spec.md` wire authority.

## 2. Approach
Spec-driven, ocmar SERIAL flow: brainstorm (frozen spec) → plan (7 tasks + Criterion Ownership Matrix) → grill (5 plan refinements) → 7 sequential subagent tasks (implementer + per-task rev-grok review) → final whole-branch rev-gpt review → fresh `_priv-verifier` gate → promote-batch → release. Code-truth over doc-truth; v1-impl-spec line numbers corrected in T7; additive discipline enforced throughout.

## 3. What shipped (per task)
| Task | Content | Review |
|---|---|---|
| T1 | `errors.py` `CodedHTTPException(status,*,code,**fields)` + `register_error_handlers`; `require_directory` migrated; wired into `app.py` + 3 test helpers. +conftest, +test_errors | APPROVED (rev-grok) |
| T2 | G2 `status` 404/503 split (`_raise_upstream_status`); `projects()` 502/503 split; **status-map non-mapping JSON guard** (added in final fix wave). +test_sessions_routes (9) | APPROVED |
| T3 | G7-soft `_resolve_messages_directory` on list/since/full; query/header conflict → 400. +tests | APPROVED |
| T4 | G8 `full/{mid}` streaming cap (`read_with_cap` + `try/finally: await response.aclose()`; no `response.content` in full-mode). +tests | APPROVED |
| T5 | shell/PTY deny-list (`proxy.py` `_SHELL_PATH_RE`+guard; `config.py` `shell_deny_list_enabled`, **no** `shell_deny_paths`). +test_proxy (13 cases) | APPROVED |
| T6 | `questions.py` 4 sites → `CodedHTTPException` (+2 codes `invalid_directory_count`, `invalid_route_token`). +test_questions_routes | APPROVED |
| T7 | Docs sync: INTERFACE_MAP (transform-busy 502→503 + G2/G7-soft/shell subsections), CHANGELOG (7 bullets), CLIENT_CHANGES (error-shape), v1-impl-spec (§3/§4 + §11 two new rows); design-v2 skipped (no B1 section). | APPROVED (1 fix wave) |

## 4. Verification evidence (double-gate)
- **Final whole-branch review (rev-gpt):** APPROVED — `.ocmar/workflows/v1-b0-b1/review-final-rev-gpt-20260718231242.md` (0 Critical / 0 Important / 0 MUST_FIX; 1 cosmetic Minor deferred).
  - One fix wave applied: status-map non-mapping JSON guard (sessions.py:179-180) + regression test `test_status_map_non_mapping_json_returns_503` (was promote-blocker M-2.3, escalated from Minor during final review).
- **Fresh verifier (`_priv-verifier`, live rerun):**
  - Log: `.ocmar/workflows/v1-b0-b1/verify-1784387604.log` (first line `OCMAR_VERIFY_START=1784387604`)
  - **EXIT=0 · FAILURES=0 · PASSED=135** (`135 passed, 1037 warnings in 5.61s`)
- **Gate:** EXIT=0 AND FAILURES=0 AND final review APPROVED → **PASS**.

## 5. Changeset (working tree vs `324a2d2`, uncommitted)
- **Modified (13 files, +391/-45):** `CHANGELOG.md`, `docs/CLIENT_CHANGES.md`, `docs/INTERFACE_MAP.md`, `docs/v1-impl-spec.md`, `src/oc_slimapi/{app,config,proxy}.py`, `src/oc_slimapi/routes/{messages,questions,sessions}.py`, `tests/{test_health,test_messages_routes,test_metrics}.py`.
- **New (6 files):** `src/oc_slimapi/errors.py`, `tests/{conftest,test_errors,test_proxy,test_questions_routes,test_sessions_routes}.py`.
- **Do-not-touch respected:** `sse/hub.py`, `skeleton.py`, `tokens.py`, `versioning.py`, `transform.py` untouched. `X-Slimapi-Version` unchanged (additive).

## 6. Commit policy
Per user authorization, **only the baseline `324a2d2` (spec + plan) was committed**. All T1–T7 + final-fix changes remain as uncommitted working-tree diffs against `324a2d2`. No `git commit` / tag / push performed by the run. Commit/ship decision is the user's.

## 7. Deferred / rejected Minors (post-release tidy, non-blocking)
From the final review's Minor triage (13 accumulated + 1 cosmetic):
- **MUST-FIX (fixed this run):** M-2.3 (status-map non-mapping JSON → 503).
- **DEFER (future tidy):** M-1.1 (errors handler return-type annotation → Starlette `Response`), M-2.2 (projects 4xx symmetric test), M-5.1 (httpx private `AsyncIteratorByteStream` test helper), M-6.2 (review-package file-list header completeness), M-3.1 (self-check evidence hygiene — corrected this run), +1 cosmetic (self-check suite count line).
- **REJECT (not worth doing):** M-1.2, M-2.1, M-2.4, M-3.2, M-5.2, M-6.1, M-6.3.
- Full ledger: `.ocmar/workflows/v1-b0-b1/minor-ledger.md`.

## 8. Out of scope (explicitly deferred to a future B2 run)
G1 conditions verified in B0 (opencode v1.18.3) but **not** implemented this run:
- `/global/event` SSE frame-shape passthrough.
- session cursor / `Link` header semantics beyond current behavior.
- `/slimapi/**` version-gate edge hardening beyond current `ACCEPTED_CLIENT_VERSIONS`.

## 9. Audit references
- **Ledger:** `.ocmar/workflows/v1-b0-b1/state.json` (rev=24, FINISHED) + `progress.md` + `manifest.md`.
- **Spec / plan:** `docs/ocmar/specs/2026-07-18-v1-b0-b1-design.md`, `docs/ocmar/plans/2026-07-18-v1-b0-b1.md`.
- **Per-task:** `.ocmar/workflows/v1-b0-b1/task-{1..7}-{brief,report}.md` + `review-task-{1..7}-rev-grok-*.md`.
- **Final review:** `review-final-rev-gpt-{20260718230837 (NEEDS_FIX), 20260718231242 (APPROVED)}.md`.
- **Self-check:** `.ocmar/workflows/v1-b0-b1/self-check.md`. **Verify log:** `verify-1784387604.log`.
- **Gate:** state.json `gates=[parallel-admission]` (oracle CONDITIONAL → user chose SERIAL).

## 10. Conclusion
All 33 task criteria + 3 FINAL criteria = HIGH evidence (0 MISSING). Final review APPROVED; fresh verifier green (135 passed, EXIT=0, FAILURES=0). Additive-only (no version bump). Run **FINISHED**; working tree ready for the user to review/commit.
