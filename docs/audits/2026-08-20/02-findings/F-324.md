# F-324：孤儿 fixture——tests/fixtures/g_f1/（equal_ts_page1.json + README）全仓零引用，README 自述的配套文件不存在
- 严重度：P3（死数据；与 F-018 同类的仓库卫生项）
- 类别：test
- 位置：tests/fixtures/g_f1/equal_ts_page1.json（325B）、tests/fixtures/g_f1/README.md（610B）
- 置信度：high（rg 文件名/`g_f1` 全 tests/ + src/ 零引用；README 自述 `loop_scenario_pages.json` 「Not yet used」且该文件确实不存在）
- 快照：0b836e7
- 状态：confirmed
## 证据
1. `rg -l "equal_ts_page1|g_f1" tests/ src/ scripts/` → 仅 fixtures/g_f1/README.md 自身；
2. README 内容自认用于「G-F1 cursor 测试」计划，配套 loop_scenario_pages.json 未落地；
3. 现行 cursor 测试全在 test_cursor_matrix.py（36 用例，自建数据），与该 fixture 无关联。
## 分析（机制、影响面、触发条件、可达性）
- 无运行时影响；误导维护者以为存在未接线的 cursor 金样计划。tests/fixtures/ 内有效数据仅 msg40.json（test_skeleton.py 单一消费者）。
## 契约/文档对照（规范轨引文）
- 无。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：rg 重放：`ls tests/fixtures/g_f1/` → README.md + equal_ts_page1.json 存在；`rg -l 'equal_ts_page1|g_f1' tests/ src/ scripts/` → 仅 README 自身——孤儿 fixture 零引用复现。
- 维持 P3 / confirmed。原状态注记移入：verified（Phase 1 census §3.3 已列；A12 复核确认）。
## 建议方向（非实现）
- 删除 tests/fixtures/g_f1/ 目录；或在 README 标注「superseded by test_cursor_matrix.py」保留意图记录。
