# F-214：关停排空预算与 systemd TimeoutStopSec 不匹配——最坏 45s+ 超 15s 上限，SIGKILL 截断尾部回调（最终快照/access-log flush 丢失）
- 严重度：P3
- 类别：risk（运维/关停正确性预算）
- 位置：src/oc_slimapi/app.py:70（`_MAINT_DRAIN_TIMEOUT`=30s）、:79（`_TRANSFORM_DRAIN_TIMEOUT`=10s）、:85（`_DBAUX_DRAIN_TIMEOUT`=5s）、:97（`_GRACEFUL_SHUTDOWN_TIMEOUT`=5s，注释自认 systemd TimeoutStopSec=15 在其上）
- 置信度：high（算术 + 注释自证）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 最坏 lifespan 排空路径（LIFO，见 D05 §4.1）：C14 maintenance 30s（app.py:695-709 超时后 force cancel）+ C13 dbaux 5s（:605-615）+ C12/C11/C10/C9…（秒级）+ C4 transforms 10s（:326-337）→ **45s+**。
- app.py:93-97 注释自认：systemd `TimeoutStopSec=15`（docs/operations.md 部署单元）"sits above this value so the unit's 90s SIGKILL default is overridden by a shorter, explicit bound"——但 15s < 45s 最坏合计。
- uvicorn 连接排空 5s（:97/:780）与资源排空分属两层、无统一预算（e1-06 疑问点 4 同判断）。
- 佐证材料：03-reports/D05-concurrency.md §4.2 K4；01-explore/parts/e1-06-app.md 疑问点 4。
## 分析（机制、影响面、触发条件、可达性）
- 机制：各 drain 均为**上限**而非常量——典型关停（无卡死 worker/慢 gzip/维护 to_thread）在亚秒~秒级完成，不触面。触发需要：access-log 维护 to_thread 卡 ≥30s（gzip 大文件/慢盘）或 transform worker 卡 ≥10s（大 body gzip L6——与 F-201 的压缩量级相关）等组合。
- 后果：SIGKILL 截断尾部 LIFO 回调——顺序上 C2（最终流量快照，app.py:297-304）与 C1（access-log 句柄 flush，:254-265）恰在队尾，**最优先丢失**；upstream.aclose（C3）也可能被跳过（连接由进程退出兜底）。数据损失 = 最终快照帧 + 未 flush 的尾部 access 行（均 best-effort 语义，可接受但应自知）。
- 与 F-007 的交互：若 C11 qp_sweep 抛错跳过 C10-C1（F-007），叠加本条时尾部同样缺失——两条独立路径同一损失面。
## 契约/文档对照（规范轨引文）
- docs/operations.md（systemd 单元、TimeoutStopSec=15）与 app.py:93-97 注释为本条规范轨；docs/release.md 质量门禁不涉及。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——app.py:70/:79/:85/:97 四常量（30/10/5/5s）命中；:93-97 注释自认 systemd TimeoutStopSec=15 逐字命中——15s < 45s+ 最坏合计的算术成立。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「confirmed（A5/D05 Phase 2；Phase 3 终审）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 统一关停预算：如总预算 12s（< TimeoutStopSec=15），各 drain 按剩余墙钟分配（mirror absorb 的收窄模式）；或将 unit 的 TimeoutStopSec 提至 ≥50s（运维文件改动，二者取一，需与 operations.md 同步修订）。
