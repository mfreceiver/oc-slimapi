# F-350：traffic-accounting.md 版本窗口径文内不一致——头部/§2 写 supported:[3] 而自身 §5.1 字段表已载 wireVersion "4"/selectorResult v4
- 严重度：P3
- 类别：docs
- 位置：docs/manual/traffic-accounting.md:34（头部横幅+§2 快速查询）；对照 :169-170（§5.1 字段表）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- traffic-accounting.md:34：「`/slimapi/**` 端点须带查询参数 `?v=3`（**v3-only 终态**：缺 `v` / `v=2` / 不支持值 → 400 `unsupported_version supported=[3]`…）」。
- 同文件 §5.1 access log 字段表（:169-170）已载加性字段 `wireVersion`（"2"|"3"|**"4"**…）与 `selectorResult`（含 **v4**）——4.0.0 观测字段已入表，但入口口径仍写 v3-only。
- 实际：supported=[3,4]（selector.py:622-634）；`v=4` 合法且生产已有消费方（oc-webui，CHANGELOG [4.4.0] 动因）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：4.0.0 只更新了字段表（观测加性面），入口节的版本窗口径未同步——同一文件两个时代的层积。
- 影响面：按手册做流量/省流分析的运维（本手册是 access log/snapshot 查询的唯一样例源）；查询示例本身用 `?v=3` 仍合法（v3 消费面存在），主要是「supported=[3]」字面与新消费方接入认知冲突。
- 定级：手册入口句滞后、字段表（真正的工作面）正确——P3。
## 契约/文档对照（规范轨引文）
- v4-contract §9.1（selectorResult+v4 / wireVersion+"4"）；selector.py:95-109（枚举含 v4）；versioning.py:44。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 traffic-accounting.md:34（「v3-only 终态…supported=[3]」）对照同文件 :169-170（wireVersion 枚举含 "4"、selectorResult 含 v4、「4.0.0 起 (3,4) 双版本期」）打开核对一致——同文件两时代层积实证。
- 维持 P3 / confirmed。原状态注记移入：verified（A14）。
## 建议方向（非实现）
- :34 改「缺 `v` / 不支持值 → 400 supported=[3,4]」并删「v3-only 终态」字样（或改「(3,4) 双版本窗口」）。纯文档变更。
