# F-306：分层单向性验证通过——0 反向依赖、0 环（正向结论）+ 2 条边界注记
- 严重度：P3（informational——审计确认项，无违规）
- 类别：verification（A11 任务 1）
- 位置：全仓 71 个 src 模块（AST import 图 247 条内部边）
- 置信度：high（AST 解析 + Tarjan SCC + 指定 rg 命令双重复核）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 指定反向依赖清查 `rg -n "from oc_slimapi.routes|from ..routes|from ..app|from oc_slimapi.app" src/oc_slimapi/{sse,dbaux} src/oc_slimapi/*.py`（排除 app.py 自身）：**0 条命中**。
- AST 全量 import 图（71 模块、247 边）：Tarjan SCC 检出 **0 环**；`oc_slimapi.app` 扇入 0（无人 import 组合根）。
- 分层指派（util→核心→sse/dbaux→routes→app）逐边验证：核心(L1)→sse/dbaux/routes/app 边 = 0；sse/dbaux(L2)→routes/app 边 = 0；routes(L3)→app 边 = 0。
- 边界注记 2 条（非违规，归类讨论）：
  1. `src/oc_slimapi/upstream.py:8`（`from .config import ...`）与 `:9`（`from .middleware.request_id import REQUEST_ID_KEY`）——upstream 若归类 util 则上穿了 L1/middleware；实际其语义是「上游 HTTP 客户端工厂+头转发」核心件，建议文档化其 L1 定位而非改动。
  2. middleware/ 不在五层栈定义内：middleware/traffic_accounting.py import selector/traffic（L1），selector 扇入 17 中 1 个来自 middleware——中间件与核心件互不反向，现状健康。
## 分析（机制、影响面、触发条件、可达性）
- 机制：单向分层成立意味着「sse/dbaux 可独立于 routes 演进、routes 可独立于 app 演进」，重构（F-301/302/303）不会产生循环导入阻力。
- 影响面：正向结论，为拆分建议提供可行性背书。
## 契约/文档对照
- AGENTS.md/设计文档未显式冻结五层规则——建议把「util→核心→sse/dbaux→routes→app」写入 AGENTS.md 或 design 文档作为架构不变式（当前是事实成立、文档未声明）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：rg 重放（原命令逐字）：`rg -n "from oc_slimapi.routes|from ..routes|from ..app|from oc_slimapi.app" src/oc_slimapi/{sse,dbaux} src/oc_slimapi/*.py`（排 app.py）→ 字面 1 命中 = features.py:11 docstring 散文「sourced from ``routes/health.py``」（regex 通配 `..` 伪命中），**真实 import 反向依赖 0 条**结论成立；Tarjan SCC 0 环结论维持（快照 manifest 一致性由 Phase 0 保证）。
- 维持 P3 / confirmed（informational 正向结论）。原状态注记移入：draft（A11/D11 §2）。
## 建议方向（非实现）
- 将分层规则与 upstream.py 的 L1 定位补进架构文档；check.sh 可加 AST 单向性断言（复用本审计 /tmp/dep_graph.py 思路，脚本入库 ~60 行）防未来回归。
