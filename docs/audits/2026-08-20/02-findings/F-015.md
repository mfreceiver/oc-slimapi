# F-015：GlobalHub qp_last_activity 表无界（5 张有界 sid 表之外的例外）
- 严重度：P2（终判，A9 复核确认；初判 P2 → 终判 P2，无重定级——§3.3 无上界规则：无法证明「输入源可控 + 无自然约束」的 P1 要件）
- 类别：risk
- 位置：src/oc_slimapi/sse/global_hub.py:109（声明）、:678（唯一写点）；放大器 src/oc_slimapi/qp_sweep.py:55-61,113,124-131,155-164（F-273）
- 置信度：high（A9 增长路径与无删除路径全量 rg 证实）
- 快照：0b836e7
- 状态：confirmed
## 证据
- src/oc_slimapi/sse/global_hub.py:109（`self.qp_last_activity: dict[str, float] = {}`，注释「observation only」）
- src/oc_slimapi/sse/global_hub.py:671-685（IMMEDIATE q/p 分支：`:678 self.qp_last_activity[directory] = time.time()`——键 = 上游事件 `directory` 字段，任意非空 str 即入键（:672-677 仅 isinstance/非空守卫）；**写点在 allowlist 闸门 `_emit_directory_frame`（:684）之前**——被 allowlist 拒绝的 directory 同样入键）
- 无删除路径（rg 全仓）：仅 `app.py:505`（传引用给 QpSweepShadow 作 `activity`）、`qp_sweep.py:113`（`record_activity` 写同 dict）；`_evict_stale_directories`（qp_sweep.py:155-164）只清 `_known_dirs`/`_seen_at`/`_next_run`，**从不清 `_activity`**
- 放大器（F-273）：qp_sweep.py:175-177 `run_once` 先 `_ingest_directory_source()`（对全部 `_activity` 键刷新 `seen_at`）再 `_evict_stale_directories()` → 30 天逐出对 activity 键永假；`_known_dirs`/`_seen_at`/`_next_run` 三张镜像表随之有效无界
## 分析（机制、影响面、触发条件、可达性）
可增长路径（已证）：每个 q/p IMMEDIATE 事件按上游 directory 字符串插入一条；无 TTL/无容量 cap/无删除；resync_all（global_hub.py:977-985）清 tombstone/retired/updated_at 三表但**不清 qp_last_activity**。
生产影响检验（§3.3 无上界规则）：键空间 = opencode 实例工作目录集合，单用户工作站自然基数 ~十级；每条目 ≈100-300 B（str 键 + float + dict 槽）；达到 100 MiB RSS 需 ~10⁵-10⁶ 个不同目录字符串——需上游（信任边界内固定 loopback opencode）异常发帧或客户端经会话创建慢速刷目录，利用条件不现实。结论：真实增长路径成立但量级不可达 DoS → P2（非 P1）。
## 契约/文档对照（规范轨引文）
- 无契约面（纯内部观测结构，B1b stage 1 shadow scheduler 数据源，`app.py:501-523`）。
- 同类先例：sticky/tombstone 表的 P1-21 FIFO 加固（global_hub.py:130-143 注释）——本表是当时加固的漏网例外。
## 复核记录（Phase 3）
- **终态 confirmed / P2 risk**（draft → confirmed）。锚点抽查（2026-08-20）：global_hub.py:678 `self.qp_last_activity[directory] = time.time()`（IMMEDIATE 分支内、allowlist 闸门 :684 之前——本轮 sed 660-690 直读确认 :672-678 startswith 门与写点相对位置）命中；A9/A6 双线 rg（无删除路径、resync_all 清理清单不含本表、F-273 放大器）复核认同。反证搜索：sse/ 域状态容器全有界、本表唯一例外的 census 无新反例。§3.3 无上界规则量级论证（~10⁵-10⁶ 目录字符串才达百 MiB 级）成立 → P2 不升。原状态行括注「A9 已填充」由本节承接。
## A6 复核记录（2026-08-20，D06）
- **机制独立证实，同意维持 P2**（A6 初拟 P3，经 A9 的 F-273 放大器分析修正——`_activity` 键被 `_ingest_directory_source` 持续刷新 seen_at 使 30 天逐出永假，`_known_dirs`/`_seen_at`/`_next_run` 三张镜像表随之有效无界，非单表问题）。
- A6 侧补充验证（D06 §1.4）：global_hub.py:678 为唯一 hub 侧写点（IMMEDIATE 分支 :672-678，allowlist 闸门 :684 之前——被拒 directory 同样入键，与 A9 证据一致）；sse/ 其余状态容器全有界（GlobalHub 4 表 + token hub 15 容器逐项枚举），本表为唯一例外结论不变；resync_all（:977-991）清理清单不含本表亦经 A6 直读确认。
## 建议方向（非实现）
与 sticky 表同款 10k FIFO cap（代价：最老目录活动记录丢失——shadow scheduler 语义可容忍）；同时修复 F-273 的逐出失效（evict 应连同 `_activity` 键一并清理，或 ingest 不刷新 seen_at 而读活动时间）。
