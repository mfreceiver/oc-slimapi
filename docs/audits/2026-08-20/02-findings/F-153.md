# F-153：WebSocket 501 stub code `websocket_not_supported` 无契约归宿——v3 §8.2 catch-all 关闭条款未提 WS 面
- 严重度：P3（§3.3 contract-violation 行——未载类；501 stub 为边界面，消费方不应触达）
- 类别：contract
- 位置：src/oc_slimapi/proxy.py:35-40（`websocket.accept()` → `send_json({"code": "websocket_not_supported", "status": 501})` → `close(code=1011)`）；docs/specs/v3-contract.md:198-200（§8.2 catch-all 终局仅载 HTTP 404 `thin_route_not_found` 与收编全集）；docs/specs/v4-contract.md §8（无 WS 条款）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- proxy.py:35-40 如上；
- 负向搜索：`rg -n 'websocket_not_supported|WebSocket|websocket' docs/specs/v3-contract.md docs/specs/v4-contract.md` → 0 命中（INTERFACE_MAP §4 有 WS 行——追踪面有载、规范面无载）；
- 测试锁定：tests/test_proxy.py:184 `test_websocket_route_still_mounted`（锁定 stub 存在，code 字面无规范出处）。
## 分析（机制、影响面、触发条件、可达性）
- 三向对账「无主码」之一。WS upgrade 到任意路径得到 501 + code JSON——该行为自 v3 终态起存在，规范层从未收编。行为本身合理（终端边界），缺的是契约载明。
- 影响面：极小（消费方无 WS 用例）；属完备性缺口而非缺陷。
## 契约/文档对照（规范轨引文）
- v3 §8.2（catch-all 关闭 + 404 终局——HTTP 面穷尽，WS 面缺席）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——proxy.py:35-40 WS stub 命中。证据链成立，维持严重度与建议方向。
- P3 负向 rg 重放：`rg -n -i 'websocket' docs/specs/v3-contract.md docs/specs/v4-contract.md` → 0 命中（exit 1）——规范面无载成立。
- 与 F-142 的 WS facet 互补（本条=code 无契约归宿；F-142=501 呈现层级语义），非重复立项。
- 状态行归一：confirmed（原「verified」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- v3 §8.2（或 v4 §8）补一句：「WebSocket upgrade（任意路径）→ 501 stub `{"code":"websocket_not_supported","status":501}` 后 close(1011)」。
