# F-366：check_routes_doc 对账盲区 8 项合集（A14 消费）——反向漂移/语义白名单 7/54/collector 耦合等
- 严重度：P3
- 类别：gap
- 位置：scripts/check_routes_doc.py（collector :53,:57,:116-119,:162-180,:188-233；validate :261-275；SEMANTIC_CHECKS :71-87）
- 置信度：high（脚本逐行 + routes 全模块 grep 实证 + proxy.py 反例实证）
- 快照：0b836e7
- 状态：confirmed
## 证据
已 enforced 的（对照面）：存在性（代码→文档单向，表行才认 :188-204）、method 一致（:265-266）、7 条路径语义关键词（:71-87）、文档缺失 exit 2（:279-281）；基线 54 路由全绿（logs/check-baseline.txt）。

盲区 8 项（不查什么）：
1. **反向漂移**：`validate()` 只遍历代码路由（:261-275）——文档表行残留已删路由不报错；现态 84 条表行 vs 54 条 `**METHOD \`path\`**` 标题行（grep 实证），30 行差异无机制校验/解释。
2. **语义白名单仅 7/54**：SEMANTIC_CHECKS :71-87 只覆盖 7 条路径，其余 47 条路由语义零校验（:25-29 自认「刻意保守」）。
3. **描述列正确性**：白名单关键词子串之外（参数/默认值/错误码数值/行为描述）任意漂移不检。
4. **动态注册/app 级路由不可见**：只扫 `routes/*.py` 的 `@router.<attr>`（:162-180）——实证 `src/oc_slimapi/proxy.py:34-43`（`app.websocket` 501 stub + `app.api_route` catch-all）即在门外；今日两者是设计上的边界路由（非 /slimapi），但未来 app 级/`add_api_route` 的 /slimapi 路由将**静默逃逸**门禁（脚本 :34-38 已自我声明局限）。
5. **collector 耦合脆弱性**：router 变量必须恰名 `router`（:116-119 `func.value.id == "router"`）；`APIRouter(prefix="…")` 必须双引号字面量（:53 正则）——单引号/f-string/改名 → **整模块静默漏采**（漏检非误报）。现态 18 个 router 模块全合规（grep：全部为 `router = APIRouter(prefix="…")` 双引号形式）。
6. **SyntaxError 静默 continue**（:169-170）：语法损坏文件路由全漏检——check.sh 的 compileall+pytest 是事实兜底，但属静默吞错路径。
7. **`"12"` 纯子串匹配**（:80-84）：行内任意含 `12` 的串（12288、年份…）即命中；expand 类目计数锁定是名义性的，实际守卫只有 `EXPAND_CATEGORIES`。
8. **文档行 finditer 多标题**（:201-203）：同一物理行并列多个 `**METHOD \`path\`**` 全计入 `by_path`，理论上放宽 method_mismatch 判定（当前文档形状未触发）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：静态 AST 单向对账 + 保守白名单——设计取向是「防新增路由漏文档」（正方向），不防「文档侧腐化」（反方向）与「路由绕过 routes/ 声明」（侧方向）。
- 影响面：INTERFACE_MAP 作为 A14/省流审计的对照基准（AGENTS.md:65），其反向准确性无机械保证——省流审计（passthrough 聚合对照）若依赖已删路由的幽灵行会误判。
- 触发：删路由忘删文档行（1）；改 router 写法/引号风格（5）；在 app.py/proxy.py 直接挂 /slimapi 路由（4）。
## 契约/文档对照（规范轨引文）
- AGENTS.md:61/65：「每个 `/slimapi` 路由须在 INTERFACE_MAP 有记录，防漂移」——enforced 面与声称一致（单向）；反向无声称，属能力缺口而非违规。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 check_routes_doc.py:71-87（SEMANTIC_CHECKS 恰 7 路径）与 :261-275（validate 仅遍历代码路由 → 反向漂移盲区）打开核对一致——8 项盲区中 1/2/4/5/7 已直接定点复核，其余（3/6/8）随脚本全文通读维持。
- 维持 P3 / confirmed。原状态注记移入：verified（2026-08-20，D15 / A15；A14 契约线消费本清单）。
## 建议方向（非实现）
- 反向校验廉价可加：`by_path` 键集 − 代码路由集 = 幽灵行清单（报 warning 或 fail）；`"12"` 改 `\b12\b` 或直接依赖 EXPAND_CATEGORIES 引用 + 计数正则；collector 对「模块有路由装饰器但 prefix 未识别」给告警（覆盖盲区 5 的静默性）。
