# F-024：死代码/只写不读状态合集：_busy_sids（tokenstream）、last_touch（replay_log）、recycle 近 no-op、directory_source（qp_sweep）、strip_hop_by_hop（upstream.py:49-111 生产零消费者）、build_sessions_query dead import、_V4_PARENT_RESERVED 未使用
- 严重度：P3（初判，按 §3.3 决策表待 Phase 2/3 复核定级）
- 类别：smell
- 位置：见下表逐项 file:line
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
逐项验证表（完整版见 03-reports/D03-legacy-passthrough.md §8）：

| 项 | 位置 | 验证证据 | 裁决 |
|---|---|---|---|
| `_busy_sids` | sse/tokenstream/hub.py:283（docstring :240 自称 O(1) busy lookup mirror） | 全 src 零成员读取（唯一读表达式 = 自身 prune 的 len()，:2058；写点 :1056-1058、pop :1061/:1113、clear :2176）；仅测试白盒断言（test_token_hub_lifecycle.py:533-788） | 只写不读，docstring 用途无消费者 |
| `last_touch` | sse/replay_log.py:216（__slots__）,:225,:392,:430,:493,:510 | 全 src 零读取（rg `\.last_touch` 无读表达式）；TTL/recycle 决策不经它（sweep 用 entry TTL；recycle 由 frame_count==0 驱动） | 只写状态 |
| `recycle_domain` 近 no-op | sse/replay_log.py:495-513；调用点 replay_wire.py:274-277 | 唯一调用点条件 `domain_frame_count(domain)==0` ⇒ while 零迭代、bytes 已 0、domain shell 不删；净效果 = 写一次 write-only last_touch + return True | as-wired 真 no-op（§3.4 内存回收意图被调用点条件 defeating） |
| `directory_source` | qp_sweep.py:41,:62,:124-129 | 生产装配不传（app.py:504-509）→ `_ingest_directory_source` 生产恒 no-op（qp_sweep.py:127）；仅 tests/test_b1b_sweep_shadow.py:368-383 注入 | 生产死参数（test-seam 未标注） |
| `strip_hop_by_hop`+`HOP_BY_HOP`/`FORBIDDEN_*` | upstream.py:11-37,:49-111 | src 消费者零（仅 tests/test_upstream.py:6-190）；反代退役后无转发路径 | 生产死码（含 Set-Cookie merge caveat 一并遗留） |
| `build_sessions_query` dead import | routes/sessions.py:12 | sessions.py 内出现次数=1（import 行自身）；真实消费在 dbaux 内部与测试（test_sql_semantics.py:15、test_eqp_matrix.py:15） | 死 import |
| `_V4_PARENT_RESERVED` | routes/sessions.py:272 | 全 src+tests 零引用；保留词语义以内联字面量实现（sessions.py:405,:499,:506） | 死常量（单一真源意图未接线） |

- 佐证材料：A3/A10 汇总裁决；01-explore/parts/e1-01/09/17/16/11 卡片疑问点。
## 分析（机制、影响面、触发条件、可达性）
- 【A3 补】七项共性：无一项有行为影响（除 recycle 重接线选项外），全部为低风险清理项；两项附带「声明与实现不符」（_busy_sids docstring、_V4_PARENT_RESERVED 意图），一项附带「设计意图被接线条件 defeating」（recycle）——后者需在「删除」与「重接线为真回收」间做设计决策（受 REPLAY-018 seq shell 保留约束）。
- 可达性：全部不可达/无效果（生产路径）。
## 契约/文档对照（规范轨引文）
- 无一项属 wire 冻结面：_busy_sids/last_touch/directory_source/strip_hop_by_hop/build_sessions_query import/_V4_PARENT_RESERVED 均内部实现细节；recycle_domain 行为（domain 回收语义）受 v4-contract §7/REPLAY-018 序单调约束——删除调用不改变 wire，重接线才需契约对照。
## 复核记录（Phase 3）
- **终态 confirmed / P3**。锚点抽查（2026-08-20，七项抽四）：sessions.py:12 `build_sessions_query,`（import 行）+ :272 `_V4_PARENT_RESERVED = ("all", "none", "only")` 命中（本轮 sed 直读）；hub_types/global_hub 的 `_busy_sids`、qp_sweep `directory_source` 等由 A3/A10 双线 rg（生产零消费者/零读取）复核认同，无新反例。smell 合集（无行为影响、清理项）定性维持。原状态行括注「A3/D03 逐项 rg 验证升级：7/7 成立为真」由本节承接。
## A10 复核记录（2026-08-20，D10）
- 快照直读七项全部在位（`_busy_sids` hub.py:283 写点群 / `last_touch` replay_log.py:216 / recycle :495-513 / `directory_source` qp_sweep.py:41,124-129 / `strip_hop_by_hop`+常量 upstream.py:11-37,49-111 / `build_sessions_query` import sessions.py:12 / `_V4_PARENT_RESERVED` sessions.py:272）——**维持 verified / P3，A3 裁决不变**。
- A10 delta（F-024 未含、另立/并入）：token_stream gzip 恒-identity 残链 → **F-290**；global_hub dead import `logging` + messages 陈旧 getattr 回退 → **F-292**。本条维持七项原范围。
## 建议方向（非实现）
- 单 cleanup PR 批量处置：4 项直接删（strip_hop_by_hop+常量+其测试、死 import、死常量、_busy_sids 或接线）、2 项删或暴露（last_touch）、recycle 二选一（删调用+方法 / 改回收条件）；directory_source 随 F-140 的 qp_sweep 去留决策一并处理。
