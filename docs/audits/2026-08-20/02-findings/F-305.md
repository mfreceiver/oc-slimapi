# F-305：app.state 服务定位器 25 键隐式契约面（21 个文件读取、3 个私有键仅靠下划线约定）
- 严重度：P3（可维护性/隐式契约）
- 类别：design（耦合度量，A11 任务 3）
- 位置：写入点 src/oc_slimapi/app.py:197-684（lifespan）与 app.py:149-186（smoke 写 smoke_status/schema_degraded）；读取面 21 个 src 文件
- 置信度：high（rg 全量清点实测）
- 快照：0b836e7
- 状态：confirmed
## 证据
- rg `(?:request\.)?app\.state\.[a-zA-Z_]+` 全量清点：**25 个不同键**，总访问 ~178 次。按读频排序：config(38)、upstream(30)、transforms(19)、hubs(12)、schema_degraded(10)、replay_log(10)、smoke_status(8)、qp_sweep(5)、token_hub(4)、questions_semaphore(4)、permissions_semaphore(4)、traffic_snapshotter(3)、traffic_ledger(3)、replay_epoch(3)、deployment_revision(3)、dbaux(3)、actions_registry(3)、turn_registry(2)、token_registry(2)、catalog_cache(2)、sessions_degraded(1)、raw_fetch_registry(3)。
- 私有键 3 个：`_replay_sweep_task`（app.py:455）、`_access_log_stop_event`（:657）、`_access_log_maintenance_task`（:684）——生命周期句柄与业务键同容器，仅命名约定区分。
- 读取面 21 文件（rg -l 实测）：app.py + routes/ 15 文件 + skeleton.py + sse/global_hub.py + sse/registry.py + traffic.py + transform.py + discovery.py。
- 键集无静态定义：不存在 AppContainer/protocol；新增键=改 app.py + 消费方直接属性访问，拼写错误在运行期才暴露（AttributeError→500）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：FastAPI 惯用的 `app.state` 服务定位器模式；键空间已从早期少数服务增长到 25 键（config/传输/池/hub 族/信号量/观测/任务句柄混装）。
- 影响面：跨 21 文件的隐式契约——重构（如 F-301/F-302 拆分）需人工保证键读写对齐；测试 fixture 同样手工拼装 state（test census 佐证）。
- 触发条件：任何组件增删/改名。
- 可达性：持续存在；当前无运行时错误，纯演化风险。
## 契约/文档对照
- docs/specs/design-v2.md 描述 lifespan 接线但未冻结键清单（键集属实现细节，无契约冲突）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：rg 重放：`rg -o '(?:request\.)?app\.state\.[a-zA-Z_]+' src/ | sort -u | wc -l` → **25 个不同键**，与证据计数精确吻合；私有键 3 个（_replay_sweep_task 等）行号抽核一致。
- 维持 P3 / confirmed。原状态注记移入：draft（A11/D11 §5.3）。
## 建议方向（非实现）
- 引入 typed `AppContainer`（@dataclass，25 字段 + 3 个任务句柄独立 `LifecycleTasks` 子对象），lifespan 构造后整体挂 `app.state.container`；消费方经属性访问获得静态检查。DI 化可行性评估：**中高**——SSE 后台任务（hub flush loop、replay sweep）本就持有 app 引用，容器化不改变其生命周期；路由侧可渐进迁移（先 container 后 Depends），无需一次性切换。25 键中仅 config/upstream/transforms/hubs 四个高频键覆盖 ~56% 访问，先迁移这四个即获大半收益。
