# F-222：hub 侧 stop_after_grace 触发后 stop_task 残留 done 引用——unsubscribe 的 not stop_task 守卫使第二次宽限永不武装
- 严重度：P3
- 类别：defect
- 位置：src/oc_slimapi/sse/global_hub.py:359-360（unsubscribe 守卫）+ :419-425（stop_after_grace 尽毕不清引用）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- :419-425 `stop_after_grace`：sleep 30s → 无消费者则 cancel 三任务后**返回**——`self.stop_task` 仍指向已完成 task；
- :359-360 `unsubscribe`：`if not self.has_consumers() and not self.stop_task:` 才武装——stop_task 为**真值 done task** 时守卫失败，第二次 grace 永不武装；
- 复位路径唯二：`ensure_upstream` :213-215（新消费者到来时 cancel+None——cancel done task 是 no-op 但置 None 生效）/ 直接属性覆写。
## 分析（机制、影响面、触发性）
- 复现序列：直连 hub 的订阅→退订（arm stop_task #1）→30s 到期触发（三任务已 cancel）→再订阅→再退订——第二次 `not self.stop_task` 为 False → 不武装。后果：hub 侧三任务……已被第一次 grace cancel，本路径后果 = 后续 ensure_upstream 走 `task.done()` 重建（:216-217），功能自愈。
- **生产不达**：生产 detach 走 `HubRegistry.unsubscribe`（registry.py:232-256，自有 `_removal_task` 生命周期完整：4 个完成出口中 3 个显式置 None、canceller 先置 None）+ `registry.close`；GlobalHub.unsubscribe 自述「exercised directly only by unit tests」（:350-355）。故本缺口实际面 = 测试/维护路径 + 未来误用。
- 定级 P3：生产不可达、测试面行为偏差（第二次宽限不触发可使依赖 grace 计时的测试闪烁）。
## 契约/文档对照（规范轨引文）
- 无契约面（内部生命周期）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——global_hub.py:359-360 unsubscribe 守卫 `not self.stop_task` 与 :419-425 stop_after_grace 尽毕不清引用命中。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「verified（A6/D06 2026-08-20；Phase 3 终审）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- `stop_after_grace` 尽毕置 `self.stop_task = None`（一行）；或 unsubscribe 守卫改 `if ... and (not self.stop_task or self.stop_task.done())`。
