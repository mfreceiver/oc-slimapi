# F-290：token_stream.py gzip 残链死代码——`_accepts_gzip` 零调用、`use_gzip=False` 常量死条件、compressor/encode gzip 分支不可达、gzip 计数器恒 0 上报
- 严重度：P3
- 类别：smell（死代码）
- 位置：src/oc_slimapi/routes/token_stream.py:51（import）、:84-85（_accepts_gzip）、:203（use_gzip=False）、:213-226（compressor/encode gzip 分支）、:224-225（计数写点）；sse/tokenstream/models.py:93-94（字段）、sse/tokenstream/subscriber.py:869-870（快照上报）
- 置信度：high（rg 全量验证，src+tests）
- 快照：0b836e7
- 状态：confirmed
## 证据
- `_accepts_gzip`（:84-85）：rg `_accepts_gzip` 全仓（src+tests）**调用点 ∅**（仅定义）；`accepts_gzip` import（:51）唯一消费者即它。
- `use_gzip = False`（:203）字面常量 → `compressor = zlib.compressobj(...) if use_gzip else None`（:213-217）恒 None → `encode`（:219-226）恒 identity；gzip 分支（:223-225）不可达。
- `subscriber.metrics.gzip_raw_bytes_total / gzip_compressed_bytes_total`（:224-225）写点不可达 → subscriber.py:869-870 快照键 `gzipRawBytesTotal`/`gzipCompressedBytesTotal` **恒 0**。
- v3 终态注释自认（:25-35,197-201）：「the stream is ALWAYS identity … gzip negotiation remains」——3.0.0 lever2 退役后残链（e1-15 卡片 :137/:164 先证，A10 rg 复核）。
## 分析（机制、影响面、触发条件、可达性）
- 纯死链：无行为影响；误导面=读者以为 SSE gzip 例外仍活、metrics 两键看似有效遥测实恒 0（观测噪音）。
- 与 F-024 同族但独立项（F-024 七项未含此链）。
## 契约/文档对照（规范轨引文）
- v3-contract §7.2/终端态「SSE 流不做 content-encoding」——删除死链零 wire 变化；INTERFACE_MAP 注记「M3 终态恒 identity 不 gzip」与死链并存构成文实差。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 token_stream.py:84-85（`_accepts_gzip` 定义）、:203（`use_gzip = False` 字面常量）、:213-226（compressor 恒 None、encode 恒 identity、gzip 计数分支不可达）打开核对一致。
- rg 重放：`rg -n '_accepts_gzip' src/ tests/` → 仅定义处 1 命中（:84），调用点 ∅——死链全量验证复现。
- 维持 P3 / confirmed。原状态注记移入：verified（A10/D10 2026-08-20）。
## 建议方向（非实现）
- 删 `_accepts_gzip`+import、`use_gzip`/compressor/encode gzip 分支、两计数器字段与快照键（metrics 面**减键属加性反面**——需按 §9 观测契约确认快照键删除是否破坏消费方；保守方案=留键+注释「恒 0，历史保留」）。
