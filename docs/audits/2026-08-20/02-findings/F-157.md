# F-157：CLIENT_CHANGES.md 时效性滞后五族——权威指针指向已退役 v2 契约、已删除头/信封头、token stream gzip 例外、truncated/partEventRevision 语义
- 严重度：P3（§3.3 docs 行——面向 ocdroid 开发者的对接文档滞后；AGENTS.md 已载「冲突以本仓契约为准」兜底）
- 类别：docs
- 位置：docs/specs/CLIENT_CHANGES.md:1-5（头部「Aligned with v2-contract.md (lite-v2)」）、:57（「翻页用 X-Next-Cursor」）、:75-81（「sessions 列表完整性头 X-Complete」整节）、:112/:344/:361/:486（「须带 `X-Slimapi-Version: 2`」）、:356/:366（token stream「wire 以 v2-contract §3.x/§6.x 为准」「允许 gzip（首个 SSE gzip 例外）」）、:380/:413-414（「truncated 帧」「partEventRevision 必须 strict > 去重（契约 §3.x.2）」）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- :1-5 头部权威指针 → v2-contract（已于 3.0.0 退役；现行 = v3/v4 契约 + CHANGELOG，AGENTS.md 相关文档索引行口径）；
- :57 `X-Next-Cursor` / :75-81 `X-Complete`——两头已于 3.0.0 移除（v3 §1 退役表），v3 envelope（body 内 nextCursor/complete）替代，文档未标废止（v3 §4）；
- :112 等四处「须带 `X-Slimapi-Version: 2`」——头已删除（v3 §1），唯一通道 `?v=3`（v3 §2）；错误码节（:348 附近）仍列 `400 version_required`/`version_incompatible`（3.0.0 后不存在，selector.py 无此码）；
- :366 token stream「是 SSE 但允许 gzip（首个 SSE gzip 例外）」三层语义——v3 终态 SSE 恒 identity（v3 §7.3/§7.7；INTERFACE_MAP §3.1 已注明 lever2 压缩路径随 v3 终态移除）；
- :380 「truncated 帧（携带 partEventRevision）」/:413 「partEventRevision strict > 去重」——锚 v2 契约且与 :443 自述「events tokens=1 帧无 partEventRevision」矛盾（docs-notes D13）。
- 正向部分：2026-08-19 双轨 catch-up（digest 精拉 + 周期 304 对账）等新节已同步（时效性非全坏）。
## 分析（机制、影响面、触发条件、可达性）
- 面向 ocdroid 开发者的对接清单停留在 v2/v3 早期混合态；AGENTS.md 有「路径/版本以本仓库契约为准；文档滞后以 v3-contract + CHANGELOG 为准」兜底句，降低误用风险，但五族滞后句若被照做（带头/读信封头/gzip token 流）会直接对接失败。
## 契约/文档对照（规范轨引文）
- v3 §1（头退役表）/§2（?v= 通道）/§4（envelope）/§7.3-§7.7（SSE identity）；AGENTS.md 客户端配套说明行。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——CLIENT_CHANGES.md:1-5「Aligned with v2-contract.md」、:57 X-Next-Cursor、:75-81 X-Complete 节、:112 X-Slimapi-Version: 2 逐字命中——五族滞后成立。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「verified（A4 快评；docs-notes D9-D13 同源记录）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 头部权威指针改指 v3/v4-contract + CHANGELOG；五族滞后句逐条标注废止/改写（X-Slimapi-Version→?v=3、X-Next-Cursor/X-Complete→envelope 字段、token stream gzip→恒 identity、truncated/partEventRevision→按现行实现复核后删除）。
