# F-325：/slimapi/events 订阅上限错误的 HTTP 层映射（SubscriberCapacityError → 503 错误体）无路由级断言——仅 registry 单元级锁定
- 严重度：P3（错误产生与信封字段在 HubRegistry/TestAdmission 单元级精确锁定；缺的只是 events 路由 try/except → error_response 这一段的接线证明）
- 类别：test
- 位置：tests/test_hub_behavior_lock.py:949-1040（TestAdmission：per-directory/total cap 的 code/limit/current/rejected 计数——registry 级）；tests/test_hub.py:1169-1180（teardown 释放 slot，间接依赖 503 语义）；events 路由 HTTP 503 溢出用例：rg 零命中
- 置信度：high（`rg "sse_subscriber_limit" tests/` 仅 hub_behavior_lock/批量 lifecycle 的 registry 面 + 路由源码注释；test_events_tokens/test_sse_replay_wire 均不覆盖溢出）
- 快照：0b836e7
- 状态：confirmed
## 证据
1. TestAdmission 四用例锁 `err.code == "sse_subscriber_limit_directory"/"_total"`、limit/current 值、拒绝不计数、per-dir 先于 total 的短路序（:957-1040）——错误对象语义完备。
2. 但「路由把该异常转成 503 + 错误体 + 订阅不泄漏」只有 :1169 的 teardown 用例从侧面压住计数器；若 events.py 的 except 分支丢失/改状态码，无测试红。
3. 矩阵对应行：GET /slimapi/events 的 error_sse_subscriber_limit_total / _directory 两行 = P3（v3_test 引 registry 级用例）；boundary 行同引（cap 两侧在 registry 级有：admit_under_cap / reject_over）。
## 分析（机制、影响面、触发条件、可达性）
- 触发条件：真实溢出（默认上限内网单客户端部署几乎不可达）或 events.py 异常映射重构。影响面 = 消费方重连风暴场景的错误形状。
## 契约/文档对照（规范轨引文）
- v3 §7 / route-census：events local errors 含 sse_subscriber_limit_directory|_total（错误码在册）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：rg 重放：`rg -l 'sse_subscriber_limit' tests/` → 仅 test_hub_behavior_lock.py（TestAdmission registry 级）+ test_hub.py（teardown 侧面）——events 路由 HTTP 503 溢出用例零命中复现。
- 维持 P3 / confirmed。原状态 verified。
## 建议方向（非实现）
- 补一条路由级用例：registry 预填充至 cap → GET /slimapi/events → 断言 503 + `{"code":"sse_subscriber_limit_total",…}` 整body（复用 TestAdmission 的构造手法）。
