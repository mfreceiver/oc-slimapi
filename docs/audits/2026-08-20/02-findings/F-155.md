# F-155：v4-contract §3.2 `allowlist` 字段位置措辞歧义——行文可读作根级字段，实现嵌套于 `features.allowlist`（沿 v3 §3a）
- 严重度：P3（§3.3 contract-violation 行——措辞歧义）
- 类别：contract
- 位置：docs/specs/v4-contract.md:67（§3.2「v4 视图新增瞬态字段：`auxiliary: {…}`；`allowlist: {enabled: bool}`（机制是否启用，B4-4 落地…）」）；实现 src/oc_slimapi/routes/health.py:90-101（`resp["features"]["allowlist"] = {"enabled":…, "droppedEvents":…}`——嵌套于 features）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- v4-contract.md:67 原文将 `allowlist` 与根级 `auxiliary` 并列于「v4 视图新增瞬态字段」句中，未写明路径；
- health.py:90-101：`allowlist` 实际挂在 `features` 下（与 v3-contract.md:74 §3a「`features.allowlist`」一致）；`auxiliary` 挂根级（health.py:75-85）；
- 测试锁定：tests/test_health_features.py / test_health_dual_view.py（features.allowlist 形状）——行为锁定一致。
## 分析（机制、影响面、触发条件、可达性）
- §3.2 句法上「新增瞬态字段：A；B」诱导读者认为两者同级（均根级）；实现 B 在 features 下。按轨一序，v3 §3a 的嵌套定位 + 实现一致 → 实现无罪；纯 v4 措辞歧义。消费方若按 v4 §3.2 字面读根级 `allowlist` 会取不到值。
- 影响面：文档读者/新消费方对接；现 v4 消费方（oc-webui）未报告受影响。
## 契约/文档对照（规范轨引文）
- v3-contract §3a（`features.allowlist` 加性形状冻结）；v4 §3.2（措辞）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——v4-contract.md:67 命中；health.py:88-102 allowlist 嵌套于 features 命中。证据链成立，维持严重度与建议方向。
- 冲突消解（V3）：与 F-102 同锚 :67 两 facet（见 F-102 复核记录）——单句勘误同闭，04-final 合并处置。
- 状态行归一：confirmed（原「verified」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- v4 §3.2 措辞改为「`features.allowlist: {enabled}`（沿 v3 §3a 嵌套；v4 视图照常携带）」一词即可关闭歧义。
