# F-101：versions.py 模块 docstring 残留「nine-ID readiness gate」——修订二后实为十 ID，代码注释与冻结契约 §3.3 漂移（wire 正确）
- 严重度：P3（初判；按 §3.3 决策表：smell/文档漂移类，无行为影响）
- 类别：quality（代码注释漂移；兼 docs）
- 位置：src/oc_slimapi/routes/versions.py:24（模块 docstring）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- src/oc_slimapi/routes/versions.py:23-29 模块 docstring 原文（2026-08-19 修订批次注释）：
  ```
  2026-08-19 revision batch: the ``"4"`` face gains two ADDITIVE keys —
  ``readiness`` (§3.3 nine-ID readiness gate, always advertised) and
  ``expand`` (§14, emitted iff ``messages.expand.v4`` is satisfied; ...)
  ```
- 对照实现（wire 正确，十 ID）：src/oc_slimapi/readiness.py:58-69 `REQUIRED` 十元组（含第 10 项 `session.post-actions.v4`）、:93 `SATISFIED = frozenset(REQUIRED)`；versions.py:104-132 `_capabilities4` 经 `readiness_payload` 发出十 ID 载荷。
- 测试锁定（十 ID 为行为真值）：tests/test_versions_readiness.py:148-157（`len(readiness.REQUIRED) == 10`、第 9/10 位断言）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：修订二（owner 裁决 2026-08-19，已实施 v4.3.0）将 §3.3 全集 9→10 加性扩展；readiness.py 的模块注释与代码均已更新为十 ID（readiness.py:8-15 修订二段落完整），但 versions.py 的修订批次注释未被同步——「nine-ID」为修订一批次的历史措辞残留。
- 影响面：纯注释，零 wire/行为影响（载荷经 readiness_payload 构造，恒十 ID）。风险仅在维护者读注释形成错误心智模型（以为 versions 面广告的是九 ID 旧形状）。
- 可达性：任何读该模块的后续维护者。
## 契约/文档对照（规范轨引文）
- v4-contract §3.3（修订二冻结）：「`required: string[]  // 服务端恒发十 ID 全集（修订二后）`」「feature ID 全集 U（冻结；修订二 9→10 加性扩展）…10. `session.post-actions.v4`」。
- 实现与契约一致（十 ID）；仅 versions.py:24 注释与契约漂移。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——versions.py:22-29 docstring「§3.3 nine-ID readiness gate」逐字命中；对照 readiness.py:58-69 REQUIRED 十元组（第 10 项 session.post-actions.v4）——注释漂移而 wire 十 ID 的定性成立。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「draft（A1 主辖）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
下次触碰 versions.py 时将 :24 的 "nine-ID" 更正为 "ten-ID (revision 2)"；或独立一行注释修正（不构成行为变更，不需发版）。
