# F-138：死符号群——SELECTOR_V2/SELECTOR_ABSENT 常量、hub shim 的 `_LAST_UPDATED_AT_BY_SID_MAX` 死 re-export、global_hub 死 `import logging`、hub_types 死 `logger`
- 严重度：P3（初判，按 §3.3 决策表待 Phase 2/3 复核定级）
- 类别：smell
- 位置：src/oc_slimapi/selector.py:98-99；src/oc_slimapi/sse/hub.py:17；src/oc_slimapi/sse/global_hub.py:11；src/oc_slimapi/sse/hub_types.py:27
- 置信度：high（A3 rg 全仓零引用实证，D03 §2.1/§3-C1）
- 快照：0b836e7
- 状态：confirmed
## 证据
- selector.py:98-99：`SELECTOR_ABSENT = "absent"`、`SELECTOR_V2 = "v2"`——rg `SELECTOR_V2|SELECTOR_ABSENT` src+tests **零引用**（selector.py:96 注释自认两维不再产生，但常量符号本身也无人用；§9.1 观测域以字符串字面量存在于 traffic/access_log 消费端）。
- sse/hub.py:17：`from .global_hub import GlobalHub, _LAST_UPDATED_AT_BY_SID_MAX`——经 shim 消费该符号者**为零**（测试直连 `from oc_slimapi.sse.global_hub import _LAST_UPDATED_AT_BY_SID_MAX`，tests/test_batch3_lifecycle.py:1138,1150）。
- sse/global_hub.py:11：`import logging` 全文件未使用（logger 来自 logging_config，e1-05 疑点 1 同判）。
- sse/hub_types.py:27：`logger = get_logger(__name__)` 定义后本文件零调用（e1-05 疑点 12 同判）。
## 分析（机制、影响面、触发条件、可达性）
- 纯死符号，无行为影响；代价是阅读误导（`SELECTOR_V2` 似在表达活语义、shim 似在承诺 re-export 契约）与 5.0.0 收敛 (4,4) 时退役清单噪音。
- 与 F-024 区分：本条为 A3 新发现的四个符号；F-024 七项已另行裁决。
## 契约/文档对照（规范轨引文）
- v3-contract §9.1：selectorResult 枚举域以**值**冻结（`absent|v2|v3|rejected|exempt|not_applicable`）——删常量符号不影响冻结域（消费端均为字面量/独立常量）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——selector.py:98-99 两常量、sse/hub.py:17 re-export、global_hub.py:11 `import logging`、hub_types.py:27 `logger` 四锚点全命中；rg `SELECTOR_V2|SELECTOR_ABSENT` src+tests = 仅 selector.py:98-99 定义自身——零引用成立。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「draft（A3/D03 产出）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 四处一并小 PR：删 selector.py:98-99 两常量（保留注释说明域值冻结于契约）、hub.py:17 改为仅 `GlobalHub`、删 global_hub.py:11 与 hub_types.py:27 两死符号；零行为影响。
