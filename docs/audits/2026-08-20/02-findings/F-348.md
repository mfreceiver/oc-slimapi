# F-348：operations.md 示例滞后族——health 期望响应 [3,3]/sidecar 1.1.1/缺 allowlist 键、§3.2 unit 示例缺两行、supported:[3] 口径未提 v=4
- 严重度：P3
- 类别：docs
- 位置：docs/operations.md:353-361（§6.2 health 示例）、:458（§8 表 supported:[3]）、:75-120（§3.2 内嵌 unit 示例）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- operations.md:353-361：health 期望响应示例写 `"server": {"api_version": 3, "accepted_client_versions": [3, 3]}`、`"sidecar": {"version": "1.1.1"}`、features 块仅 `tokenStream/thresholdedSkeleton/skeletonInlineOutputMaxBytes`——实际 (3,4)（versioning.py:44）、包版本 4.4.0（pyproject）、features 含 `allowlist` 键（[3.3.0] 起，v3 §3a）、v4 视图另有 `auxiliary`（v4 §3.2）；同节文字「M3 起 [3,3]」「无 ?v=3 或 v=2 会被 400 拒绝」未提 v=4 合法。
- operations.md:458（§8 ocdroid 接入表）：「`?v=3`（无 `v`/`v=2`/不支持值 → `400 unsupported_version supported:[3]`）」——实际 supported=[3,4]（selector.py:622-634；test_terminal_matrix.py:115-177）。
- operations.md:75-120（§3.2 内嵌 unit 示例）：缺 `TimeoutStopSec=15`（deploy:24 有；§4:164 文字引用该值）与 `OC_SLIMAPI_TRAFFIC_SNAPSHOT_RETAIN_DAYS=30`（deploy:45 有；§5.3:239/§5.4 文字引用）——示例/文字/模板三处不同步；有 :122「以 deploy 为准」兜底句。
- 注：§3.2 注释 :92-94「生产 unit 已同款清理，模板不再示例」的不实陈述与 deploy:32-33 残留**不在本条**（F-004/F-005 已裁决，A13 辖）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：operations.md 的示例块在 3.x 定稿后未随 4.0.0（双版本/auxiliary）与 [3.3.0]（features.allowlist）更新；§3.2 示例与 deploy 模板演进脱节。
- 影响面：运维/排障人员按 §6.2 示例做健康自检会误判（期望 [3,3] vs 实际 [3,4]）；按 §3.2 示例（而非 deploy 模板）部署会回落 systemd 默认 90s 关停窗口并失去 snapshot 30 天保留（行为退化非故障，兜底句缓解）。
- 定级：runbook 示例滞后、有权威兜底（deploy 模板 + AGENTS 契约序）——P3。
## 契约/文档对照（规范轨引文）
- v4-contract §3.2（health 双视图/auxiliary）；v3-contract §3a（features.allowlist）；versioning.py:44；deploy/oc-slimapi.service:24,45。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 operations.md:353-361（health 示例 `[3, 3]` + sidecar `1.1.1` + features 无 allowlist）与 :458（`supported:[3]`）打开核对一致——对照 versioning.py:44 (3,4) 与 deploy:24（TimeoutStopSec=15，§3.2 示例缺）成立。
- 维持 P3 / confirmed。原状态注记移入：verified（A14）。
## 建议方向（非实现）
- §6.2 示例改双视图口径（v3 视图 accepted [3,4]…按 health.py 现实现重抄）+ sidecar 版本改占位符 + features 补 allowlist；:458 supported:[3,4]；§3.2 示例补两行或整块替换为指向 deploy 模板的引用。纯文档变更。
