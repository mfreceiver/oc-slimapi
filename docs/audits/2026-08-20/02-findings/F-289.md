# F-289：同码错误体字段命名层积——`response_too_large` 双字段名（`limit` ×13 vs `limitBytes` ×1）+ snake_case/camelCase body 字段并存（均契约冻结）
- 严重度：P3
- 类别：docs/quality（契约层积；实现无走样）
- 位置：见下表（全量发射点）
- 置信度：high（rg 全量核对）
- 快照：0b836e7
- 状态：confirmed
## 证据
- `response_too_large` 413 带 `limit=`：write_groups.py:142,227,374；_read_passthrough.py:229；_catalog_common.py:299,432；sessions.py:170,532,765；messages.py:842,1026；read_groups.py:499（13 发射点）。
- 同码带 `limitBytes=`：read_groups.py:334（providers §12）——v4-contract.md:512 冻结 `{"code":"response_too_large","limitBytes":<max_response_bytes>}`；todo/children 设计文档（specs/traffic-route-todo-2026-08-10.md:152）钉 `{"code":"response_too_large","limit":<cap>}`。
- expand 族 `limitBytes`（messages.py:1226,1526,1593，v3-contract.md:134,142）；投影限额 `limit`+`limitValue`（read_groups.py:356，v4-contract.md:517）；容量族 `limit`+`current`(+`bufferBytes`)（events.py:135、token_stream.py:178-180，v2-contract.md:472-473）。
- body 字段风格并存：snake_case `retry_after`（_catalog_common.py:49、messages.py:281；v3-contract.md:197 冻结）、`timeout_s`（actions.py:184）vs camelCase `sessionID`（upstream_errors.py:81）、`limitBytes`、`queryDirectory`/`headerDirectory`（selector.py:693-694）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：三代契约冻结层积（v2 容量族 → v3 expand/受控代理 → v4 §12/§13），每代择名不同且互不回改——实现忠实各冻结条款，无单点违约。
- 影响面：ocdroid 对 `response_too_large` 需双字段解析；新增路由面临「选哪形」无总则可依（read_groups 内同模块即两形并存，F-287 关联）。
- 可达性：全部 413/503 错误路径，客户端可见。
## 契约/文档对照（规范轨引文）
- v4-contract.md:512、v3-contract.md:134,142,197、v2-contract.md:472-473（各冻结原文，见证据）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：rg 重放 `response_too_large` 发射点全量：messages:841/:1025、sessions:169/:531/:764、_read_passthrough:227、_catalog_common:298/:431、read_groups:333(limitBytes)/:497(limit)、write_groups:225。
- 计数修正：证据所列 write_groups.py:142/:374 实为 **request_too_large** 的 `limit=` 行（非 response_too_large）——response_too_large 带 `limit=` 发射点实测 **10 处**（非 13），`limitBytes=` 1 处；「同码双字段名 + 命名风格层积」核心结论不受影响，contract 冻结引文（v4-contract.md:512 等）维持。
- 维持 P3 / confirmed（计数修正入档）。原状态注记移入：verified（A10/D10 2026-08-20；定性为文档/客户端互操作提示，非实现缺陷）。
## 建议方向（非实现）
- 契约总则加「错误体字段命名层积表」（一次性列全双形/风格出处，给 ocdroid 解析提示）；未来 major 窗口统一；新路由一律取既有族内同形（可在 refactor backlog 记 lint 规则雏形）。
