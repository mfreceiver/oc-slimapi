# F-242：候选 glob 不滤目录——`opencode*.db` 目录名计入候选可致 open_failed 30s 无效重探循环
- 严重度：P3
- 类别：defect
- 位置：src/oc_slimapi/dbaux/path_resolution.py:124（`glob.glob(...)` 无 isfile 过滤）
- 置信度：high（glob 语义 + 重探链复核，快照 0b836e7）
- 快照：0b836e7
- 状态：confirmed
## 证据
- path_resolution.py:124 `candidates = sorted(glob.glob(os.path.join(glob.escape(data_dir), _CANDIDATE_GLOB)))`——`glob.glob` 匹配目录名（`opencode-backup.db/` 之类目录命中 `opencode*.db` 模式）。
- 恰一个目录候选 → `ResolvedPath`（:125-130）→ lifecycle `_open_conn` 对目录 `sqlite3.connect` → OperationalError "unable to open database file" → classify=cantinit（lifecycle.py:132-135）→ `open_failed` 禁用 → 30s reprobe（ResolvedPath 有 path，重探语义正确）→ 永远失败（目录不会变成库）→ warning 循环 + probes 计数虚增。
- 多候选含目录 → path_ambiguous（fail-closed，正确但 reason 亦不指明目录混杂）。
## 分析
- 触发面：运维在数据目录留下名为 `opencode*.db` 的目录（备份脚本误建/mkdir 笔误）——低频误配置场景。
- 影响：辅助源永久禁用（降级 HTTP，功能不损）+ 30s 日志噪声；与 F-012 的不可自愈不同——本条修复盘上状态即自愈（reprobe 会成功），仅噪声与误诊面。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——path_resolution.py:124 `candidates = sorted(glob.glob(...))` 无 isfile 过滤命中。证据链成立，维持严重度与建议方向。
- 严重度归一：原 P4（越出 P0-P3 刻度）→ P3（Phase 3 归一化）。
- 状态行归一：confirmed（原「verified（2026-08-20，D07 / A7，新发现）」辖属/批次注记移入本记录）。

## 建议方向（非实现）
- 候选枚举加 `os.path.isfile` 过滤（保持纯读取性探测语义不变）。
