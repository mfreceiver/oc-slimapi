# F-364：check.sh MODE 参数校验后置于全套检查之后 + `--full` 死别名
- 严重度：P3
- 类别：quality
- 位置：scripts/check.sh:7（`--full` 兼容别名声明）、:30-35（case 校验在 pytest/对账/compileall 之后）
- 置信度：high（脚本逐行）
- 快照：0b836e7
- 状态：confirmed
## 证据
- scripts/check.sh:19 `MODE="${1:-default}"`，但 case 校验在 :30-35——传错参数（如 `--ful`）仍会先完整跑 pytest（基线 127s）+ 对账 + compileall 才报「用法: check.sh [--full]; exit 1」。
- :31 `--full|default|"")` 三分支等价空动作——`--full` 与默认完全同行为（:7 注释自认「兼容别名（行为等价于默认）」），无差异化 full 模式。
## 分析（机制、影响面、触发条件、可达性）
- 机制：参数校验未前置（对照：release.sh:22-23 参数校验在第一步）。
- 影响面：误传参数浪费一轮完整门禁（~2 分钟）；`--full` 语义噪音——读者预期 full 有额外项，实际没有（release.md §4 的「可选扩展（后续）」叠加加剧误读，见 F-363）。
- 方向性安全：浪费但不误放行（错误参数最终仍 exit 1，任何非 `--full|default|""` 值都失败）。
## 契约/文档对照（规范轨引文）
- AGENTS.md:93 用法示例仅 `./scripts/check.sh`（无 --full）；docs/release.md 未提 --full——别名无文档消费者，删除无对接影响（属本仓内部脚本）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 check.sh:7（`--full` 兼容别名，注释自认行为等价默认）与 :30-35（case MODE 校验位于 pytest/对账/compileall 之后）打开核对一致——误传参数先跑全套门禁才报错成立。
- 维持 P3 / confirmed。原状态注记移入：verified（2026-08-20，D15 / A15）。
## 建议方向（非实现）
- MODE 校验移到 venv 检查之前；评估直接删除 `--full` 别名（git 历史可查消费痕迹，当前仓内零调用）。
