# F-340：sessionsDegraded 计数器 best-effort 挂载失败静默丢计数——`ensure_sessions_degraded_counters` setattr 失败返回临时实例，degraded 观测恒 0 且无信号
- 严重度：P3（防御路径缺陷：触发前提罕见；但触发即静默清零「operator-facing truth」计数器）
- 类别：defect（observability）
- 位置：src/oc_slimapi/traffic.py:398-417（:410-416 临时实例分支）；src/oc_slimapi/middleware/traffic_accounting.py:425-435
- 置信度：high（代码自证；触发路径为防御分支）
- 快照：0b836e7
- 状态：confirmed
## 证据
- traffic.py:398-417 `ensure_sessions_degraded_counters(state)`：get-then-set 惰性挂载到 `app.state`；:410-416 `except Exception` 时「return a throwaway counter so recording never raises」——注释自认丢计数（best-effort）。
- 挂载点 traffic_accounting.py:425-433：每请求 `ensure_…` → `record_degraded_200()/record_fail_closed_503()`；metrics.py:80-84 读 `app.state` 上的**同一实例**——临时实例的计数进不了 metrics。
- 全链无 warning/无计数（对比同函数族 `_record` 三段均 warning）。
## 分析
- 机制：setattr 失败（如只读 state 对象/极端内存态）后每请求都新建临时实例、逐次丢弃——`sessionsDegraded` 块虽会出现（若曾有一次挂载成功）但恒 0，或永不出现；两种形态都无诊断线索。
- 影响面：v4 降级「operator-facing truth」（metrics.py:70-79 注释语）失真——降级持续发生而计数为 0 比块缺席更具误导性。
- 触发条件：app.state setattr 失败——生产 FastAPI state 为可写 dict-backed，现实几乎不可达（防御分支）；评分 P3 而非 P2 的依据。
## 契约/文档对照
- v4-contract §9.1（per-response degraded 计数是 rev-gate BLOCKER-4 要求）——「per-response 计数」承诺在防御分支静默失效。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 traffic.py:398-417 打开核对一致：:407-411 get-then-set 惰性挂载、:410-416 `except Exception` 返回临时实例（"best-effort — counts live only for this call" 注释原文）——静默丢计数机制成立；metrics 读同实例口径维持。
- 维持 P3 / confirmed。原状态注记移入：verified（A13/D13 §1.3 #10；Phase 3 终态待定）。
## 建议方向（非实现）
- :410-416 except 分支加一次性 warning（`get_logger("traffic").warning`，自限频防刷屏）+ 进程级 `degraded_counter_mount_failures` 计数（进 F-337 元观测块）；文档不必改（内部防御）。
