# F-252：directory allowlist 覆盖面不完整——仅 file 三路由 + SSE 帧过滤 + v4 sessions 降级矩阵受控，其余 directory 敏感路由不查 allowlist
- 严重度：P2（E-II 组合下加重为 F-251 的一部分；E-I/E-III 受信消费方下 P3）
- 类别：security/gap
- 位置：src/oc_slimapi/routes/read_groups.py:123-143（`_authorized_file_directory`——file/file/content/file/status 三路由唯一调用点）；src/oc_slimapi/sse/global_hub.py:572-586（SSE 帧过滤）；src/oc_slimapi/routes/sessions.py:488-491（v4 降级矩阵：allowlist 非空→503）；src/oc_slimapi/dbaux/cursor.py:100-113（指纹）；src/oc_slimapi/routes/health.py:90-101（回显）
- 置信度：high（rg 全 src 的 allowlist 消费点穷举）
- 快照：0b836e7
- 状态：confirmed
## 证据
- `_authorized_file_directory` 全仓调用点仅 3 处：read_groups.py:159/171/182（file_list/file_content/file_status）。
- 不设门的 directory 敏感路由（均以 `X-Opencode-Directory` 或上游自身 directory 语义工作）：`/slimapi/vcs`、`/slimapi/vcs/status`、`/slimapi/vcs/diff`（read_groups.py:192-221）、`/slimapi/find/file`（:227-241）、`/slimapi/config/providers`（:395-409）、`/slimapi/session/{sid}`（:555-576）、messages 族（messages.py 全部）、todo/children/diff（todo.py:72、children.py:78、diff.py:97）、write 族 12 端点（write_groups.py:262-583）、token stream（token_stream.py:88-119——directory 仅作冲突判定）。
- 上游侧 directory 语义确认：WorkspaceRoutingQuery 挂在 vcs/config/session 等组（read_groups.py:23-33 注释引 groups/file.ts、instance.ts、config.ts、session.ts）——这些路由的 directory 选择真实影响上游行为，但 sidecar 不施加 allowlist。
- SSE hub 侧语义不对称（既有记载）：allowlist `""`=[] 时 /file 路由 reject-all 而 hub 放行（config-census §1 #34 / e1-04 Q5）——同为覆盖面割裂的佐证。
## 分析（机制、影响面、触发条件、可达性）
- 机制：allowlist 的设计定位是「/file 族读授权 + SSE 帧过滤」（rev-2 closure 注释 config.py:216-246 只点名 routes/read_groups.py 与 sse/global_hub.py 两处决策点）——即**有意局部**，但没有任何文档把「allowlist ≠ 全局目录授权边界」写成显式契约；运维若按直觉设置 allowlist 会得到虚假安全感（vcs/diff、find、providers、session-single、全部写路由照常任意目录）。
- 影响面：启用 allowlist 的部署中，未覆盖路由仍是任意 directory 面（写路由可对任意目录会话发起 prompt/删除）。
- 触发条件：`OC_SLIMAPI_DIRECTORY_ALLOWLIST` 设置为非空列表。
- 可达性：配置即可达。
## 契约/文档对照
- v3-contract §10.a（file 组 403 门）；operations.md 对 allowlist 语义**零记载**（config-census D9——文档缺口与本发现互为表里）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——read_groups.py:123-143 `_authorized_file_directory` 定义命中。证据链成立，维持严重度与建议方向。
- P2 rg 重放：`rg -n "_authorized_file_directory" src/` = read_groups.py:123（定义）+ :159/:171/:182（file_list/file_content/file_status 三调用）——全仓消费点穷举与发现一致，其余 directory 敏感路由不查 allowlist 成立。
- 状态行归一：confirmed（原「draft（A8/D08 §2.2）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 二选一：①把 allowlist 提升为全 directory 消费路由的统一前置门（selector 层实现最省——validate_directory 之后统一 `_authorized_file_directory`）；②在 operations.md + v3/v4 契约 allowlist 条目明示「仅覆盖 /file 族与 SSE 帧」的安全语义，把「需要全局边界时配合 host 绑定/网络层」写死。修复 F-251 前不应依赖现状 allowlist。
