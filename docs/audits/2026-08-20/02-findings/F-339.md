# F-339：runbook 缺口汇总——operations.md 对 20+ 生产 env 零记载、503/degraded/订阅/重放/allowlist 场景零动作条目（19 条缺口清单）
- 严重度：P2（运维面广泛缺失：含安全相关 allowlist 语义与 E-II 姿态下唯一的访问控制面；单条皆 P3，汇总后值班可操作性显著受损）
- 类别：ops（runbook 完备性）
- 位置：docs/operations.md（全文 644 行）；佐证 01-explore/config-census.md §1/§8、D13 §3.3
- 置信度：high（A13 逐场景核对 + grep 实证）
- 快照：0b836e7
- 状态：confirmed
## 证据（19 条缺口，grep/行号实证）
- **env 零记载族**（operations.md 无、仅 develop.md 泛指或全仓无）：①`DIRECTORY_ALLOWLIST`（三态语义/`""`=reject-all 而 SSE hub 放行的不对称/清单变更须重启——operations.md 唯二 "allowlist" 命中 :582/:642 均指 action 子进程 env）；②SSE 控制面 5 旋钮（#21-25）；③token-stream 4 旋钮（#26-29）；④`REPLAY_COUNT/BYTES_KB/TTL_S`（env 名无 MAX_ 前缀 + KiB 单位陷阱）；⑤`QP_SWEEP_*` 三参数；⑥`DBAUX_PROBE_INTERVAL_S`；⑦`CLIENT_ID_HASH/SALT`（隐私回退，联 F-342）；⑧`ETAG_ENABLED`（关闭轮换全部验证器副作用）；⑨`TRAFFIC_METRICS/ACCESS_LOG/SNAPSHOT_ENABLED` 关闭后果（联 F-009）；⑩`ACCESS_LOG_COMPRESS_ON_STARTUP`。
- **场景零条目族**：⑪allowlist 非空 × dbaux 不可用 → 全 503 fail-closed（v4 §4.2 最严列，§7.3 只覆盖熔断降级 HTTP）；⑫search 通配 × db-down → 503；⑬transform_busy 持续（skeleton 块读法零说明）；⑭SSE 订阅上限 400 排障/调参；⑮questions/permissions 200 载 errors[]/truncated 观察（联 F-331）；⑯metrics 探针 `?v=` 缺参（联 F-334，operations.md:475）；⑰crash-loop journal/systemd 判据 + unit 加固（联 F-344/F-004）；⑱观测面自身健康检测（snapshot inactive 等关键词，联 F-337）；⑲E-II §11 小节编号漂移（§11 内用 10.1-10.4 标题）+ 边界验证结果指向外部报告无回填机制。
- 对照正例：§5.5 已详载 questions/permissions/merged/catalog 12 knob、§7 已载 dbaux——证明记载纪律存在，缺口是覆盖面问题而非风格问题。
## 分析
- 影响面：72 生产 env 中约 30 有 ops 记载、20+ 零记载；D8 判 E-II（0.0.0.0 无认证）为生产姿态（F-251）——其中 `DIRECTORY_ALLOWLIST` 是该姿态下 file 读路径的主要限制面，语义零记载 = 安全控制面不可运维。
- 缓解：develop.md §配置有 14 项速查（但自带过时钉值，docs-notes D8/D13）——「ops 文档→次级文档→过时值」链式漂移使泛指引用不可靠。
## 契约/文档对照
- AGENTS.md 文档索引称 operations.md 为「部署/运维/日志」权威；v3 §5.7a/v4 §5（allowlist 语义契约侧完整）——实现与契约均完备，唯 ops 层断链。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核（质量线 P2）：rg 重放：`rg -c 'DIRECTORY_ALLOWLIST' docs/operations.md` → **零命中**；`rg -in allowlist docs/operations.md` → 仅 2 处（:642 子进程 env allowlist、:582 动作名白名单字典——原证据把 :582 也归入「action 子进程 env」略不精确，但两处均非 DIRECTORY_ALLOWLIST，核心结论不受影响）；`QP_SWEEP|REPLAY_COUNT|DBAUX_PROBE|CLIENT_ID_HASH` 同零命中——env 零记载族复现。
- 维持 P2 / confirmed（19 条缺口清单抽样 5 条全部复核成立）。原状态注记移入：verified（A13/D13 §3；Phase 3 终态待定）。
## 建议方向（非实现）
- operations.md 增补：§5.5 扩为完整 env 表（或附录链接 config-census 式四列表）+ 新 §「allowlist 运维」+ §7 扩 503 场景矩阵（⑪⑫）+ §9 扩 SSE/replay/transform_busy 三行 + metrics 探针统一带参提示；⑲ 顺手修标题编号。优先序：①⑪（安全）> ④⑭（SSE 稳定性）> 其余。
