# F-246：dbaux 卫生合集——死代码与导出面三策略漂移
- 严重度：P3
- 类别：quality
- 位置：src/oc_slimapi/dbaux/lifecycle.py:508, :47-55；src/oc_slimapi/dbaux/projection.py:82；src/oc_slimapi/dbaux/path_resolution.py:157；src/oc_slimapi/dbaux/__init__.py
- 置信度：high（rg 全仓负向搜索逐项验证，快照 0b836e7）
- 快照：0b836e7
- 状态：confirmed
## 证据
1. `_worker_thread_id`（lifecycle.py:508 写入）全仓零读消费（rg `_worker_thread_id` 仅 :356 初始化 + :508 赋值）——死代码/预留观测位未用。
2. lifecycle `__all__`（:47-55）缺 `DbAuxStatus`——`from .lifecycle import *` 拿不到，而包门面 `__init__.py:38,:73` 显式导出——两条导入路径行为分叉。
3. `PARENT_RESERVED_STATES`（projection.py:82 定义；`__init__.py:55,:80` re-export）全仓零消费者——死导出（archived 族有路由侧消费 `sessions.py:271`，parent 族没有——路由用自有 `_V4_PARENT_RESERVED`，sessions.py:272）。
4. path_resolution `__all__` 导出 `Path`（:157）——pathlib re-export 无消费者，误导（模块实际全 str）。
5. 导出面三策略并存：cursor.py 有完整 `__all__`（:211-225）、lifecycle 有但不全（:47-55）、projection.py **无** `__all__`（靠 `__init__` 显式清单）——漂移温床（同族问题见 F-024 死代码合集，本条为 dbaux 域补集）。
## 分析
- 无行为影响；维护债（阅读误导 + 重构时漏改面）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——lifecycle.py:508 `self._worker_thread_id = threading.get_ident()` 与 :47-55 __all__（无 DbAuxStatus）命中。证据链成立，维持严重度与建议方向。
- 严重度归一：原 P4（越出 P0-P3 刻度）→ P3（Phase 3 归一化）。
- 状态行归一：confirmed（原「verified（2026-08-20，D07 / A7，新发现——五项合集）」辖属/批次注记移入本记录）。

## 建议方向（非实现）
- 删 `_worker_thread_id` 或补消费者；补齐/统一三模块 `__all__`；`PARENT_RESERVED_STATES`/`Path` 删导出或补文档锚点用途。
