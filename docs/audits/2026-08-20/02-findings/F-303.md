# F-303：config.py 1158 行混载三类非 env 职责（TOKEN_* 常量 / allowlist 匹配族 / 420 行 validate）
- 严重度：P3（可维护性；变更频率全仓第 2 但结构清晰）
- 类别：design（模块化，A11 数据驱动入围：行数>500(1158) + 变更频率 Top10(22)）
- 位置：src/oc_slimapi/config.py:46-101、:200-351、:736-1155
- 置信度：high（行段实测 + e1-04 卡片全文精读）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 文件构成（def/常量行号实测）：TOKEN_* 代码级常量 :46-101（~56 行）+ 校验上限与导入期 assert :106-163 + allowlist 函数族 :200-351（`allowlist_roots` :268、`candidate_canonical` :294、`match_allowlist` :321、`directory_allowed` :341，~150 行）+ `Settings` dataclass :355-735（71 个注解字段）+ `validate()` :736-1155（**420 行单方法**）+ `settings = Settings()` :1158。
- TOKEN_* 消费方仅两处：tokenstream/hub.py（含 `apply_debug_budget_overrides` :126-161 就地改写全局）与 config.py 自身 assert（:142-163）——rg 实测。
- allowlist 族消费方：routes/read_groups.py（404 门）+ sse/global_hub.py（SSE 帧过滤）+ config.validate（e1-04 卡片职责节）。
- `validate()` 消费方仅 app.py:196、:771 两处调用（rg `config.validate` 实测 2 处）。
- 变更频率 22 次（2026-08-01 以来，全仓第 2，仅次于 app.py）——新配置项持续涌入单一文件。
## 分析（机制、影响面、触发条件、可达性）
- 机制：config.py 名义职责是「env 唯一入口」，实际还承载 token-stream 预算常量（属 tokenstream 域知识）、directory-allowlist 安全匹配（属共享安全域）、420 行校验程序；三个租户使文件成为变更磁铁（每新增旋钮/新预算/新校验规则都改此文件）。
- 影响面：可维护性；TOKEN_* 与 `apply_debug_budget_overrides` 的就地改写跨模块可变状态（e1-04 疑点）本可收拢到属主模块。
- 触发条件：任何功能发版新增配置。
- 可达性：持续发生（22 次/19 天）。
## 契约/文档对照
- `docs/specs/v3-contract.md` 无涉（纯内部）；env 变量名与默认值必须原样保留（config-census.md 已冻结清单）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 wc -l config.py = 1158 ✓；`def validate` 位于 :736（证据行段 :736-1155 = 420 行单方法）✓；TOKEN_*/allowlist 族/Settings 行段与 rg 消费方口径一致。
- 维持 P3 / confirmed。原状态注记移入：draft（A11/D11 §5.1）。
## 建议方向（非实现）
- 三步低成本拆分（Settings dataclass 本体保持扁平——env 单命名空间是 ops 面，嵌套化会波及 82 个测试文件的 settings 用法）：
  1. `tokenstream/budgets.py`：迁 TOKEN_* :46-101 + `apply_debug_budget_overrides`（hub.py:126-161），config.py 保 import 兼容 re-export（assert :142-163 随迁）；
  2. `directory_allowlist.py`：迁 :200-351 函数族 + `_ALLOWLIST_ROOTS_CACHE`（:252 模块级可变缓存，e1-04 记录无淘汰）；
  3. `config_validate.py`：迁 `validate()` :736-1155（消费方仅 app 两处 + 2 处测试引用）。
- 拆后 config.py ~400 行（纯 Settings + env 解析 helper）；三处迁移各有 rg 可枚举的窄消费面。
