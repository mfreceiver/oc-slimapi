# F-014：global_hub 非 dict JSON 逃逸 publish 会拆整条上游连接（上游异常形状的放大面）
- 严重度：P3（A6 终判：P2→P3 降级——机制证实但上游正常行为不可达，见分析）
- 类别：defect
- 位置：src/oc_slimapi/sse/global_hub.py:662（`global_event.get("directory")`）vs :665（`isinstance(payload, dict)` 守卫位次）
- 置信度：high（A6 机制推演闭环）
- 快照：0b836e7
- 状态：confirmed
## 证据
- global_hub.py:1052 `self.publish(orjson.loads("\n".join(data_lines)))`——`orjson.loads` 产出任意 JSON 值；
- global_hub.py:662 `directory = global_event.get("directory")`——**先于** :665 `if not isinstance(payload, dict): return` 守卫执行；`global_event` 为 list/int/None/str 时 `.get` 抛 AttributeError；
- :1053-1054 仅守卫 `orjson.JSONDecodeError`（坏 JSON），合法 JSON 非 dict 形状（`[1,2]`/`null`/`123`）逃逸；
- 逃逸后果链：publish 在 run() 的 try 块内（:1003-1072）→ except Exception（:1075-1090）按上游失联处理——`_notify_upstream_loss()`（resync_all 全局扇出 + write_barrier 全域 + token hub 清态）+ 退避重连 + 连接重建。
- 上游真值（E8 §3.1 / 假设 #23）：`handlers/global.ts:16-23` SSE encode + JSON.stringify 恒对象帧——正常行为不可达，仅上游自身 bug/回归可触发。
## 分析（机制、影响面、触发条件、可达性）
- 机制：单条毒帧被放大为「整条上游连接拆断 + 全体订阅者 resync + 全域 barrier 写入 + 重连周期」。若上游持续发出该形状则反复循环（重连 → 再遇毒帧 → 再拆）。
- 可达性：低——上游 v1.18.18 恒发对象帧；触发需上游引入回归。与 F-015 同信任域（上游可控输入）。
- 定级理由：初判 P2 按「拆连接」影响；A6 核实上游正常行为不可达 + 单毒帧自愈（重连后流从当前继续）后降 P3（防御纵深缺口）。
## 契约/文档对照（规范轨引文）
- 无契约条款直接涉及（内部防御位）；v3 §7.8 盲区二「SSE 断连窗口」为本路径触发后的既定恢复语义。
## 复核记录（Phase 3）
- **终态 confirmed / P3**。锚点抽查（2026-08-20）：global_hub.py:662 `directory = global_event.get("directory")` 先于 :665 `isinstance(payload, dict)` 守卫 + :1052 `self.publish(orjson.loads(…))` 仅 :1053-1054 捕 JSONDecodeError 命中——合法 JSON 非 dict 形逃逸 → publish 抛 AttributeError → run() except 按上游失联放大（resync_all + 重连）链成立。上游恒对象帧（E8）→ 防御纵深缺口定性（非现实触发）复核认同。原状态行括注「A6/D06 2026-08-20」由本节承接。
## 建议方向（非实现）
- publish 入口补 `if not isinstance(global_event, dict): return`（与 :665 payload 守卫同型，计数可选）——毒帧降级为单帧丢弃而非连接拆断。
