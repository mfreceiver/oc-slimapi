# F-313：read_groups.py 名不副实——630 行承载 12 个端点 4 个资源域（file/vcs/providers/session-misc）
- 严重度：P3（可维护性）
- 类别：design（上帝文件，A11 数据驱动入围：行数>500(630) + 扇出全仓第二(15)）
- 位置：src/oc_slimapi/routes/read_groups.py:90-630
- 置信度：high（router 装饰器行号实测）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 12 个端点 4 族共栖（@router 行号实测）：
  - file/vcs 族 7 端点：file_list(:149)/file_content(:166)/file_status(:178)/vcs_info(:192)/vcs_status(:201)/vcs_diff(:210)/find_file(:227)，共享 `_authorized_file_directory`(:123)；
  - providers 族 1 端点：config_providers(:395) + `_handle_providers_v4`(:247-381，~135 行 v4 门控逻辑)；
  - session-single 族 3 端点：session_single(:555) + `_session_single_sql`(:425)/`_project_native_session_single`(:440)/`_session_single_native_fallback`(:455)/`_handle_session_single_v4`(:520)（dbaux 投影专属 ~205 行）；session_active(:582)/global_health(:591)/session_context(:619)。
- 文件名「read_groups」是 v2 时代分组残留；session_single 的 SQL 投影知识与 file/vcs 透传知识无共享符号（除 `_resolve`/`_project_session` 两通用小 helper）。
- 变更频率 6、缺陷 0（F-253 的 InvalidURL 族波及 `_catalog_common` 而非本文件特有）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：按「读操作」粗粒度分组而非按资源域——providers（4.2/4.4 两轮修订面）与 session-single（4.0 新增 dbaux 投影）持续往同一文件堆叠。
- 影响面：v4 修订面（providers、session-single 都是活跃修订区）与稳定透传族（file/vcs）互相拖累 review 范围。
- 触发条件：下一轮 v4 修订。
- 可达性：持续（4.2.0/4.3.0/4.4.0 三轮修订均触达本文件）。
## 契约/文档对照
- INTERFACE_MAP 按端点登记（不按文件）——拆分零文档成本，仅需同步 INTERFACE_MAP 实现列。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 `rg -c '@router\.(get|post|…)' read_groups.py` → **12 端点** ✓；wc -l = 630 ✓；file/vcs 7 端点 + providers + session-single 族的行号分段抽核一致。
- 维持 P3 / confirmed。原状态注记移入：draft（A11/D11 §4.4）。
## 建议方向（非实现）
- 拆三文件（保持 router 前缀不变，app.py include_router 改三行）：
  - `routes/files_vcs.py`（~270 行：7 端点 + `_authorized_file_directory` + `_resolve`/`_project_session` 副本或提取到 `_read_common.py`）；
  - `routes/providers.py`（~165 行：_handle_providers_v4 + config_providers）；
  - `routes/session_single.py`（~240 行：session-single 族 + misc 3 端点）。
- 测试锚点：5 个测试文件 16 处引用（rg 实测）均按端点路径/函数锚——拆分零迁移。
