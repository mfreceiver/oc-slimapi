# F-154：actions 第 8 码 `invalid_request_body`（422 POST body 畸形）不在 v2-contract §2 七码表内——三契约皆无的主码
- 严重度：P3（§3.3 contract-violation 行——未载类；actions 面窄、错误路径表示细节）
- 类别：contract
- 位置：src/oc_slimapi/routes/actions.py:94-105（orjson 解码失败 / 非 dict / `confirm` 非 bool → 422 `invalid_request_body`，附 `Cache-Control: no-store`）；docs/specs/v2-contract.md:239-246（§2 actions 错误表恰 7 码：action_not_found/action_confirm_required/action_throttled/actions_disabled/action_unavailable/action_busy/action_timeout + 413 request_too_large）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- routes/actions.py:94-105 三处 `CodedHTTPException(422, code="invalid_request_body", ...)`；
- 负向搜索：`rg -n 'invalid_request_body' docs/specs/ docs/ CHANGELOG.md` → 0 命中（v2 七码表、v3/v4、CHANGELOG 全无）；
- v2-contract.md:205 附近仅载「空 body 或 `{}` → 视为 `{}`」「require_confirm 缺 confirm → 409」——畸形 JSON / 非 object / confirm 错型的归宿未定义，实现自设 422 第 8 码。
## 分析（机制、影响面、触发条件、可达性）
- 三向对账「无主码」之一（且连 CHANGELOG 都无记载——三向全空）。实现行为合理（422 + no-store 与仓库 422 惯例一致），但加性 wire 行为（新错误码）按 AGENTS 硬规则应记 CHANGELOG、按流程应有契约归宿——两者皆缺。
- 触发条件：POST /slimapi/actions/{name} 携畸形 JSON / 非 object body / confirm 非 bool。actions 为活路由（生产 manifest 已启用，见 F-019 A3 补证）。
## 契约/文档对照（规范轨引文）
- AGENTS.md 硬规则「接口行为变更记录：每次破坏/加性 wire 行为变更必须记 CHANGELOG」；v3 §0.1（未提及语义沿 v2——v2 亦未载）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——routes/actions.py:94-105 三处 422 invalid_request_body 命中。证据链成立，维持严重度与建议方向。
- P3 负向 rg 重放：`rg -n 'invalid_request_body' docs/ CHANGELOG.md` 命中全部为 docs/audits/ 审计自身产物（route-census/inventory/F 文件/D04），docs/specs/ 与 CHANGELOG.md 零命中——三向无主成立。
- 状态行归一：confirmed（原「verified（与 F-019 同族：F-019 主辖「权威链断点」，本条主辖「表外加性码」）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 与 F-019 收编动作合并处置：actions 错误表补第 8 码（422 `invalid_request_body`）或改并既有 409 语义；CHANGELOG 补记。
