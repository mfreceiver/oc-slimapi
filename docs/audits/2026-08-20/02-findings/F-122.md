# F-122：SSE resync reason 值域封闭性无运行时强制——route 层直发 resync 帧不经 V4_RESYNC_REASONS 门控，log 层新增第五 reason 会未经门控直上 wire
- 严重度：P3（初判；当前值域事实上封闭，风险为结构性防线缺失而非现行缺陷）
- 类别：risk
- 位置：src/oc_slimapi/routes/events.py:196-198、src/oc_slimapi/routes/token_stream.py:270-272（route 层直发）；对照门控点 src/oc_slimapi/sse/hub_types.py:311-317、src/oc_slimapi/sse/tokenstream/subscriber.py:444-489、src/oc_slimapi/sse/tokenstream/hub.py:1502-1526
- 置信度：high（升级 01-explore/parts/e1-09-replay.md 疑问 8 为正式发现；代码路径实读）
- 快照：0b836e7
- 状态：confirmed
## 证据
- events.py:196-198 与 token_stream.py:270-272：route 层在 replay 计划返回后直接 `yield` resync 帧——不经过任何 `V4_RESYNC_REASONS` 成员检查
- 安全性现状仅靠两条事实：① replay_plan 的 reason 来自 replay log 层封闭四值常量（`sse/replay_wire.py`）；② v3 fallback 路径 reason 恒为 `RESYNC_RECONNECT_NO_REPLAY`（events.py:124-126、token_stream.py:164-167）
- 对照：hub 侧两处门控把域外 reason 转 v4 静默 STOP——hub_types.py:317（`if self.wire_v4 and reason not in V4_RESYNC_REASONS`）、subscriber.py:450/:489、hub.py:1522-1526（`_fanout_resync`）
- v4-contract §7.2：「resync 帧 reason 值域（v4 冻结，加性扩展）：`epoch_changed` | `replay_expired` | `replay_gap` | `reconnect_no_replay`」——契约冻结四值
- 无运行时 assert / 常量集校验拦截「log 层新 reason → route 层上 wire」路径（rg 全仓无第五 reason，现状封闭成立）
## 分析（机制、影响面、触发条件、可达性）
- 机制：值域封闭性由「字面量纪律」维持——两处门控只覆盖 hub 侧 fanout 路径，route 层 replay-plan 直发路径在门控覆盖面之外。
- 触发条件：未来任何实现者为 replay log / replay_plan 增加新 reason 常量（例如新的逐出策略 reason）且未同步 V4_RESYNC_REASONS——v4 wire 立即出现域外 reason，违反 §7.2 冻结值域，客户端按未知 reason 处理路径未定义（契约未规定客户端对未知 reason 的行为）。
- v3 面不受影响（v3 reason 值域本就开放，legacy reason 是 v3 冻结行为）。
- 可达性：当前不可达（无第五 reason）；属 v3/v4 双语义长期维持中的防线缺口。
## 契约/文档对照（规范轨引文）
- v4-contract §7.2（reason 值域冻结四值）、CHANGELOG [4.0.0]（rev-gate R3 裁决：域外 reason 在 v4 不发帧直接终结）
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——events.py:196-198 与 token_stream.py:270-272 route 层 ReplayResync 直发（不经 V4_RESYNC_REASONS 门控）命中；对照门控点 hub_types.py:317 等引用保持有效。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「draft（A2/D02 产出；v3/v4 双语义维持成本项下的结构性风险）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 机械性前置准备类（D02 §5.5-5）：为 route 层直发路径补一条测试级断言（reason ∈ 冻结值域 ∪ v3 legacy 集），或将 route 层直发统一收经既有门控 helper——纯防线加固，零 wire 变化。
