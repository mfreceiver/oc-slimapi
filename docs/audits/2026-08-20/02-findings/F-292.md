# F-292：A10 卫生合集——global_hub dead import `logging`、messages 陈旧 getattr 回退 8MiB（死默认）、`_strip_directory_query` 逐字节 ×2、resolve/validate stanza ×10、Retry-After 字面量 "5" ×2、无静态门禁
- 严重度：P3
- 类别：quality（卫生合集；单项均零行为影响除特别注明）
- 位置：见下表逐项
- 置信度：high（逐项 rg/直读验证）
- 快照：0b836e7
- 状态：confirmed
## 证据

| 项 | 位置 | 验证 |
|---|---|---|
| dead import `logging` | sse/global_hub.py:11 | 文件内 `logging.` 使用 ∅（logger 来自 :25 `logging_config.get_logger`；:88/:542 为注释） |
| 陈旧 getattr 回退 `8 * 1024 * 1024` | routes/messages.py:1567-1569 | Settings 已有 `max_expand_response_bytes`（config.py:372-374 dataclass 字段必在）→ 回退分支永不触发；:1565-1566 注释自认 lane C 过渡「keep the fallback until integration」，集成已完成 |
| `_strip_directory_query` 逐字节 ×2 | read_groups.py:602-616 ≡ write_groups.py:504-514 | 含语义 docstring 全同 |
| resolve→validate stanza ×10 | agent.py:37-39 / command.py:34-36 / todo.py:66-68 / children.py:72-74 / diff.py:88-90 / sessions.py:708-710,:832-834 / messages.py:309-316（变体）/ read_groups.py:106-112 / write_groups.py:106-109 | 6 处逐字节同（`resolve_route_directory`→`if not None: validate_directory`） |
| Retry-After 字面 `"5"` ×2 | events.py:137、token_stream.py:180 | 对照具名常量：TRANSFORM_RETRY_AFTER_SECONDS=2（×2 模块）、_AUX_RETRY_AFTER="30"（sessions.py:274） |
| 无静态类型/风格门禁 | pyproject.toml（无 mypy/ruff 节）、scripts/check.sh（=pytest+路由文档 gate+compileall） | 注解密度抽样（sorted(inventory)[2::3]，23/71 模块）：返回注解 89%、形参 75%——自觉水平无防退化门 |

- 佐证：03-reports/D10-code-quality.md §1.iii/§6/§7。
## 分析（机制、影响面、触发条件、可达性）
- 六项均为低风险清理/加固项；唯一行为性注记=messages 陈旧回退若 config 字段未来改名将静默回落 8MiB（现不可能：dataclass 字段静态存在）。
- stanza ×10 与 `_strip_directory_query` ×2 归并后 routes 层入口逻辑单一真源（selector 侧）。
## 契约/文档对照（规范轨引文）
- 全部内部实现细节，零 wire 面；metrics/快照键无涉。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：六项逐一定点核对：global_hub.py:11 `import logging`（文件内零 `logging.` 调用，logger 经 logging_config）✓；messages.py:1567-1569 getattr 回退 8MiB + :1565-1566 lane C 注释 ✓；`_strip_directory_query` read_groups.py:602 / write_groups.py:504 双拷贝 ✓；Retry-After 字面 "5" events.py:137 + token_stream.py:180 ✓；pyproject 无 mypy/ruff 节 ✓。
- 维持 P3 / confirmed（卫生合集）。原状态注记移入：verified（A10/D10 2026-08-20）。
## 建议方向（非实现）
- 单 cleanup PR：删 dead import 与陈旧回退（直取 config 字段）；`_strip_directory_query` 上移 selector.py；stanza 收敛 `resolve_route_directory_or_none()`；Retry-After 容量族具名常量；refactor backlog 候选=mypy（或 pyright）渐进门禁 + ruff（F-018 respx 死依赖同批清理）。
