# F-217：上游 SSE 流 EOF 时未终结 data 块（无尾空行）静默丢失
- 严重度：P3
- 类别：defect
- 位置：src/oc_slimapi/sse/global_hub.py:1027-1055（SSE 组帧循环）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- :1027 `data_lines: list[str] = []`；:1048-1050 仅 `data:` 前缀行累积、空行触发 publish；
- :1050 `elif not line and data_lines:` —— publish 条件依赖空行终结；aiter_lines 正常耗尽（EOF）后循环退出，**`data_lines` 残留无 flush**（:1056 直接进入 EOF 处理）；
- 对照上游（E8 §3.1）：`handlers/global.ts:16-23` 每帧完整 `event: message\ndata: <json>\n\n`——正常路径恒有空行；丢帧仅在「连接在帧中途被切断」时发生（服务端写完 data 行未写空行即断）。
## 分析（机制、影响面、触发性）
- 机制：EOF 前最后一个不完整帧丢弃且无日志。EOF 本身走 INV-6 丢失通知 + barrier（:1056-1072），客户端被引导 HTTP 对齐——丢失帧多为半帧（JSON 可能不完整），丢弃语义可辩护；但「完整 JSON 已到、仅缺空行」的帧也被丢（publish 未尝试），且无 debug 计数区分。
- 触发：上游异常断连瞬间恰在帧边界外；低频。影响：单帧信息损失，被既有 resync 语义覆盖。
## 契约/文档对照（规范轨引文）
- v3-contract §7.8 盲区二（SSE 断连窗口无补偿）——本发现是其实现层细化，不新增 wire 偏离。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——global_hub.py:1027 data_lines 声明、:1048-1050 空行触发 publish、:1056 起 EOF 处理（无残留 flush）命中。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「verified（A6/D06 2026-08-20；Phase 3 终审）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- EOF/异常路径对残留 `data_lines` 尝试一次 publish（orjson 失败自然落入既有 JSONDecodeError 守卫），或至少 debug 计数 `eof_partial_frames`。
