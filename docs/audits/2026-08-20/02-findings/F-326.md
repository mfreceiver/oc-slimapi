# F-326：「byte/verbatim/frozen」命名的测试多数实为字段级断言——真·字节级回归仅 4 族；命名强度与断言强度系统性错位
- 严重度：P3（保护本身多数足够强：全字典相等/键序锁定；错位在命名让人高估保护层级）
- 类别：test
- 位置（命名↔实际强度错位样例）：tests/test_versions_readiness.py:504（`…_byte_unchanged` → 仅 4 键值断言）；tests/test_v3_envelope.py:133（`…_byte_verbatim` → startswith + 键序）；tests/test_v4_dual_window.py:197（docstring "byte-regression" → 键级 + 负向键）；tests/test_messages_routes.py:803（`x_next_cursor_is_byte_for_byte_verbatim` → cursor 字符串相等，非整body）
- 真·字节级回归 inventory（A12 全量核对）：① test_v3_rawbody_regression.py:198（sessions body_hex 全等 + PYTHONHASHSEED=0 围栏；health/versions 同态归一 sidecarVersion 后字节比较）；② test_sse_replay_wire.py:2480,2498,2514（events/stream 帧序列整 body 字节字面量全等 ×3）；③ test_expand_href_v4.py:271（href 冻结字节子串 + 负向 `?v=4` 不出现——子串级字节）；④ test_providers_projection_v4.py:967（v3 passthrough byte-identical，整 body 比对）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
见上 inventory 与样例行号；四个「错位」样例均逐行读断言体核对（键级/子串级/字典级，非 raw bytes 全等）。
## 分析（机制、影响面、触发条件、可达性）
- 字段级全字典相等（如 test_versions_caps3_face_zero_change 的整 dict ==）对键序不敏感、对序列化层变化（gzip/编码/分隔符）不敏感；命名暗示的「逐字节」会让人误以为序列化层也被冻结。实际序列化层锚定仅上述 4 族承载（sessions/health/versions/SSE 帧/href/providers v3 面），messages envelope 等其余「byte」命名不在其列。
- 影响：评审者/后续审计以测试名为据会高估 v3 冻结的机械强度（本仓 AGENTS「?v=3 逐字节不变」承诺的自动化证据面实际是 4 族 + 一批字段级锁）。
## 契约/文档对照（规范轨引文）
- v4-contract §0.1（?v=3 管线逐字节不变——承诺面 vs 测试证据面的差值即本条）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 test_versions_readiness.py:504（`…_byte_unchanged` 命名）与 test_v3_rawbody_regression.py:198（`test_sessions_v3_raw_body_bytes` 真·字节级）打开核对一致；4 族字节级 inventory 与 4 个「错位」样例的断言体口径抽核维持。
- 维持 P3 / confirmed。原状态 verified。
## 建议方向（非实现）
- 二选一：改名（_byte_→_shape_/_field_）或升级断言到 content 全等；至少在 test_v3_rawbody_regression.py 文件头补充「全仓字节级回归覆盖清单」指明其余面为字段级。
