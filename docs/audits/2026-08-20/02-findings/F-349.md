# F-349：INTERFACE_MAP.md 描述列漂移族——banner/versions/health 行 [3]/[3,3]/current=3 未随 4.0.0 更新、metrics 行要求已删头、九处双值 Vary 与自身 banner 矛盾、catch-all 收编计数过期
- 严重度：P3
- 类别：docs
- 位置：docs/specs/INTERFACE_MAP.md:7,14,95-97,98,99 + 双值 Vary 九处 :25,26,31,32,33,34,35,39,51
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- **版本窗滞后**：:7 头部横幅「⚠ v3-only 终态（M3）…`unsupported_version","supported":[3]`」+ :14 §0 selector 段同款 + :95 versions 行主值「`current=3`、`available=[3]`」（行内已补 4.0.0 v4 注记但主值未改）+ :96 health 行「M3 终态单视图…三者恒 3…M3 起 [3,3]」+ :97 ready 行——实际 current=4/available=[3,4]/health 双视图（v4-contract §3.1/§3.2；versions.py:61-64；health.py:30-85）。
- **已删头**：:98 metrics 行「须带 `X-Slimapi-Version:int`…缺/坏/越界版本头→门闩 400」——头已删（v3 §1），metrics 与其他 /slimapi 路由同走 `?v=` selector。
- **双值 Vary 九处**（:25/:26/:31/:32/:33/:34/:35/:39/:51，含 B1 ETag 注记「Vary 合并为 `Accept-Encoding, X-Opencode-Directory`」与 todo/children/diff/file/写路由返回列主值）——实现恒单值：etag.py:171-176 `merged_vary()` 无条件返回 `"Accept-Encoding"`（docstring：「The X-Opencode-Directory dimension is retired」）；test_vary_directory_unconditional.py 11 用例锁定。与自身 :7 横幅「Vary 全路由收缩为 `Accept-Encoding` 单值」**文内自相矛盾**。
- **计数过期**：:99 catch-all 行「收编面冻结（§8.2）：…§10.a 读 7 组 + §10.b 写 12 端点…」——v3 §10 现行读 9 组 + 写 17 端点（v3-contract.md:214；3.1.0/3.3.0 两次扩容未回写；v4 三条 POST 后 54 条）。
- gate 盲区确认：check_routes_doc.py 只校验表行 method+path 存在性与 7 路由错误码子串（check_routes_doc.py:71-87），版本窗字面/Vary 值/头要求/计数 prose 全部不在保障范围——本条全部漂移均过 gate。
## 分析（机制、影响面、触发条件、可达性）
- 机制：INTERFACE_MAP 是 check_routes_doc 的对账文档，gate 保证「行存在」不保证「行内容正确」；4.0.0 与 3.1.0/3.3.0 的加性变更走行内注记补丁，主值/横幅/计数未回写。
- 影响面：以 INTERFACE_MAP 为实现追踪面的读者（含 ocdroid 侧对照）会读到错误版本窗与已删头要求；banner 有「v2 时代注记以终态声明为准」兜底，但双值 Vary 的 B1 注记是 v3 时代（2026-08-16）写入、不属于「v2 时代注记」，兜底不覆盖，构成真矛盾。
- 定级：追踪面文档（非契约权威，冲突以契约为准 per AGENTS）——P3。
## 契约/文档对照（规范轨引文）
- v4-contract §3.1/§3.2（versions/health 双视图）；v3-contract §1（头退役）/§6.2（2026-08-19 修订注记：全部 /slimapi ETag 路由恒单值 Vary）/§10（读 9 + 写 17）；etag.py:171-176；test_vary_directory_unconditional.py。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 INTERFACE_MAP.md:7（横幅 `supported":[3]`）/:95（versions 行 `current=3、available=[3]`）/:98（metrics 行「须带 `X-Slimapi-Version:int`…门闩 400」）/:99（catch-all 行「读 7 组 + 写 12 端点」）打开核对一致；etag.py:171-176（`merged_vary` 无条件返回 "Accept-Encoding"，docstring "The X-Opencode-Directory dimension is retired"）✓；:25 行内 `Vary 合并为 Accept-Encoding, X-Opencode-Directory` 双值注记实存——文内自相矛盾实证。
- 维持 P3 / confirmed。原状态注记移入：verified（A14）。
## 建议方向（非实现）
- 横幅与 selector 段/versions/health/ready 行改 (3,4) 口径；metrics 行改 `?v=` selector；九处 Vary 统一单值；catch-all 行计数改「读 9 组 + 写 17 端点（+v4 三条 POST，54 条）」。纯文档变更；若考虑给 check_routes_doc 增加版本窗字面断言属可选加固（非必须）。
