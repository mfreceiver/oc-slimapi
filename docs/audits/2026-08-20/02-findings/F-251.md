# F-251：E-II 明文无认证全功能面——0.0.0.0:4097 × directory_allowlist 默认 None × sidecar 零认证/零授权（部署边界未验证）
- 严重度：P1（**E-II 入口专属**；E-I/E-III 下同一代码面由 stunnel mTLS 护住，不适用）
- 类别：security（部署组合面）
- 位置：deploy/oc-slimapi.service:28（`OC_SLIMAPI_HOST=0.0.0.0`）；src/oc_slimapi/config.py:356（默认 127.0.0.1 被 deploy 覆盖）；src/oc_slimapi/routes/read_groups.py:123-143（allowlist 门仅覆盖 file 三路由）；src/oc_slimapi/routes/questions.py / permissions.py（跨目录聚合）；src/oc_slimapi/routes/directories.py:17-108
- 置信度：high（代码/部署文件级；**可达性依赖 Tailscale ACL 实效——不可实机验证，标「部署边界未验证」**）
- 快照：0b836e7
- 状态：confirmed
## 证据
- deploy/oc-slimapi.service:26-27 自述：「:4097 为明文，远程暴露必须靠 stunnel mTLS / Tailscale ACL 隔离」——认证/授权完全外置。
- sidecar 进程内零认证原语：middleware 栈 = RequestId → TrafficAccounting → Selector → 路由（app.py:747-755），无任何身份校验层；selector 的 `?v=`/directory 消费是协议路由不是授权。
- `directory_allowlist` 默认 None=不过滤（config.py:498 `field(default_factory=_directory_allowlist_env)`，env 缺席即 None；config-census §1 #34）；deploy unit 未设置该 env → 照模板部署=任意 directory 放行。
- 可达能力面（E-II 攻击者视角）：① 任意目录文件内容读——`GET /slimapi/file/content?path=…&directory=<任意目录>&v=3`（上游 containment 只约束「path 在 directory 子树内」，directory 本身不受限，opencode-src handlers/file.ts:96-99 + core/filesystem.ts:68-77）；② 跨目录枚举与聚合——/slimapi/directories（全工作目录绝对路径，directories.py:187-195）、/slimapi/questions、/slimapi/permissions（按全局会话目录扇出）；③ 全部写路由（write_groups.py §10.b 12 端点：session create/patch/delete/prompt_async/abort/…）；④ providers v3 密钥面（F-017，E-II 维度 HIGH）；⑤ openapi.json 侦察（F-137）；⑥ 条件性 actions exec（若 OC_SLIMAPI_ACTIONS_FILE 启用——manifest 固定 argv 但 confirm 是客户端自报 bool，非授权；actions.py:12-19 risk-accepted 自述、routes/actions.py:144）。
- 加重因素：F-252——即使运维设置 allowlist，vcs/find/providers/messages/写路由等仍不查 allowlist（覆盖面不完整），allowlist 不能单独闭合本面。
## 分析（机制、影响面、触发条件、可达性）
- 机制：设计信任链=「stunnel mTLS（或 loopback）之后皆受信」；deploy 单元为 Tailscale 直连把监听放宽到 0.0.0.0 后，该信任链的唯一一环变成外部 ACL。
- 影响面：单用户工作站上的全部 opencode 会话/文件/写操作（含删除会话、触发 prompt）+ provider 密钥读取。
- 触发条件：ACL 失效/误配/新增网络界面；任何能路由到 :4097 的主体。
- 可达性：仓库内证据链完整；实际网络可达性=部署边界未验证。
## 契约/文档对照
- AGENTS.md 架构图（E-II 为「deploy 单元现实」）；docs/operations.md:88 记载 HOST=0.0.0.0 有意。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——deploy/oc-slimapi.service:26-28（注释自述 :4097 明文依赖 stunnel mTLS/Tailscale ACL + `Environment=OC_SLIMAPI_HOST=0.0.0.0`）命中；config.py:356 默认 127.0.0.1 被覆盖命中；read_groups.py:123-143 allowlist 门（None→ungated）命中——「部署边界未验证」限定保留，P1（E-II 入口专属）维持。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「draft（A8/D08 §2.2/§3）」辖属/批次注记移入本记录）。
- 2026-08-20 Phase 3 V2 深度复核（P1 专项；三步证伪）：**(1) H8 依赖解除**——发现原推演依赖「deploy 文件=生产实况」（H8：单元文件为权威样本但本机非生产机）。本轮只读实机观测：`systemctl --user is-active oc-slimapi` → **active**；`ss -tlnp` → **`0.0.0.0:4097 LISTEN (python,pid 5762)`** + `0.0.0.0:14097 (stunnel4)`；生产 unit（`systemctl --user cat`）:29 `Environment=OC_SLIMAPI_HOST=0.0.0.0`——**E-II 绑定姿态在本机为运行中 socket 事实，非模板推定**；未验证残项收窄为「远程主体能否路由到 :4097（Tailscale ACL 实效）」，与「部署边界未验证」标注所指一致，标注语义准确、已满足。**(2) sidecar 层缓解反证搜索**：`rg "auth|token|secret" src/oc_slimapi/middleware/ src/oc_slimapi/app.py` 零认证原语；`directory_allowlist` 默认 None（config.py:498）且生产 unit 未设置；config.py:494 自述「NOT a security guarantee (real isolation = stunnel mTLS + network edge)」——确实无遗漏的缓解层（allowlist 亦受 F-252 覆盖面限制）。**(3) 定级复核**：维持 P1 不升 P0（远程可达性未经实机证实、单用户工作站 ACL 默认可信）、不降级（活绑定 + 零认证 + 全功能面成立）。终判 confirmed / P1。
## 建议方向（非实现）
- 首选：生产 unit 回归 `OC_SLIMAPI_HOST=127.0.0.1`（stunnel 已在本机 accept 127.0.0.1:14097，Tailscale 直连需求可由 stunnel/或专门监听面承担）；若必须 0.0.0.0，则至少：设置 directory_allowlist（并先修 F-252 覆盖面）+ 在 operations.md 写明 ACL 是唯一认证层的显式风险接受条款 + 考虑最小 per-token 门（如 X-Slimapi-Shared-Secret 环境变量级）作为纵深。
