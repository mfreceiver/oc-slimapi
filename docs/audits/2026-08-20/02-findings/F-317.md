# F-317：错误路径锁定大量依赖「家族代表」单端点断言——directory_conflict/multi-value/request-cap/response-cap 等仅在被 parametrize 覆盖之外的 1 条端点上断言
- 严重度：P3（共享中间件/共享 cap 管线使漂移风险低；但参数化纪律不一致，新增端点易漏）
- 类别：test
- 位置：tests/test_write_groups.py:305-325（conflict/multi-value 仅 POST abort）；:385-405（request/response cap 仅 create）；04-final/test-gap-matrix.csv（P3 行 241 条中的构成：error_directory_conflict×29、error_response_too_large×33、error_invalid_version_selector×45、error_unsupported_version×28、error_directory_header_retired×26、error_invalid_directory_selector×24、error_request_too_large×11 等）
- 置信度：high（逐测试函数体核对 parametrize 装饰器与 URL 字面）
- 快照：0b836e7
- 状态：confirmed
## 证据
1. test_write_groups.py 同文件两种纪律并存：happy/body-forward/v2-header/invalid-query/upstream-4xx/5xx 对 12 端点 **parametrize**（:182/:210/:233/:247/:265/:332/:347）；而 directory_conflict（:305）、multi-value invalid_directory_selector（:317）、request cap（:385/:528）、response cap（:399）、network-error（:358）、3xx verbatim（:372）、query verbatim（:409）、strip 断言（:422）**只用 abort 或 create 单端点**。
2. 读组同样：test_read_groups.py:554-606 的 directory 三形态仅 file/vcs/find 承载；response 413 仅 file（:517）；upstream 5xx 则 11 路径全扫（:420-433，正面样例）。
3. 矩阵呈现：580 行中 241 行为 P3（= 仅家族代表或函数级锁定）；error_directory_conflict 31 行中 29 行 P3。
## 分析（机制、影响面、触发条件、可达性）
- 这些错误全部产生于共享层（selector 中间件 / read_with_cap / 写转发管线），路由本体无分支；单代表断言 + 共享层实现 = 实际回归保护率高。风险集中在**新增第 13 条写端点/新读路由时**，无 parametrize 骨架强制带动，容易只复制 happy 测试（历史先例：B4 新路由测试就未覆盖 conflict/cap 面）。
## 契约/文档对照（规范轨引文）
- v3-contract §8.3（错误优先级链路由无关）；§10（写端点统一行为）——契约本身按「族」定义，测试按族代表锁定可辩护，但 parametrize 纪律不齐是内部不一致。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 test_write_groups.py:305-325（conflict :305 / multi-value :317 仅 POST abort 单端点）与 :385/:528（over-cap 单端点）打开核对一致；`rg -c parametrize` = 7 处（对 12 端点全跑的仅 happy/4xx/5xx 族）——两种纪律并存实证。
- 维持 P3 / confirmed。原状态 verified。
## 建议方向（非实现）
- 把 :305-325 与 :385-405 的四个单端点测试并入既有 ENDPOINTS parametrize 骨架（conflict/multi-value/cap 对 12 端点全跑，成本 = 参数化改造，运行时间增量可忽略——纯 400 早退，无上游 IO）。
