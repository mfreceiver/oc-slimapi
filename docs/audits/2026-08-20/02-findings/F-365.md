# F-365：check.sh compileall 在 src/ 产生 __pycache__ 副产物无清理（树内残留实证）
- 严重度：P3（低）
- 类别：quality
- 位置：scripts/check.sh:27-28（`compileall -q src`，无清理）；残留实证：`scripts/__pycache__/`、`src/oc_slimapi.egg-info/` 同类生成物并存
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- scripts/check.sh:27-28 直接 `"$PY" -m compileall -q src`——每次门禁在 `src/oc_slimapi/**/__pycache__/` 写入 .pyc；脚本无清理步骤。
- 工作树实证（A15 ls）：`scripts/__pycache__/` 已存在（check_routes_doc.py 被 `python scripts/…` 方式执行 :25 所致）、`src/oc_slimapi.egg-info/`（editable 安装生成）。
- 兜底：.gitignore:2 `__pycache__/`、:4 `*.egg-info/`——全部被吸收，`git status --short` 干净（仅审计工作区 untracked），**无入库风险**。
## 分析（机制、影响面、触发条件、可达性）
- 影响面：纯本地树卫生（IDE 索引噪音、`grep -r` 命中 .pyc 干扰、陈旧 .pyc 在极端 Python 版本切换后可能误导 importlib 缓存判断）；对发版零影响（gitignore 兜底 + release.sh 干净树检查只查已跟踪文件 release.sh:29-33）。
- 触发：每次跑 check.sh 必然产生。
## 契约/文档对照（规范轨引文）
- AGENTS.md 硬规则「禁止把 secret / `.venv` / 本机路径密钥提交进仓」——生成物入库防线（.gitignore）已满足；无规范违反，仅卫生。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 check.sh:27-28（`compileall -q src` 无清理步骤）打开核对一致；scripts/__pycache__/ 与 egg-info 残留、.gitignore:2/:4 兜底口径维持（A15 快照证据，manifest 一致）。
- 维持 P3 / confirmed。原状态注记移入：verified（2026-08-20，D15 / A15）。
## 建议方向（非实现）
- compileall 后 `find src -name __pycache__ -type d -exec rm -rf {} +`（或 `-b` 直接字节码写到 `__pycache__` 外的旧布局也可评估）；scripts/__pycache__ 同理可用 `PYTHONDONTWRITEBYTECODE=1` 调 check_routes_doc.py。
