# F-322：写组 request_too_large 仅「超限侧」单边锁定——无 at-cap/低于-cap 边界测试（全仓唯一双侧 cap 边界测试在 actions POST）
- 严重度：P3（cap 判定为共享 read_with_cap/长度预检，off-by-one 风险低；但边界纪律只对 actions 落地）
- 类别：test
- 位置：tests/test_actions_routes.py:463-489（`test_post_body_content_length_over_cap_413` + `test_post_body_at_cap_boundary_not_413`——全仓唯一 at-cap 边界）；tests/test_write_groups.py:385-405（create 单端点、仅超限侧）；tests/test_expand_routes.py:815-861（expand source/fragment **双侧** at-cap/one-over——读侧正面样例）
- 置信度：high（rg at-cap/exactly/one_byte_over 全量 + 逐读）
- 快照：0b836e7
- 状态：confirmed
## 证据
1. `test_post_body_at_cap_boundary_not_413`（:477）断言恰等于 cap 的 body 不 413——唯一的请求侧边界值测试。
2. 写组 12 端点的 request cap 只有 over-cap（test_write_request_body_over_cap_413 :385、test_write_request_cap_zero_upstream_calls :528）；POST 等效三动作族同样仅 over（test_post_actions_v4.py:302,360,517）。
3. 读侧对照：expand source/fragment 有完整双侧（test_expand_routes.py:815/829/843/855），messages full 有 under/over 对（test_messages_routes.py:613,643）——证明双侧纪律在本仓有先例、可复制。
## 分析（机制、影响面、触发条件、可达性）
- 若写转发 cap 判据从 `> cap` 漂移为 `>= cap`，现测试全绿（at-cap 请求被误拒的场景无人断言）。触发面：任何 cap 比较式的重构。影响 12+3 写端点 × 消费方写路径。
## 契约/文档对照（规范轨引文）
- v3 §10.b/v2 继承链：request_too_large 413 语义（阈值行为未逐字冻结「恰等于 cap 放行」，但与 actions/expand 的既有边界语义一致性是仓内事实标准）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 test_actions_routes.py:477 `test_post_body_at_cap_boundary_not_413`（body==1024 恰 cap → 422 非 413）打开核对一致——全仓唯一请求侧 at-cap 边界；test_write_groups.py:385/:528 仅超限侧复现；test_expand_routes.py 双侧对照在案。
- 维持 P3 / confirmed。原状态 verified。
## 建议方向（非实现）
- 把 actions 的 at-cap 用例模式复制为 ENDPOINTS parametrize 一行（body 长度 == max_message_bytes 断言非 413）。
