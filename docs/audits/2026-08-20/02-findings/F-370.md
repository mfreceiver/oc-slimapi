# F-370：测量资产无门禁联动——measure_token_overhead main() 恒 return 0、零测试引用、.md 数字快照无防漂移
- 严重度：P3
- 类别：gap
- 位置：scripts/measure_token_overhead.py:410-412（`return 0` 无条件）、scripts/measure_token_overhead.md（结果快照，判定 NO：gzip median 1.47x）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- scripts/measure_token_overhead.py:410-412：`main()` 末尾无条件 `return 0`——MISSES 目标（1.47x > 1.2x）也不失败退出（docstring 定位为测量 harness 非门禁，属设计选择）。
- 零消费面：`grep -rn measure_token_overhead tests/ scripts/check.sh` 零命中（rc=1）——check.sh 不跑它，tests 不引用它；.md 报告数字（12 trace 表、median 1.47x/11.04x）纯快照。
- 双向漂移无机制：脚本常数/生成器改动后 .md 不会同步失效；反之 .md 数字被误改也无校验。（.py:11-12 引用已不存在的 `hub.py:110` 属同族陈旧引用，已归 F-020/A14 辖。）
- 与 eqp_matrix 对比：eqp_matrix 至少被 tests importlib 复用其纯函数（v4_fixture.py:50-55）；measure_token_overhead 连间接锚都没有。
## 分析（机制、影响面、触发条件、可达性）
- 机制：一次性验证 harness（design-token-stream §11 方法论落地）完成使命后成为孤儿资产。
- 影响面：token-stream gzip 决策的量化依据（1.47x→目标重锚建议）随实现演进（frames.py/hub_types.py 现行实现）可能失真而无人知晓；后续若有人按 .md 数字做容量/带宽推演，依据可能过期。
- 触发：token stream 帧实现/flush 参数改动后无人重跑脚本。
## 契约/文档对照（规范轨引文）
- docs/specs/design-token-stream.md §11 为方法论权威；v3-contract §7 为 wire 契约——.md 快照非契约，无违规；AGENTS.md 对 scripts/ 无「必须入 CI」的声称。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 measure_token_overhead.py:410-412（main() 末尾无条件 `return 0`）打开核对一致；`rg measure_token_overhead tests/ scripts/check.sh` 零消费复现——孤儿测量资产 + .md 快照无防漂移成立。
- 维持 P3 / confirmed。原状态注记移入：verified（2026-08-20，D15 / A15）。
## 建议方向（非实现）
- 二选一：① 在 .md 头部加「快照时点 + 生成命令 + 失效条件」横幅并修 :11-12 陈旧引用（低成本）；② 把关键不变量（如 marker 帧形状/12 trace 确定性）抽成 pytest 断言进 tests/（脚本本体仍不进门禁）。
