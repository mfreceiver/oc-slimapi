# F-245：stop 后 start 不可复用无守卫——executor 已 shutdown 后首次 submit 即 RuntimeError
- 严重度：P3
- 类别：quality
- 位置：src/oc_slimapi/dbaux/lifecycle.py:388（stop 置 `_task=None`）+ :368-369（start 幂等检查据此放行）+ :410（executor 已 shutdown）
- 置信度：high（代码路径，快照 0b836e7）
- 快照：0b836e7
- 状态：confirmed
## 证据
- stop：:388 `task, self._task = self._task, None`；:402/:410 executor shutdown；:405 `_disable("stopped")`。注释 :403-404 明言「重启用新实例」。
- 再 start：:368-369 `if self._task is not None: return self.status()`——`_task` 已是 None → 幂等检查通过 → :373 `await self._open_and_gate(why="startup")` → `_submit` → `run_in_executor` 对已 shutdown executor → `RuntimeError("cannot schedule new futures after shutdown")` → 被 :552 catch-all 吞成 `open_failed` 禁用（不崩溃，但语义误导：像是打不开库）。
- 无 `_closed`/`_stopped` 防御标志；生产 app lifespan 一次性构建/销毁（app.py:598-615），正常流程不触发。
## 分析
- 触发面：仅测试/未来复用代码误调 start-after-stop。后果为静默语义漂移（open_failed）而非崩溃。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——lifecycle.py:388（stop 置 _task=None）、:368-369（start 幂等检查据此放行）、:402-411（executor shutdown）命中。证据链成立，维持严重度与建议方向。
- 严重度归一：原 P4（越出 P0-P3 刻度）→ P3（Phase 3 归一化）。
- 状态行归一：confirmed（原「verified（2026-08-20，D07 / A7，新发现）」辖属/批次注记移入本记录）。

## 建议方向（非实现）
- start 入口检查 `self._executor._shutdown` 不可直接用（私有）——加显式 `_stopped` 标志，重 start 直接 raise 或 no-op 保持 stopped 态。
