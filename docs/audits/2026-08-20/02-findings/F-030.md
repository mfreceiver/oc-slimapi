# F-030：tokenstream 两处 TODO（hub.py:663/:760）properties part/fields key casing——上游真值已定论 camelCase，TODO 可解除但未解除（维护债而非行为错误）
- 严重度：P3（终判维持初判；§3.3 smell 行——维护债，无行为错误）
- 类别：quality
- 位置：src/oc_slimapi/sse/tokenstream/hub.py:663,760；上游 `opencode-src/current/packages/schema/src/v1/session.ts:81-85,612-620`（快照 v1.18.18）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- hub.py:663 `# TODO(§13.2): confirm live wire key casing for properties.part.`——其后代码 `props.get("part")` / `part.get("sessionID")` / `part.get("messageID")`（camelCase）；
- hub.py:760 `# TODO(§13.2): confirm live wire key casing for properties fields.`——其后 `props.get("field")`；
- 上游真值（E8 定论，01-explore/upstream-notes.md §7）：v1.18.18 `packages/schema/src/v1/session.ts:81-85`（TextPart 等 part schema）与 :612-620（EventV2 `properties` 字段集）全为 camelCase（`sessionID`/`messageID`/`field`）——TODO 所述「confirm」已完成，结论 = 现码正确。
## 分析（机制、影响面、触发条件、可达性）
- 两处 TODO 引用的裁决（§13.2，指 design-token-stream.md 稿节）已被 E8 上游对照封闭：上游恒 camelCase，实现按键正确。TODO 注释滞留会误导后续维护者以为 casing 未定。
- 无行为影响；属注释维护债。
## 契约/文档对照（规范轨引文）
- v3-contract §7.1（帧名/帧形零变化）——行为不受影响；上游形状出处以 E8 笔记锚定（AGENTS.md 上游对照路径表）。
## 复核记录（Phase 3）
- **终态 confirmed / P3**（注释性维护债，行为正确）。锚点抽查（2026-08-20）：tokenstream/hub.py:663 `# TODO(§13.2): confirm live wire key casing for properties.part.` + :760 `# TODO(§13.2): confirm live wire key casing for properties fields.` 命中（本轮 sed 直读两行原文）；其后取键 camelCase 与上游真值一致（A4/A6 双核）。维护债定性维持。原状态行括注「A4 复核：实现取键已按 camelCase 正确工作，仅注释性 TODO 未解除」由本节承接。
## A6 复核记录（2026-08-20，D06）
- **维持 verified / P3**。快照直读：:663 与 :760 两处 TODO 原文在位；其后取键 `part.get("sessionID")/("messageID")/("id")`（:667-669）与 `props.get("field")`（:761-766）均 camelCase，与上游真值（schema/src/v1/session.ts:81-85,:612-620,:632-641）一致——**实质风险为零，注释性维护债定性不变**。附带确认：`field != "text"` 门（:761）与现存全部上游发布者一致（processor.ts 仅 "text"）；schema 的 field 为开放 string，上游未来新增字段名会被静默丢——契约级观察维持（不属本 TODO 范畴）。
## 建议方向（非实现）
- 删除两处 TODO 注释（或改写为「casing 已对上游 v1.18.18 验证 = camelCase」的定论注释）。
