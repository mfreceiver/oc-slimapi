# F-337：观测面自身健康零观测位——access-log handler disabled / snapshotter inactive / 维护循环停摆只有 journald warning（或完全静默），metrics 无健康块
- 严重度：P3（观测链单点失效不可见；与 F-009/F-008 构成「失效+不可见」组合）
- 类别：gap（observability）
- 位置：src/oc_slimapi/traffic_snapshot.py:341-372（start 失败永久 inactive）；src/oc_slimapi/app.py:245-250,720-726；src/oc_slimapi/routes/metrics.py:20-111（无对应块）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- traffic_snapshot.py:359-369：首帧失败 → 永久 inactive + 一条 startup warning（「inactive fail-loud」设计自述于手册 §9.1 :303）；此后进程内不再写快照，**除该条日志外无任何状态出口**——`/slimapi/metrics` 不暴露 snapshotter active 位（rg metrics.py 无该键）。
- app.py:245-250：access log enabled-but-install-failed → 一条 warning（"maintenance loop suppressed"）后 handler disabled——access log 行从此零写入，metrics 不暴露该状态；explicit `ACCESS_LOG_ENABLED=false` 则连 warning 都无。
- app.py:720-726：snapshotter start 失败仅 warning（"traffic snapshotter start failed"）。
- 组合后果：F-009（snapshot prune 停摆→目录无界增长）、F-008（legacy 堆积）、瞬时磁盘满（inactive 永不自愈 :359-369）都以「静默失效」形态存在——唯一检测手段是 journald 关键词（D13 告警表 #14），而 operations.md 无一记载这些关键词。
## 分析
- 机制：观测组件 best-effort 吞异常（设计取向「绝不影响业务」正确），但缺少对自身的元观测——「观测的观测」缺位。
- 影响面：磁盘满/权限错等一次性事件后，省流数据链无声断流；跨重启对比（snapshot 分段）时才发现缺段——回溯期数据不可补。
- 触发条件：首帧写失败（磁盘满/路径不可写）或 ACCESS_LOG_ENABLED=false / 安装失败。
## 契约/文档对照
- v3-contract §9 / v4-contract §9：观测块清单不含自身健康位（加性 ops 面，不违约）；手册 §9.1 只描述 inactive 行为未给检测手段。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 traffic_snapshot.py:359-369（首帧失败 → 单条 warning + return，永久 inactive 无重试）与 app.py:245-250（enabled-but-install-failed → warning 后 maintenance loop suppressed）打开核对一致；metrics.py 无 snapshotter/accessLog 健康键（:20-111 通读）✓。
- 维持 P3 / confirmed。原状态注记移入：verified（A13/D13 §1.3 #9；Phase 3 终态待定）。
## 建议方向（非实现）
- metrics 加 `observability` 小块：`{accessLogActive, snapshotterActive, maintenanceLoopRunning}`（app.state 已持有或可挂全部三态；zero-knowledge additive 风格与现有块一致）；operations.md §5 补三个 journald 关键词与 metrics 检查点；与 F-009 的 `snapshotPruneSuspended` 建议合并落地。
