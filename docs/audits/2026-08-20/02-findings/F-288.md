# F-288：messages.py 本地复制 `_busy_response`/`_stream_upstream`/`TRANSFORM_RETRY_AFTER_SECONDS`，且其流式变体不转发 X-Request-ID（与 catalog 链行为漂移）
- 严重度：P3
- 类别：smell（重复 + 可观测性漂移）
- 位置：src/oc_slimapi/routes/messages.py:44（常量）、:268-284（_busy_response）、:319-334（_stream_upstream）；对照 src/oc_slimapi/routes/_catalog_common.py:41,44-52,55-94
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- `messages._busy_response`（:268-284）与 `_catalog_common.busy_response`（:44-52）逐行同逻辑（error_response("transform_busy",503,retry_after=…) + 头补写）；模块常量 `TRANSFORM_RETRY_AFTER_SECONDS = 2` 成对（messages.py:44 ≡ _catalog_common.py:41）。
- `messages._stream_upstream`（:319-334）与 `_catalog_common.stream_upstream`（:55-94）近重复；**关键差异**：messages 版仅 `headers=forward_directory_headers(directory)`（:329）——**不携带 `X-Request-ID`**；catalog 版经 `forward_upstream_headers(directory, request_id_from_scope(...))`（:76-78）双头转发（upstream.py:117-136 docstring：契约 §7 要求每个 sidecar→opencode 请求带 X-Request-ID 以便与 opencode 日志关联）。
- messages 调用点：:792、:1010（列表/合并路径经 `_stream_upstream`）；busy 6×。
- 佐证：03-reports/D10-code-quality.md §1.iii。
## 分析（机制、影响面、触发条件、可达性）
- 重复面：~30 行 + 常量对，双维护。
- 漂移面：`/slimapi/messages/{sid}`（列表）与 merged 路径的上游 GET 无 X-Request-ID——opencode 侧日志无法与 sidecar access log 行关联（P0-6 目标只覆盖 catalog/ready/写路径族）。属可观测性缺口而非 wire 缺陷（上游不依赖该头）。
- 可达性：messages 列表每请求（v3/v4 均然）。
## 契约/文档对照（规范轨引文）
- v3-contract §7（X-Request-ID 关联观测承诺——若该承诺是「全部上游请求」则本项为契约缺口，需 A4/A14 复核条款措辞；若仅 catalog/写路径承诺则为内部不一致）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 messages.py:268-284（`_busy_response`）≡ _catalog_common.py:44-52（`busy_response`）逻辑逐行同构；messages.py:329 仅 `forward_directory_headers(directory)`（无 X-Request-ID）对照 _catalog_common.py:76-78 `forward_upstream_headers(directory, request_id_from_scope(...))` 双头——漂移实证成立。
- 维持 P3 / confirmed。原状态注记移入：draft（A10/D10 2026-08-20）。
## 建议方向（非实现）
- 删本地副本改 import 共享版；`_stream_upstream` 并入共享版（params dict + read_timeout 参数化）；补 X-Request-ID 转发前先由 A4 裁定 §7 条款覆盖面。
