# F-332：passthrough 桶语义失真——3.0.0 后桶内只剩被拒 404，省流审计口径「按 bucket==passthrough 找未省流」已失效
- 严重度：P3（文档/口径失真 + 观测语义误导；无行为错误）
- 类别：docs（observability 口径）
- 位置：src/oc_slimapi/traffic.py:191-192；src/oc_slimapi/proxy.py:44-51；docs/manual/traffic-accounting.md:112,119；docs/operations.md:195；AGENTS.md 流程表
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- traffic.py:191-192：非 `/slimapi/**` 路径仍归 `passthrough` 桶；proxy.py:44-51 catch-all 已关闭——这些请求**全部**是 404 `thin_route_not_found`（3.0.0 起），无任何真实过境流量。
- 三处文档仍以 passthrough 为「未省流流量」口径：traffic-accounting.md:112（桶表「catch-all `/**`……不省流，透传，基线，比值≈1」）、:119；operations.md:195（「哪些请求未省流按 bucket==passthrough 过滤」）；AGENTS.md 流程表同款。
- 关联：手册 §4「透传基线对照 `ratios.passthrough` 应 ≈1.0 验证账本没系统性偏差」——404 拒绝的 bytes 比值无此语义，基线校验法失效。
## 分析
- 机制：桶名是 2.x 反代时代遗物；catch-all 关闭后桶的语义从「过境流量」变为「被拒扫描/探测流量」（有价值：可看外部扫描面），但三份文档仍按旧语义教运维使用。
- 影响面：省流审计结论失真（把 404 拒绝当透传基线）；`not_applicable|…|passthrough` 矩阵键同理是死维度组合（e1-13 疑点 1）。
- 触发条件：任何按文档执行的省流审计。
## 契约/文档对照
- v3-contract §8.2（catch-all 关闭终态）与 §9.2（矩阵维度保留历史值）——实现与契约一致；漂移在 ops 文档侧。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 traffic.py:191-192（非 /slimapi 归 `passthrough` 桶）与 proxy.py:44-51（catch-all 关闭 → 404 `thin_route_not_found`）打开核对一致——桶内只剩被拒 404 的机制成立；三处文档旧口径（traffic-accounting.md:112/:119、operations.md:195）核对在案。
- 维持 P3 / confirmed。原状态注记移入：verified（A13/D13 §1；Phase 3 终态待定）。
## 建议方向（非实现）
- 桶改名（`rejected_non_slim`，metrics 键集加性变更走 minor）或至少三文档改口径：「passthrough 桶现仅含 catch-all 404 拒绝，可作外部扫描面观察；省流基线请用 write_session 等真透传桶」；AGENTS.md 流程表同步。
