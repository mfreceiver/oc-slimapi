# F-151：v3-contract §2/§3/§3a 冻结文本仍载 supported:[3] / [3,3]，未随 (3,4) 双版本窗口加注——轨内文本与现行实现 [3,4] 漂移
- 严重度：P3（§3.3 contract-violation 行——漂移/滞后类；v4-contract 为现行权威、实现正确，属 v3 文本未同步注记）
- 类别：contract
- 位置：docs/specs/v3-contract.md:47（§2 表行「`{"code":"unsupported_version","supported":[2,3]}`（3.0.0 起 `[3]`）」）、:53（「无 `v` / `v=2` → 400 …`supported:[3]`」）、:59-66（§3 versions 载 `available: [2, 3]`…「3.0.0 起 `available:[3]`」）、:70-71（§3a `accepted_client_versions` 2.0.0=`[2,3]`、3.0.0=`[3,3]`）；实现 src/oc_slimapi/selector.py:622-634（`supported: [3,4]`）、routes/versions.py:61-64（`available=[3,4]`）、versioning.py:44
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- v3-contract.md:47/53/59-66/70-71 原文如上（冻结文本停留在 3.0.0 单版本终态口径）；
- v3-contract.md:20（§0.6，2026-08-19 修订）只声明「`?v=4` 下的行为差异…由 v4-contract 规定，本文件不重复载述」——但 no-v/unsupported 拒绝体、versions `available`、health `accepted_client_versions` 是**不依赖 `?v=4` 的面**（任何请求都可观察），不在 §0.6 的「v4 视图差异」授权范围内；
- 实现与测试：tests/test_terminal_matrix.py:115-127,169-177 断言 `supported == [3, 4]`；tests/test_v4_dual_window.py:182-210 断言 health 双视图（v4=4/[3,4]）；
- 佐证：**测试名滞后**——test_terminal_matrix.py:169 `test_unsupported_v_reports_supported_3` 实际断言 `[3,4]`（名实不符，同一滞后的代码侧痕迹）。
## 分析（机制、影响面、触发条件、可达性）
- 4.0.0 major 扩窗时修订了 v4-contract 与 §0.6 交叉引用，但 v3 §2/§3/§3a 的冻结字面未加「(3,4) 窗口起以 versioning.py 钉死值为准」注记——按双轨规则（§0.2 轨一序），v4-contract（同层、更新）+ 实现一致，裁决无歧义；但 v3 文本单独读会误导（「3.0.0 起 [3]」在 4.0.0 后已不真）。
- 影响面：契约读者/消费方对接者；无运行时影响（实现/测试均正确锁定 [3,4]）。
- 同族滞后已由 docs-notes 记录于 operations.md（D5/D7）、traffic-accounting.md（D18）、INTERFACE_MAP（D14）——本发现限定 v3-contract 契约文本自身（规范轨第 1 层）。
## 契约/文档对照（规范轨引文）
- v4-contract §0.1（4.0.0 起 `ACCEPTED_CLIENT_VERSIONS=(3,4)`；无 v → 400 `supported:[3,4]`）、§2 表行、§3.1（`available:[3,4]`）——与 v3 §2/§3 字面冲突，v4 为差异层权威（v4 §0「凡未提及语义逐字沿用 v3」的反向：已提及处以 v4 为准）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——selector.py:622-634 supported=list(SUPPORTED_WIRE_VERSIONS)=[3,4]、versions.py:61-64、versioning.py:44 (3,4) 命中；v3-contract.md:47/:53/:59-66/:70-71 冻结行 [3]/[2,3]/[3,3] 漂移抽读相符。证据链成立，维持严重度与建议方向。
- 冲突消解（V3）：与 F-125 锚点重叠（:47/:53/:66）——A4（契约质量）与 A2/D02（退役轨）双辖同修复面，04-final 应合并为一次加注，避免重复计数。
- 状态行归一：confirmed（原「verified」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- v3-contract §2/§3/§3a 三处加一行统一注记：「2026-08-19 起 (3,4) 双版本窗口——本节 `[3]`/`[3,3]` 字面以 v4-contract §0/§2/§3.1 与 versioning.py 钉死值为准」；测试名 `test_unsupported_v_reports_supported_3` 改名。
