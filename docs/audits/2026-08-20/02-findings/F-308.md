# F-308：global_hub.py 1090 行单类 34 方法，publish() 巨方法 ~320 行 if/elif 事件分发链
- 严重度：P3（可维护性；并发/状态缺陷已由 D05/D06 覆盖，此处仅模块化维度）
- 类别：design（上帝文件，A11 数据驱动入围：行数>500(1090) + 扇出 Top10(8) + 变更频率 Top10(11) 三条全中）
- 位置：src/oc_slimapi/sse/global_hub.py:63-1090（class GlobalHub）；publish 事件链 :658-977
- 置信度：high（def 行号图实测 + e1-05 卡片精读）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 单类 34 方法/属性（:66-993 def 图实测），职责混装：任务组 supervisor（ensure_upstream/_spawn_group/_make_group_done_callback :194-328）、订阅管理（subscribe/unsubscribe :329-361）、digest 防抖 flush（flush/flush_sid/flush_loop/heartbeat_loop :427-489）、依赖注入 setter 族（set_token_hub/set_directory_observer/set_turn_registry/set_directory_allowlist/set_replay_log :490-546）、prune 族（:601-657）、事件分发（publish :658-977）。
- publish() 单方法 :658-977（~320 行）：if/elif 链分派 `session.status`(:695)/`session.deleted`(:724)/`session.updated`(:744)/`session.error`(:806)/`message.part.updated`(:886)/`message.part.removed`(:913)/`message.removed`(:937)/`message.part.delta`(:969) 八个事件族，混杂 q/p 直推（:678 前）。
- 变更频率 11 次（并列第 6）；CHANGELOG 修复 3 轮触达：0.4.0（C⑩ tombstone）、0.11.0（G1-A flush_sid）、3.3.1（sticky lastError 格式归一化——修复被迫横跨 global_hub+hub_types+tokenstream/hub 三文件）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：八事件族的处理逻辑物理平铺在一个方法内，新增事件族=继续加长链；3.3.1 修复显示事件归一化知识分散在多文件（修法是往 hub_types 加共享 helper——正确方向但未根治分发集中度）。
- 影响面：可维护性；对 wire 无影响。
- 触发条件：新增/修改任意 global 事件族语义。
- 可达性：持续。
## 契约/文档对照
- v3-contract §6（SSE digest/q/p 帧）不变。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 wc -l global_hub.py = **1090 行** ✓；`def publish` 位于 :658（证据 :658-977 ~320 行）✓；class GlobalHub 起 :63 ✓；八事件族 if/elif 链分发行号抽核一致。
- 维持 P3 / confirmed。原状态注记移入：draft（A11/D11 §4.4）。
## 建议方向（非实现）
- 最低成本重构：publish() 拆为 per-event 处理器方法或分派表（`_on_session_status/_on_session_deleted/_on_session_updated/_on_session_error/_on_message_*`，每支 30-60 行），publish 保留 ~40 行分派骨架；无需文件级拆分（类本身即 sse 域聚合根，外部锚点多）。
- 测试锚点：10 个测试文件 27 处引用（rg 实测）全走 public 方法——处理器方法化零迁移。
- 与 F-301 同理可后续组合：digest 引擎（flush/prune）与事件解释（publish 分派）若继续增长再物理分文件。
