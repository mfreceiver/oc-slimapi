# F-302：routes/messages.py 1643 行混合三族端点职责（list 投影 / full 合并 / expand 提取器）
- 严重度：P2（可维护性；变更频率全仓第 3 + 历史缺陷 5 轮）
- 类别：design（模块化/上帝文件，A11 数据驱动入围：行数>500 + 扇出 Top10(14) + 变更频率 Top10(17) 三条全中）
- 位置：src/oc_slimapi/routes/messages.py:58-1643
- 置信度：high（def 行号图实测 + e1-02 卡片精读）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 行数 1643、顶层符号 51 个（全仓符号数第一）；4 个路由端点共栖一文件：messages(:948)、message(:1144)、expand_message_fragment(:1616)、expand_part_fragment(:1631)（inventory routes）。
- 三族职责行段实测：
  - list 族（sort/投影/cursor/Link 解析/lease 取数）：:58-356 + :752-947（~510 行，含 _parse_sort_project/_project_list_sorted_and_pack/_link_rel_tokens/_extract_before_verbatim/_parse_link_next_cursor/_canonical_list_query/_messages_list_key/_fetch_list_raw/_messages_via_lease）；
  - full 合并族：:361-750（~390 行，_CapExceeded/_placeholder_pairs/_expand_ref_pairs/_merged_candidate_pairs/_dedicated_full_get/_fetch_full_shared/_merge_fulls/_merge_fulls_and_pack）；
  - expand 提取器族：:1275-1643（~368 行，16 个 _extract_* 函数 + _EXPAND_EXTRACTORS 表 + _expand_fragment_worker/_expand_fragment/两个路由）。
- 变更频率 17 次（2026-08-01 以来，全仓第 3）；CHANGELOG 修复 5 轮涉及本文件：0.4.0（G6 mid 形状）、1.1.2（full 非 dict→503）、1.1.5（cap-bail upIn 口径）、1.1.6（P1-24 list 中途断连 503）、1.3.1（diffStats 投影位，连带 skeleton.py）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：三个端点族各自的 helper 家族在同一命名空间平铺（51 个顶层符号），expand 提取器表（:1471-1484）与 list 投影常量互相不可见地共享文件头 import；评审任一族改动需通读 1643 行定位波及面。
- 影响面：可维护性 + 缺陷定位成本（历史 5 轮修复分布三族，均需跨族阅读确认无交叉）。
- 触发条件：任何 messages 投影/分页/expand 演进。
- 可达性：已发生（1.1.6 P1-24 修复时 list 分支缺 except 的根因即三族错误处理路径分叉难对照）。
## 契约/文档对照
- v3-contract §5（messages/expand 端点族）不变——纯文件重组。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核（质量线）：主锚点 wc -l messages.py = **1643 行** ✓；四端点 def 实测 :949/:1145/:1617/:1632（证据记 :948/:1144/:1616/:1631，系装饰器行 vs def 行的 1 行口径差，不影响结论）✓。
- rg 重放（P2）：`rg -n 'async def messages|async def message|def expand_message_fragment|def expand_part_fragment'` → 四端点共栖一文件复现；顶层符号实测 49（证据 51，含常量/类的口径差）。
- 维持 P2 / confirmed。原状态注记移入：draft（A11/D11 §4.2）。
## 建议方向（非实现）
- 拆为 `routes/messages/` 包 + re-export shim（先例：sse/hub.py）：
  - `messages/_list.py`（~600 行：cursor/Link/sort/投影/lease + messages 路由）；
  - `messages/_full_merge.py`（~400 行：full 合并族 + message 路由）；
  - `messages/_expand.py`（~380 行：16 个提取器 + 表 + expand 两路由）；
  - `messages/__init__.py` re-export `router`。
- 迁移风险低：测试锚点 17 个测试文件、105 处引用（rg 实测），主要锚 `from oc_slimapi.routes.messages import ...` 路由函数与 `_parse_link_next_cursor` 等少数 helper——shim 保持原 import 路径即可。
- 收益：三族各自 <650 行；expand 提取器表成为独立单元（对应 e1-02 疑点：提取器类别扩展只触达单文件）。
