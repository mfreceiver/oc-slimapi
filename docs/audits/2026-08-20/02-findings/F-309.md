# F-309：app.py 组合根变更频率全仓第一（27 次/19 天）——lifespan 单体 600 行吸收所有组件接线 churn
- 严重度：P3（可维护性）
- 类别：design（耦合度量，A11 数据驱动入围：行数>500(785) + 扇出第一(24) + 变更频率第一(27) 三条全中）
- 位置：src/oc_slimapi/app.py:190-762（lifespan + 组装）；import 时副作用 :734-762
- 置信度：high（git log 聚合实测 + e1-06 卡片）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 变更频率 27 次（2026-08-01 以来全仓第一；第二名 config.py 22）——每次新增组件/旋钮都必改 app.py（25 个 app.state 键全部在此写入，:197-684）。
- lifespan 单函数 :190-~730（~540 行）串行完成：settings 校验、traffic ledger/snapshotter、upstream client、transform pool、catalog cache、raw_fetch singleflight、信号量、smoke、replay log+sweep task、hub registry、qp sweep、token hub/registry、turn registry、dbaux、access log 维护任务——再嵌 shutdown 排空（5 个 drain timeout 常量 :87-92）。
- import 时副作用（e1-06 卡 :160）：:734-762 模块导入即构建 app、注册异常处理器/三层中间件/18 个 router + proxy——任何 import（含测试）都拉起全部路由模块。
- 无缺陷史（0 条 CHANGELOG Fixed 直接归因 app.py）——纯 churn 磁铁而非 bug 源。
## 分析（机制、影响面、触发条件、可达性）
- 机制：组合根模式本身正当（扇出 24 = 全仓唯一合法的全量依赖点），问题在「接线+排空+smoke+main」四类逻辑单函数平铺，churn 集中且 review 冲突面大。
- 影响面：多人/多 agent 并行开发时 app.py 是必然冲突点；lifespan 内组件初始化顺序的隐式依赖（如 token_hub 依赖 replay_log 先建）无结构化表达。
- 触发条件：任何组件增删。
- 可达性：持续。
## 契约/文档对照
- docs/operations.md 描述启停行为；无契约冲突。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 wc -l app.py = 785（证据 785）✓；`async def lifespan` 位于 :190（证据 :190-762）✓；import 时副作用 :734-762 与 25 键写入段口径一致。
- 维持 P3 / confirmed。原状态注记移入：draft（A11/D11 §4.5）。
## 建议方向（非实现）
- lifespan 按组件拆分 stage 函数（`_wire_traffic/_wire_upstream/_wire_hubs/_wire_dbaux/_wire_maintenance` + 对应 `_drain_*`），app.py 保持组合根地位但每个 stage ≤80 行；与 F-305 的 AppContainer 同步推进（stage 返回填充 container 字段）。
- 保持「app.py 不拆文件」结论：组合根集中是架构优点（D11 §4.5 保持论证），重构仅函数级。
