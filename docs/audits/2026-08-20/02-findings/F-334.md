# F-334：`GET /slimapi/metrics` 受 selector 管辖须带 `?v=`——operations.md §9 排障指引照抄即 400，监控/告警探针同坑
- 严重度：P3（runbook 指引缺陷 + 探针设计陷阱）
- 类别：ops（docs）
- 位置：src/oc_slimapi/routes/metrics.py:7-9；docs/operations.md:475；docs/manual/traffic-accounting.md:34（正例）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- metrics.py:7-9 docstring：「The version selector (`?v=3` terminal) already covers every `/slimapi/**` route」——metrics 端点自身不豁免（唯一豁免 = `GET /slimapi/versions`，v3 §2）；无 `?v=` → 400 `unsupported_version supported:[3,4]`。
- operations.md:475（§9 排障速查「SSE 卡顿/断」行）：「查 `/slimapi/metrics` 的订阅者计数」——**未带 `?v=3`、未给完整 URL**，值班照抄得到 400，且 400 错误体（unsupported_version）与「服务异常」表象混淆。
- 对照正例：traffic-accounting.md:34 明确记载须带 `?v=3`（但该手册不是排障第一入口）；operations.md 自身的 health/ready 探针（:348,:372）都带了 `?v=3`——唯 metrics 引用漏参。
## 分析
- 影响面：①runbook 排障指引一步失败；②外部监控探针（uptime/告警抓包）若按字面 `GET /slimapi/metrics` 配置，会把常态 400 误判为故障（或因 4xx 判活规则漏报真宕机——与 F-004 crash-loop 的「连接拒绝 vs 400」判别叠加，D13 §4.3）。
- 触发条件：照 operations.md §9 执行；或探针未带版本参数。
## 契约/文档对照
- v3-contract §2（versions 唯一豁免）——实现正确；operations.md 指引与契约不一致。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 metrics.py:7-9（docstring "The version selector (`?v=3` terminal) already covers every `/slimapi/**` route"）与 operations.md:475（§9 排障行「查 `/slimapi/metrics` 的订阅者计数」无 `?v=`、无完整 URL）打开核对一致——照抄即 400 成立；对照正例 traffic-accounting.md:34 在案。
- 维持 P3 / confirmed。原状态注记移入：verified（A13/D13 §3.2；Phase 3 终态待定）。
## 建议方向（非实现）
- operations.md:475 补全 `curl -s 'http://127.0.0.1:4097/slimapi/metrics?v=3' | jq '.sse'`；§9 增加一行「所有 /slimapi 探针必带 ?v=3（versions 除外）」通用提示；发布说明/告警配置模板（D13 §5）统一带参。
