# F-335：recordType 过滤陷阱——聚合函数 aggregate_v3_observability 自身不过滤，§9.4 对账节未重申，SSE 桶计数可被生命周期行放大约 3 倍
- 严重度：P3（文档显眼度缺口 + 官方聚合函数口径陷阱）
- 类别：docs（工具口径）
- 位置：src/oc_slimapi/traffic_snapshot.py:239-241（counts 不过滤 recordType）；docs/manual/traffic-accounting.md:172,179,192（提示位）vs :363-365（§9.4 无提示）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- traffic_snapshot.py:239-241：`aggregate_v3_observability` 的 counts 把 request/sse_open/sse_close 三类行**全部**计入矩阵（键第 4 段 recordType 才可区分）——函数不提供「仅请求」视图，消费方必须事后按键段过滤。
- 手册提示位三处：§5.1 字段表（:172「统计请求数/字节时必须过滤 recordType=="request"」）、§5 常用分析 blockquote（:192，含旧文件容错写法）、§5.1 生命周期说明（:179）。
- 缺口：§9.4 离线对账节（:363-365）描述 matrix/sseActive/carry-in 公式时未重申——而该节正是引导使用 `aggregate_v3_observability`（:365「提供该纯函数实现，供运维脚本与测试对账复用」）的入口；函数输出 `counts` 直接当请求数用即 3× 放大（每 SSE 连接 = 1 request + 1 open + 1 close 行）。
- operations.md（值班第一入口）零提及 recordType。
## 分析
- 机制：提示存在但与工具出口不同节；官方聚合函数的默认口径与手册教述的消费口径（先过滤）不一致。
- 影响面：SSE 桶（events_sse/token_stream_sse）请求数、以及任何按键聚合的日环比被放大约 3×；非 SSE 桶无影响（无生命周期行）。
## 契约/文档对照
- v3-contract §9.2（recordType 是矩阵第 4 维）——实现与契约一致；缺口在手册 §9.4 与聚合函数易用性。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 traffic_snapshot.py:239-241（`aggregate_v3_observability` 的 counts 对 request/sse_open/sse_close 全部行计数、无 recordType 过滤）打开核对一致；手册提示位三处 vs §9.4 缺口的分节口径维持。
- 维持 P3 / confirmed。原状态注记移入：verified（A13/D13 §2.2；Phase 3 终态待定）。
## 建议方向（非实现）
- `aggregate_v3_observability` 输出增加 `requestCounts`（预过滤视图，加性键）；§9.4 公式段补一句「counts 含生命周期行，请求数按 recordType 段过滤或用 requestCounts」；operations.md §5 日志策略加一行指针。
