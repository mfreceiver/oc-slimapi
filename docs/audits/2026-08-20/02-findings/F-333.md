# F-333：sweep 观测双盲——bucketize 死桶预留无路由 + metrics sweep 块三文档零记载 + getattr enabled 兜底方向反向
- 严重度：P3（死代码/文档缺口/兜底方向瑕疵合集，无生产行为错误）
- 类别：gap（observability）
- 位置：src/oc_slimapi/traffic.py:107-110；src/oc_slimapi/routes/metrics.py:39-44；src/oc_slimapi/qp_sweep.py:239-247
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- traffic.py:107-110：`/slimapi/_shadow/sweep` 前缀映射到 `sweep` 桶，注释自认「shadow observations, not HTTP routes」——rg 全仓无该路径路由，桶永不出现（死桶）。
- metrics.py:39-44：sweep 块（qp_sweep.metrics()：triggers_total/cold_hits/skips/budget_exhausted/est_bytes_total/known_directories，qp_sweep.py:239-247）——grep `sweep` 于 docs/operations.md、docs/manual/traffic-accounting.md、docs/develop.md **零命中**（A13 实测）：B1b shadow 的 6 个计数器无任何消费指引；task 点名的「sweep 块零消费方」坐实（除 tests/test_b1b_sweep_shadow.py）。
- metrics.py:43 `getattr(qp_sweep, "enabled", True)` 默认 **True**——与 :41 注释 "test apps intentionally omit" 意图相反（缺 enabled 属性的对象会被当作启用调用 metrics()；保守方向应为 False）。
## 分析
- 影响面：qp sweep 是 shadow 阶段（零真实上游请求），观测缺失无即时生产后果；但 sweep 预算耗尽/全跳过（F-273 逐出击穿的观测面）只有这 6 个计数器可见——零文档意味着转生产时无人知道去哪看。
- 触发条件：qp_sweep_enabled=true（默认）时块已在 metrics 出现——数据在、无人读。
## 契约/文档对照
- v4-contract §9 未列 sweep 块（ops 加性面，契约不禁止）；config-census D11 已记 QP_SWEEP_* env 零记载——本发现补齐「指标也零记载」半边。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 traffic.py:107-110（`/slimapi/_shadow/sweep` 死桶 + "shadow observations, not HTTP routes" 注释原文）与 metrics.py:43（`getattr(qp_sweep, "enabled", True)` 默认 True）打开核对一致；rg 重放：`rg -c sweep docs/operations.md docs/manual/traffic-accounting.md docs/develop.md` → 零命中复现。
- 维持 P3 / confirmed。原状态注记移入：verified（A13/D13 §1.1；Phase 3 终态待定）。
## 建议方向（非实现）
- `getattr(qp_sweep, "enabled", True)` → 默认 False（一行，测试面行为不变——test apps 显式设 enabled）；sweep 块 6 键补进 traffic-accounting.md §3.1 或 operations.md 新小节（含告警阈值引用 D13 §5 #7/#8）；死桶注释标注「预留，无路由」或删除。
