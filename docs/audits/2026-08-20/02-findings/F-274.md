# F-274：上游 httpx limits 32/16 硬编码不可调；无 admission 并发面（merged 8 + q 8 + p 8 + 写 + SSE 1）≈25-30 贴近上限
- 严重度：P3（A9 初判；risk——单用户现实负载远低，突发才达；影响 = pool 5s 等待后 503 降级）
- 类别：risk
- 位置：src/oc_slimapi/upstream.py:43-44（`httpx.Timeout(connect=5.0, read=30.0, write=300.0, pool=5.0)`、`httpx.Limits(max_connections=32, max_keepalive_connections=16)` 硬编码，Settings 无对应字段）
- 置信度：medium（并发面为静态加总推演，非实测）
- 快照：0b836e7
- 状态：confirmed
## 证据
- src/oc_slimapi/upstream.py:40-46（create_client 全文——limits/timeout 均字面量）。
- 无 admission 的上游 GET 并发源：merged fan-out `asyncio.Semaphore(config.merged_fanout)` 默认 8（src/oc_slimapi/routes/messages.py:650，phase B 显式不占池槽）；questions_fanout_concurrency 默认 8（src/oc_slimapi/config.py:596-598）；permissions_fanout 默认 8（config.py:614-616）；写透传（无并发 cap）；GlobalHub SSE 长连接恒占 1 条（global_hub.py:1002，read=None）。
- admission 类读路由被 `max_transforms=1` 串行化（config.py:363），不贡献并发。
- 池耗尽后果：`pool=5.0` 超时 → `httpx.PoolTimeout`（RequestError 族）→ `raise_upstream_unavailable` 503。
## 分析（机制、影响面、触发条件、可达性）
理论并发合计 ≈ 8+8+8+writes+1 ≈ 25-30，贴近 32 上限；需多请求同时处 fan-out 峰值才触顶（单用户 ocdroid 正常模式远不可达，页面并发拉取 + 后台 q/p 扫描叠加时才可能）。SSE 与普通请求共享同一 client 意味着极端池耗尽时上游事件流也会受 pool 等待影响（SSE 重连期间）。属配置化债务 + 容量余量偏紧的组合，非现实缺陷。
## 契约/文档对照（规范轨引文）
- 无契约面（内部资源参数）；对照：几乎全部其他资源面（transform/raw-fetch/catalog/subscribers）均已 env 化（config.py），唯独连接池未暴露。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 upstream.py:43-44 打开核对一致（`httpx.Timeout(connect=5.0, read=30.0, write=300.0, pool=5.0)` + `httpx.Limits(max_connections=32, max_keepalive_connections=16)` 字面量硬编码）；并发面静态加总（merged 8 + q 8 + p 8 + 写 + SSE 1）为推演口径，维持原置信度 medium。
- 维持 P3 / confirmed。原状态 draft。
## 建议方向（非实现）
`OC_SLIMAPI_UPSTREAM_MAX_CONNECTIONS` / `..._MAX_KEEPALIVE` env 化（带 validate 上限），或在文档记录 32 的容量假设与不占 admission 的并发面清单。
