# F-243：LatencyBreaker 跨线程无锁——worker record vs 事件循环 prune/reset/snapshot 的 metrics 瞬时失真
- 严重度：P3
- 类别：risk
- 位置：src/oc_slimapi/dbaux/lifecycle.py:489（worker 线程 `record`）vs :630（loop `note_probe`）/:563（loop `reset`）/:238-247（loop `snapshot`，含 :181-184 `_prune` 重绑）
- 置信度：medium-high（GIL 语义推演，快照 0b836e7）
- 快照：0b836e7
- 状态：confirmed
## 证据
- `record` 仅在专属 worker 线程调用（`_run_query` finally，:489）；`note_probe`（probe :630）、`reset`（`_open_and_gate` :563）、`snapshot`（metrics 路由）在事件循环线程——类无锁（`LatencyBreaker` :147-247 无任何 threading 原语）。
- 交错点：(1) `_prune` 的 `self._samples = [s for s in …]`（:184 重绑）与 worker 侧 `append`（:201）交错可丢刚计入的样本（窗口视图短暂少 1）；(2) `reset()`（:233-235）与 worker `record` 的 `_total += 1` 交错 → reset 后 total≠0 或样本残留；(3) `_total += 1`（:202）非原子（读-改-写）。
- GIL 保证单字节码原子、无崩溃/无损坏不变量；失真仅现于 metrics 的 p50/p99/samples/total 瞬时读数与极罕见的 trip 判定边界（窗口恰好少一个超限样本）。
## 分析
- 触发面：常态并发（查询进行中读 metrics / swap reset 与在途查询收尾竞态）——高频但后果限于观测精度。
- trips/swaps/disables 等事件计数器全在事件循环侧操作，无此问题。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——lifecycle.py:181-184 _prune `self._samples = [...]` 重绑与 :201-202 `append`/`_total += 1` 交错点命中（LatencyBreaker 全类无 threading 原语）。证据链成立，维持严重度与建议方向。
- 严重度归一：原 P4（越出 P0-P3 刻度）→ P3（Phase 3 归一化）。
- 状态行归一：confirmed（原「verified（2026-08-20，D07 / A7，新发现）」辖属/批次注记移入本记录）。

## 建议方向（非实现）
- 类 docstring 声明线程契约并加 `threading.Lock`（临界区微小，无性能面）；或统一收敛到 worker 线程操作（snapshot 经 `_submit`）。
