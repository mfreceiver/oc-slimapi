# F-001：幽灵事件类型 permission.resolved/permission.v2.resolved——上游实为 permission.replied，真实决议事件被 catch-all 静默丢弃
- 严重度：P1（初判，按 §3.3 决策表待 Phase 2/3 复核定级）
- 类别：defect
- 位置：src/oc_slimapi/sse/hub_types.py:73-77（IMMEDIATE 集）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- src/oc_slimapi/sse/hub_types.py:73-77（IMMEDIATE 集：含 `"permission.resolved"`、`"permission.v2.resolved"`）
- 佐证材料：01-explore/upstream-notes.md 主题 4/7：上游 schema/src/v1/permission.ts:61-65 为 permission.replied、schema/src/permission.ts:43-45 为 permission.v2.replied；global_hub catch-all 丢弃零观测
- 【A3 补 2026-08-20】上游直读（opencode-src/current = v1.18.18）：packages/schema/src/v1/permission.ts:61-65 `const Replied = define({ type: "permission.replied", … })`，v1 事件全集仅 asked/replied；packages/schema/src/permission.ts:43-45 `type: "permission.v2.replied"`。
- 【A3 补】负向搜索：rg `permission.resolved` 于 opencode-src/current/packages/{schema,core,protocol} 零命中——非迁移期过渡名，是 sidecar 侧自创拼写。
- 【A3 补】sidecar 消费路径：IMMEDIATE 直推 + qp_last_activity 记账于 src/oc_slimapi/sse/global_hub.py:671-685；catch-all 静默丢弃点 global_hub.py:975（无 per-type 计数/日志）。
## 分析（机制、影响面、触发条件、可达性）
- 【A3 补】机制：上游 `permission.replied`/`permission.v2.replied`（用户对 permission 请求的真实决议）不在 IMMEDIATE/SESSION/MESSAGE/token 任一分类 → publish catch-all 静默丢弃（global_hub.py:975）。订阅 `/slimapi/events` 的客户端永远收不到决议帧——与 q/p「asked 秒推」设计意图相反（asked 直推、replied 永失），客户端只能靠 digest 变化/轮询兜底。
- 触发条件：任何用户在 opencode 内回答 permission 请求（常规高频操作）。可达性：生产恒可达。
- 观测盲区：丢弃无计数无日志——本缺陷在 metrics/access log 完全不可见（upstream_events_total 不含被丢帧）。
## 契约/文档对照（规范轨引文）
- v3-contract §7（SSE 策展边界冻结面——IMMEDIATE 集成员属 wire 行为，改名须走契约修订 + CHANGELOG）。
## 复核记录（Phase 3）
- **维持 confirmed / P1 defect**。V2 三步证伪逐项执行（2026-08-20）：
  - (a) 上游发布点直读闭合：`rg "Event.Replied" packages/core/src/ packages/opencode/src/` → `core/src/permission.ts:225,239,276`（`events.publish(Event.Replied, …)`，schema 导入 `@opencode-ai/schema/permission` → `permission.v2.replied`）+ `opencode/src/question/index.ts:126`（question.replied）。桥接无过滤：`opencode/src/event-v2-bridge.ts:35-44` 对**每个** EventV2 事件无条件 `GlobalBus.emit("event", …)`；`httpapi/handlers/event.ts:55-56` `/global/event` SSE 订阅 `GlobalBus.on("event")`——**replied 确实流经 /global/event**。sidecar 侧 `rg "replied" src/oc_slimapi/` **零命中**（无任何处理/翻译层）；`rg "permission.resolved" opencode-src/current/packages/` **零命中**（幽灵拼写上游不存在）。
  - (b) 契约豁免检查：**v3-contract §7 未逐成员枚举 IMMEDIATE 集**（§7.2 仅提「q/p IMMEDIATE 直推帧」不列名）——当前权威契约未冻结幽灵名，无豁免。幽灵名载体为 v2-contract §3:289-290（历史契约，且其自述「type 字段值取上游 opencode 事件名 **verbatim**，sidecar 不重命名/不映射」与同节枚举的 `permission.resolved` **自相矛盾**——枚举本身即文档错误）+ CHANGELOG:620（1.x 历史记录）+ INTERFACE_MAP.md:81 / design-v2.md:84（现行追踪/设计文档回声，非 wire 权威）。因 v3 §7 未载入，**不改判 contract+defect 双面主定性**；文档回声面以附注保留。
  - (c) 测试锁定方向：tests/test_hub_behavior_lock.py:888-918 以**合成事件**（自造 `permission.resolved` 输入）断言直推转发——锁定的是「IMMEDIATE 成员即转发」机制（轨二），**无法检测成员名与上游真值的错配**（测试不重放真实上游帧）。按 §0.2 双轨：测试通过不豁免——幽灵名导致的真实决议帧丢弃是消费方可观察缺陷。测试锁定性质 = 「测试无法证伪的偏离面」，非契约一致行为锁定。
- A6 复核链（digest 无感 / qp_last_activity 单向 / 观测盲区）复核认同，不再复述。原状态行括注「A3/D03 2026-08-20 升级：上游 schema 直读 + sidecar 消费路径 + 负向搜索三源齐」由本节承接。
## A6 复核记录（2026-08-20，D06）
- **维持 verified / P1**。完整影响链补全（D06 §1.2）：
  1. 上游发 `permission.replied`/`permission.v2.replied` → global_hub.py publish 不在任一分类 → :975 catch-all 静默丢弃。
  2. **digest 不丢更新、也不补**：该事件不属 SESSION/MESSAGE 族，pending/sticky 零写入——digest 路径完全无感，无间接可见性。
  3. **q/p IMMEDIATE 单向失效**：`permission.asked` 直推（:671-685）创建客户端决议 UI；`replied` 永不达 → UI 只能靠超时/轮询关闭。`qp_last_activity` 只被 asked 族刷新（:672-678 的 startswith 门在 IMMEDIATE 分支内，replied 到不了）。
  4. 消费方可观察后果：ocdroid 权限弹窗经 SSE 永不收「已答复」；上游 permission 实已 merge 而客户端流视图停在被问态；唯一兜底 = 契约 §7.8 低频周期 304 对账（digest 触发重拉不可得，见第 2 点）。
  5. 观测盲区确认：`upstream_events_total`（:661）含被丢帧但不可分辨——metrics/access log 均不可见。
- 同族扩展：`question.replied`/`question.rejected`/`question.v2.replied`/`question.v2.rejected` 从未进入任何集合，同链路同后果——并入 F-216 清单（F-001 聚焦 permission.* 拼写幽灵）。
## 建议方向（非实现）
- IMMEDIATE 拼写纠正 `resolved→replied`（v1/v2 双成员）；属 wire 行为修复：CHANGELOG 记录 + 与 ocdroid 确认消费名（客户端若已适配错误名需同步）。附带在 catch-all 丢弃处补 per-type 计数，防同类幽灵名再静默。
