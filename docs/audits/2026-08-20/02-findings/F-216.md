# F-216：catch-all 丢弃零观测——76 型上游真实事件静默丢弃，无 per-type 计数/日志，事件集漂移不可检测
- 严重度：P2
- 类别：defect（observability）
- 位置：src/oc_slimapi/sse/global_hub.py:975（catch-all 丢弃点，仅注释无计数）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- global_hub.py:975 `# Drop text deltas, tool.*, message.part.*, and anything else.`——全分支尾部无任何计数/日志；
- 分类全集 vs 上游全集（D06 §1.1，E8 §4 真值）：/global/event 可观察 89 型（88 manifest + 合成 server.heartbeat），sidecar 有意义处理 13 型 → **76 型进 catch-all**：q/p 决议族 6（question.replied/rejected、question.v2.replied/rejected、permission.replied、permission.v2.replied）、session.created、session.idle(deprecated)、session.diff、session.compacted、server.connected、global.disposed、session.next.*×32、todo.updated、file.edited、file.watcher.updated、pty.*×4、command.executed、installation.*×2、integration.*×2、models-dev.refreshed、catalog.updated、project.updated、project.directories.updated、lsp.updated、mcp.*×2、tui.*×4、vcs.branch.updated、reference.updated、plugin.added、workspace.*×3、worktree.*×2、server.heartbeat；
- 唯一泛化计数 `upstream_events_total`（:661）含被丢帧但不可分辨类型。
## 分析（机制、影响面、触发性）
- 策展边界（哪些类型透传/入 digest）本身是设计冻结面——逐型不透传**不是**缺陷；缺陷是丢弃零观测：(a) 上游事件集漂移（新增/改名类型）完全不可见，幽灵名类缺陷（F-001 的 `permission.resolved`——拼写错误长期存活）正是此盲区的直接产物；(b) 运维无法回答「上游在发什么 sidecar 没用上」（省流审计的核心问题之一）。
- 触发：常态——每一个未策展事件都在发生，累计 76 型。
## 契约/文档对照（规范轨引文）
- v3-contract §7（策展边界冻结面——加计数不改变 wire，属内部观测加性）；§9 观测维度未覆盖 per-type 上游事件分布。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——global_hub.py:975 `# Drop text deltas, tool.*, message.part.*, and anything else.` 逐字命中。证据链成立，维持严重度与建议方向。
- P2 rg 重放：`rg -n "dropped|drop_" src/oc_slimapi/sse/global_hub.py` 命中全集=allowlist_dropped_events 族（:149/:556/:581/:586，allowlist 帧过滤专属计数）+注释/debug 日志（:815/:866/:930/:1054 malformed）——catch-all 丢弃无 per-type 计数/日志成立。
- 状态行归一：confirmed（原「verified（A6/D06 2026-08-20；Phase 3 终审）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- catch-all 处补 per-type 计数（如 `upstream_dropped_events_total{type}` 或 metrics 端低基数码表 + 未知类型告警）；与 F-001/F-002 的分类器表清理同 PR 最经济。
