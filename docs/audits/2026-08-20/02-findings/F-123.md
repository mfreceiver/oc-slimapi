# F-123：INTERFACE_MAP.md 全局头仍声明「v3-only 终态」「v=4 不支持 supported:[3]」——与 4.0.0 起 (3,4) 双版本窗口实现矛盾
- 严重度：P2（初判；INTERFACE_MAP 是「防漂移」权威文档且 check.sh 仅做存在性对账，语义级矛盾不被门禁拦截）
- 类别：docs
- 位置：docs/specs/INTERFACE_MAP.md:5-7（头部终态声明）、:14（版本选择器行）
- 置信度：high（文档实读 + 实现对照）
- 快照：0b836e7
- 状态：confirmed
## 证据
- INTERFACE_MAP.md:7：「**⚠ v3-only 终态（M3）**：……`/slimapi/**` 唯一合法入口是 `?v=3`；无 `v`、`v=2`、合法但不支持的值 → 400 `{"code":"unsupported_version","supported":[3]}`」
- INTERFACE_MAP.md:14：「`v=3`→v3 管线……**无 `v`、`v=2`、合法但不支持（1/2/4/5…）→400 `{"code":"unsupported_version","supported":[3]}`**」——**明文把 `v=4` 列为不支持**
- 实现真值：selector.py:135-136 `SUPPORTED_WIRE_VERSIONS = tuple(range(ACCEPTED_CLIENT_VERSIONS[0], ...))` ← versioning.py:44 `ACCEPTED_CLIENT_VERSIONS = (3, 4)`；selector.py:629-631 `unsupported_version` 响应体 `"supported": list(SUPPORTED_WIRE_VERSIONS)` = `[3, 4]`
- 同文档 per-route 行已带 4.0.0 v4 面注记（如 :25 sessions 行「**4.0.0 v4 面（2026-08-18）**：同路由 `?v=4` 走 DB 投影全局面……」）——头部与正文行自相矛盾，正文行是新的
- CHANGELOG [4.0.0]（2026-08-18）：wire (3,4) 双版本窗口开启；`supported` 由 `[3]` → `[3,4]`
## 分析（机制、影响面、触发条件、可达性）
- 机制：4.0.0 发版时更新了受影响 per-route 行与 v4-contract，但 INTERFACE_MAP 的全局头（「Aligned with v3-contract.md（M3 v3-only 终态）」:5、终态声明 :7、选择器全局行 :14）未同步。
- 影响面：消费方/新对接方按 INTERFACE_MAP 头部理解会误判 v4 不可用（文档自称「描述现在实际做什么」）；AGENTS.md 将 INTERFACE_MAP 列为端点级实现追踪权威，`scripts/check_routes_doc.py` 只做路由存在性+method+语义白名单对账，不校验全局头陈述——此类漂移无门禁拦截。
- 触发条件：任何读者依据头部做版本协商判断；D02 迁移评估中若以 INTERFACE_MAP 为 v4 能力来源会得出错误结论。
## 契约/文档对照（规范轨引文）
- v4-contract §2 状态表（`v=4` → v4 视图；无 `v`/∉{3,4} → `supported:[3,4]`）；§0.1 双版本期
- v3-contract §0.6（(3,4) 双版本窗口交叉引用，2026-08-19 修订）——v3 契约头部已更新而 INTERFACE_MAP 未跟随
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——INTERFACE_MAP.md:5-14 头部「v3-only 终态（M3）」与「无 v/v=2/合法但不支持（1/2/4/5…）→400 supported:[3]」逐字命中。证据链成立，维持严重度与建议方向。
- P2 rg 重放：`rg -n 'supported":\[3\]' docs/specs/INTERFACE_MAP.md` = :7 与 :14 两处；对照 selector.py:135 SUPPORTED_WIRE_VERSIONS ← versioning.py:44 ACCEPTED_CLIENT_VERSIONS=(3,4)——头部与实现矛盾成立。
- 状态行归一：confirmed（原「draft（A2/D02 产出）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 机械性文档修正（D02 §5.5-3）：头部三处（:5/:7/:14）改述为 (3,4) 双版本窗口 + 指向 v4-contract；属零 wire 变更的漂移修复。
