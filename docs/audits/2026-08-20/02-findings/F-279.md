# F-279：merged 请求全程持有 raw-fetch lease（跨 fan-out + phase C admission 等待）——默认预算仅容 1 flight，慢 merged 页阻塞其余全部列表去重
- 严重度：P3（A9 初判；risk——设计选择的资源代价未披露；非错误行为）
- 类别：risk
- 位置：src/oc_slimapi/routes/messages.py:837（`async with lease` 作用域包裹 :862-886 全部投影 + `_merge_fulls`）；预算事实 src/oc_slimapi/config.py:899-906（「default 64 MiB × 64 MiB admits ONE concurrent distinct flight — deliberately conservative」）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- messages.py:837-886：lease 进入后依次执行 admission 等待（默认单轮 2s）→ offload 投影 → `_merge_fulls`（fan-out 网络 + phase C 再 admission，absorb 预算 2.5s）——全程持有 `raw_fetch` 预算份额（每 flight 预留全 `max_response_bytes`=64 MiB，config.py:403-408 + :899-906）。
- LeasedSingleFlight 预算满 → 后来者 `fetch_or_bypass` 返回 None → 直连路径（messages.py:835-836）——**不报错但失去去重**：并发同 key 列表请求各自打上游。
- `_release` 切断引用链的时点设计（singleflight.py:200-240 docstring）确认 lease 持有窗口即预算占用窗口。
## 分析（机制、影响面、触发条件、可达性）
默认配置下 raw-fetch 预算（64 MiB）恰好容纳 1 个 flight（每 flight 预留 64 MiB）。一个 merged 页若慢（fan-out 网络 + 2.5s absorb 等待，最坏可拖到秒级），期间**全部**其他可去重列表 GET（messages/sessions/status/questions/permissions）bypass 直连——省流失效窗口 = merged 页全程。叠加 F-006 后果（merged 页实际上仅 1 个 full GET）实际窗口通常短；但 F-006 修复后（真 16 路 fan-out）本条的占用窗口会变长——修复 F-006 时应一并评估。
## 契约/文档对照（规范轨引文）
- CHANGELOG.md:283-284（1.5.0 披露「默认容量下同时只有 1 个去重 flight，超额自动降级直取」）——预算=1 的事实已披露；**未披露** merged 请求对 lease 的长持有会放大 bypass 窗口。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 messages.py:837（`async with lease` 包裹后续投影+merge 全程）与 config.py:899-906（"default 64 MiB × 64 MiB admits ONE concurrent distinct flight — deliberately conservative" 注释原文）打开核对一致。
- 维持 P3 / confirmed。原状态 draft。
## 建议方向（非实现）
merged 请求在进入 fan-out 前释放 list lease（list body 已消费完毕，phase B/C 不再需要共享 body——需核对 :862-877 的 body 消费时点确认可行性）；或文档披露该占用窗口。
