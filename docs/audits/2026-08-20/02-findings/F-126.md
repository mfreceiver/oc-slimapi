# F-126：v3→v4 规范耦合量化——v4-contract 67 行 v3 引用/13 处显式继承表述 + 12 个双视图测试文件的等价性对照依赖，构成任何未来版本窗收窄的字面化前置工作量锚点
- 严重度：P3（初判；非缺陷，为 D02 口径 b 成本模型的可复核量化记录）
- 类别：risk
- 位置：docs/specs/v4-contract.md:5（继承基线条款）等全文；tests/ 12 个双 wire 视图文件；src 分叉统计见 D02 §5.4
- 置信度：high（rg 全量计数，可复现）
- 快照：0b836e7
- 状态：confirmed
## 证据
- `rg -c "v3" docs/specs/v4-contract.md` = **67 行**含 v3 字面；`rg -c "沿用 v3|继承 v3|v3 原样|沿袭 v3|同 v3"` = **13 处**显式继承表述
- v4-contract.md:5：「**继承基线**：v3 契约（docs/specs/v3-contract.md）全量继承 + 本文件逐条差异覆盖——**凡未提及语义逐字沿用 v3**」——v4 的完整性以 v3 文档存在且有效为前提
- v4-contract §10：**47 条路由「零 v4 差异」**（v4 = v3 语义原样经 selector 分派）；§14（expand 端点「全部继承 v3 §4b 冻结语义，本修订不另立」）；§7.5（SSE 同步语义「全文语义同 v3 §7.5/§7.8」）；§8.4（复用既有码清单）
- 测试对照依赖（test-census §8 + rg 实取）：12 个 v3+v4 双 wire 视图同文件（其中 10 文件 `?v=3`+`?v=4` 字面双 selector）；v3 字节回归锚三处——test_sse_replay_wire.py（v3 无 id 字节锚定）、test_expand_href_v4.py（v3 href 字节回归）、test_v3_rawbody_regression.py（PYTHONHASHSEED=0 子进程字节基线）；8 个 v3 单态文件 106 测试函数；294 处 `v=3` 字面（47 文件）
- src 侧分叉量化（D02 §5.4）：122 行/22 文件提及 wire 版本（33 可执行分支行/14 文件）+ envelope.py 73 行 v3-only 模块——代码耦合小，**规范/测试耦合大**的倒挂结构
## 分析（机制、影响面、触发条件、可达性）
- 机制：v4 以「v3 全量继承 + 差异覆盖」方式定稿（4.0.0 基线），使 v3 文档与 v3 测试面成为 v4 语义的规范锚点与等价性基准；该耦合在 (3,4) 永久双版本下是**收益**（47 路由等价性免单测化），仅在假设性收窄场景转化为**前置字面化成本**。
- 量级（收窄前置，假设性）：① v4-contract 自包含化（67 处引用/13 处继承条款 + 47 路由零差异注载 + §7.5/§8.4/§14 继承段落的正文化）；② 12 双视图文件 v3 半区改写为 v4 字面 golden；③ v3-contract 288 行整份废止；④ INTERFACE_MAP 41 处 v3 引用重写。
- 触发条件：仅在 owner 未来另行裁决收窄版本窗时（v4-contract §0.3：当前裁决为永久双版本、5.0.0 已取消、评估判据纯观测性且未满足）。
- 可达性：当前为零成本状态；本条为度量记录。
## 契约/文档对照（规范轨引文）
- v4-contract §0.3/§9.4（永久双版本裁决 + 纯观测性评估判据）；v3-contract 头部 2026-08-19 状态注记；CHANGELOG [4.1.0]（协议封顶 4 系、5.0.0 取消）
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——rg 重放：`rg -c "v3" docs/specs/v4-contract.md` = 67 行、`rg -c "沿用 v3|继承 v3|v3 原样|沿袭 v3|同 v3"` = 13 处——与发现数值逐项复现，度量可复核。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「draft（A2/D02 产出；§0.3 合规——本条不构成退役建议，仅记录耦合度量）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 无政策输出（§0.3：维持现状）；度量供 04-final/v3-retirement-plan.md 引用。可选机械性预备（不预设决策）：对 v4-contract 引用的 v3 条款编号建立反向索引（文档附录），降低未来任何正式修订的检索成本——纯文档类，优先级低。
