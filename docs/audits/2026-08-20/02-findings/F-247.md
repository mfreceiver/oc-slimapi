# F-247：cursor 解码无长度上限——事件循环内 b64decode+json.loads 的 CPU/内存面
- 严重度：P3
- 类别：risk
- 位置：src/oc_slimapi/dbaux/cursor.py:165-208（decode_cursor 无长度检查）；消费入口 routes/sessions.py:413-414
- 置信度：high（代码 + 测试锚定，快照 0b836e7）
- 快照：0b836e7
- 状态：confirmed
## 证据
- decode_cursor docstring :177-179 明示「超长但合法的 cursor 正常解码（不过度防御）」；tests/test_cursor_matrix.py:373-378 `test_10kb_valid_cursor_decodes` 锚定该决策（10KB 合法 cursor 必须可解）。
- sessions.py:413-414 在事件循环内直接 `decode_cursor(cursor)`——`urlsafe_b64decode` + `blob.decode("utf-8")` + `json.loads` 全同步执行；`?cursor=` 值长度无路由层截断（rg `cursor` 于 sessions.py 无 len 检查）。
- 解码成功后 `i` 可为任意长字符串（:201 仅类型检查）→ 进入 SQL 绑定（参数化，无注入）——内存驻留 = 参数本身大小。
## 分析
- 触发面：恶意/失控客户端发送超大 `?cursor=`。部署形态缓解：sidecar 监听 loopback、经 stunnel mTLS、唯一合法客户端 ocdroid——外部攻击面取决于 stunnel 配置；内网失控客户端仍可造成事件循环阻塞（同步解码数 MB 级输入）。
- 与 D17 系列其他项（值域防御归 B4 的分工声明）一致——记录为审计跟踪点而非现行事故面。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——cursor.py:177-179 docstring「超长但合法的 cursor 正常解码（不过度防御）」命中；消费入口 sessions.py:413-414 引用有效。证据链成立，维持严重度与建议方向。
- 严重度归一：原 P4（越出 P0-P3 刻度）→ P3（Phase 3 归一化）。
- 状态行归一：confirmed（原「verified（2026-08-20，D07 / A7；state-machines.md 卡 17 D17-2 种子确认）」辖属/批次注记移入本记录）。

## 建议方向（非实现）
- 路由层加廉价长度上限（如 4-8KB → 400 invalid_cursor，与 10KB 测试锚点联动调整契约措辞）——decode 本身保持纯函数。
