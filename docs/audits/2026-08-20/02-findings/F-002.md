# F-002：幽灵事件类型 message.appended 为永久死码（上游 88 型事件全集不存在该类型）
- 严重度：P3（初判，按 §3.3 决策表待 Phase 2/3 复核定级）
- 类别：defect
- 位置：src/oc_slimapi/sse/hub_types.py:88-90
- 置信度：high
- 快照：0b836e7
- 状态：refuted
## 证据
- src/oc_slimapi/sse/hub_types.py:88-90（MESSAGE_EVENTS 含 `"message.appended"`；:85 注释自认「retained for wire compatibility」）
- 佐证材料：01-explore/upstream-notes.md 主题 4
- 【A3 补 2026-08-20】上游直读：rg `message.appended` 于 opencode-src/current/packages/schema/src **全树零命中**；packages/schema/src/v1/session.ts:597-633 消息族实际全集 = message.updated / message.removed / message.part.updated / message.part.removed / message.part.delta。
- 【A3 补】sidecar 消费路径：MESSAGE_EVENTS 分支 src/oc_slimapi/sse/global_hub.py:784-800（提 sid+messageID → `_bump_updated_at`）——成员资格永不命中。
## 分析（机制、影响面、触发条件、可达性）
- 【A3 补】纯死分支（「接幽灵」而非「丢真帧」，与 F-001 性质不同）：无行为影响；代价 = 分类器表面积 + 读者误导（以为上游有 append 语义会触发 digest updatedAt）。注释的「wire compatibility」保留理由不成立——sidecar 从不自行产出事件类型名，上游无此类型即无兼容对象。
- 可达性：不可达（除非上游未来恰新增同名类型，属巧合非设计）。
## 契约/文档对照（规范轨引文）
- v3-contract §7（帧分类全集冻结面）——死成员从未产出 wire 行为，删除属内部清理，无需 bump。
## 复核记录（Phase 3）
- **销案 refuted（§0.2 双轨：defect 且测试锁定契约一致行为）**。事实面复核成立但 defect 定性不成立（2026-08-20）：
  - 锚点抽查：hub_types.py:88-90 `MESSAGE_EVENTS = frozenset({"message.updated", "message.appended"})` 命中；上游负向搜索本轮复跑：`rg "message.appended" opencode-src/current/packages/{schema,core}/src/` → **零命中**（「上游无此类型」事实不变）。
  - 反证一（契约枚举）：**v2-contract §3:279/:298 逐字冻结 digest 驱动集**——「digest 仅由 `session.*` / `message.updated` / **`message.appended`** 驱动」「`messageID`←`message.updated`/`message.appended` 的 `info.id`」；v3-contract §7.1「帧名/帧形…零变化」继承——**该成员是契约冻结文本明载的 digest 驱动器，非实现自创**。hub_types.py:85-87 注释「retained for wire compatibility」的理由有契约文本支撑（发现称「保留理由不成立」被推翻）。
  - 反证二（测试锁定）：`rg "message.appended" tests/` → test_hub.py / test_hub_behavior_lock.py（各 3 处）——测试以该类型名发布合成事件并断言 digest 行为，**行为已被测试锁定**。
  - 与 F-001 的本质区别：F-001 的幽灵名导致真实决议帧被丢（消费方可观察损失）；本条的幽灵名不丢任何真实事件（上游无 append 类型、updated 覆盖追加）——「接幽灵」零 wire 后果 + 契约明载 + 测试锁定 = 契约一致行为。原状态行括注「A3/D03 上游 schema 全树负向搜索 + 消息族全集直读」由本节承接。
## A6 复核记录（2026-08-20，D06）
- **维持 verified / P3**。快照直读确认：hub_types.py:88-90 `MESSAGE_EVENTS` 含 `"message.appended"`（:85-87 注释自认兼容保留）；消费分支 global_hub.py:784-800（提 sid+messageID → `_bump_updated_at`）成员资格永不命中。上游 v1.18.18 消息族全集 = message.updated/removed/part.updated/part.removed/part.delta（E8 §4），`message.appended` 全树零命中。纯死分支定性不变；删除属内部清理无需 bump。关联 F-216（catch-all 零观测是幽灵名能长期存活的土壤）。
## 建议方向（非实现）
- 从 MESSAGE_EVENTS 删除该成员并修正注释；与 F-001 同 PR 处理分类器表最经济。
