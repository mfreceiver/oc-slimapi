# F-286：permissions.py ↔ questions.py 全模块孪生——~89% 结构同一、~320 行重复（含 121 行逐字节调度器 ×2）
- 严重度：P3
- 类别：smell（重复代码）
- 位置：src/oc_slimapi/routes/permissions.py（526 行）、src/oc_slimapi/routes/questions.py（504 行）
- 置信度：high（AST 归一化 diff 定量）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 逐字节相同：`_collect_with_byte_budget`（permissions.py:426-526 ≡ questions.py:406-504，121 行，仅 launch() 目标名与 docstring 异）；`_DirFetchFailure`（:28-39 ≡ :28-39）；`_MAX_AGGREGATE_ITEMS = 10_000`（:46 ≡ :47）；`_directories_from_sessions` 体（:59-65 ≡ :69-75）；`_pack_*_envelope` 体（:302-303 ≡ :285-286）；handler Step1 discovery 块（permissions.py:185-229 ≡ questions.py:170-214）。
- 近相同（仅端点参数异）：`_fetch_permissions_for_dir`（:306-423）vs `_fetch_questions_for_dir`（:289-403）——~110/118 行同构；差异仅 semaphore 名、上游 path `/permission`/`/question`、flight key 前缀、条目投影（permissions 7 字段白名单 :417-422 vs questions verbatim stamp :399-402）。
- 定量（AST 剥 docstring + 名称归一后 diff）：permissions 181 行代码 vs questions 179 行，**仅 20 行不同**（路由/tag、`_PERMISSION_FIELDS`、path、flight key、config knob 名）。
- 佐证：03-reports/D10-code-quality.md §1.iii；仓库自有 dedup 先例 `_catalog_common.py`（P2-B2，agent/command ~95% 相同时抽共享模块）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：copy-paste 演化孪生。任何一处修 bug（如调度器 cancel 语义、预算判界）都需手工双写；当前两版行为一致无漂移，但 F-006 类回归若单侧修复将产生行为分叉。
- 影响面：~320 行重复（121+110+50+30×2 侧）；维护双倍；测试也成对复制。
- 可达性：n/a（质量债，无运行时行为差异）。
## 契约/文档对照（规范轨引文）
- 无 wire 影响：两路由各自 v2-era 契约形状（v2-contract §/INTERFACE_MAP questions、permissions 行）冻结；合并为内部共享模块零 wire 变化。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：rg 重放（任务点名）：`diff <(sed -n '426,526p' permissions.py) <(sed -n '406,504p' questions.py)` → 仅 docstring 措辞（permission↔question）与 `_fetch_*_for_dir` 目标名两处差异，其余逐字节相同——孪生结论成立。
- 行数修正：实测 `_collect_with_byte_budget` 为 101/99 行（证据写「121 行」，系行段口径差），不影响结论；~320 行总重复面量级维持。
- 维持 P3 / confirmed。原状态注记移入：verified（A10/D10 2026-08-20，AST 定量复核通过）。
## A10 复核记录（2026-08-20，D10）
- 维持 verified / P3。AST 定量复核通过（见证据）。
## 建议方向（非实现）
- 抽 `routes/_aggregate_common.py`：endpoint spec 表（path/semaphore/flight-key 前缀/config knob 三元组/item 投影函数）+ 共享 discovery/collect/pack；两文件各剩 handler+spec <60 行（镜像 _catalog_common 先例）。
