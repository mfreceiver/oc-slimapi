# F-239：半开探针单样本即可闭合熔断 + SELECT 1 与投影延迟不同源——恢复证据弱、可振荡
- 严重度：P3
- 类别：risk（设计冻结口径内的弱点，非实现偏离）
- 位置：src/oc_slimapi/dbaux/lifecycle.py:214-217（trip 清窗）+ :219-230（note_probe 无最小样本要求）+ :641（SELECT 1）
- 置信度：high（D10-2/D10-3 复核确认，快照 0b836e7）
- 快照：0b836e7
- 状态：confirmed
## 证据
- trip()：`self._open = True; self._samples = []`（:216-217）——窗口清空，恢复只需新鲜证据。
- note_probe：`self._samples.append((now, latency_ms)); p99 = self._p99(); if p99 < self._recover_ms: self._open = False`（:225-229）——**单样本即 P99=该样本**；首个 <10ms 探针即闭合。无 min_samples 对偶约束（对比 record 的 :208-209）。
- 探针延迟测量：probe :623-629 以墙钟包裹 `await self._submit(self._probe_sync)`——含 executor 排队延迟；探针体 `SELECT 1`（:641）不读表页 → **与投影查询（SCAN/JOIN/物化）延迟分布不同源**。
- 反向锚定：test_breaker_hysteresis_probe_recovery（tests/test_dbaux_lifecycle.py:489-506）——单个 15ms 慢探针把 P99 钉 60s（最近邻秩 n<100 时 ≈ max），快探针不稀释。
## 分析
- 振荡周期：trip → 30s 探针等待 → 单快探针闭合 → ≥10 个真实慢查询（窗口 ≥10 后判）→ 再 trip。持续 P99≥20ms 负载下周期 ≈ 30s + 10 查询——503 窗口与慢 200 窗口交替。
- 与 §2.3-6 冻结措辞（「半开探针样本计入后 P99 < 10ms 恢复」）逐字一致——弱点属设计证据阈值，不属实现偏离；恢复证据（1 个 SELECT 1）系统性弱于 trip 证据（≥10 个真实查询样本）的不对称是根因。
- 影响：熔断器的抖动面（Retry-After: 30 与实际恢复节奏可能不齐）；护栏功效不减（再 trip 门槛不变）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——lifecycle.py:214-217 trip 清窗、:219-230 note_probe 单样本即可闭合（无 min_samples）代码摘录核对一致；:641 SELECT 1 与投影延迟不同源命中。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「verified（2026-08-20，D07 / A7；state-machines.md 卡 10 D10-2/D10-3 种子）」辖属/批次注记移入本记录）。

## 建议方向（非实现）
- note_probe 要求最小探针数（如 3）或要求窗口含真实查询样本后才可闭合；探针体改为轻量真实读（如 `SELECT 1 FROM session LIMIT 1`）使延迟同源。
