# F-136：shell/PTY deny-list 死配置残余——`shell_deny_list_enabled` 旋钮无实现可关、启动日志仍广播、INTERFACE_MAP 假 Ops 行
- 严重度：P3（初判，按 §3.3 决策表待 Phase 2/3 复核定级）
- 类别：smell（兼 ops/docs 漂移）
- 位置：src/oc_slimapi/config.py:491-498；src/oc_slimapi/app.py:636,644；docs/specs/INTERFACE_MAP.md:149
- 置信度：high（A3 rg 全仓实证 + 负向证据，D03 §1.2 第 2 项）
- 快照：0b836e7
- 状态：confirmed
## 证据
- config.py:491-498：`shell_deny_list_enabled: bool = os.getenv("OC_SLIMAPI_SHELL_DENY_LIST_ENABLED", "1")…`；注释（492-493）称「the path table is code-level in proxy.py (hardcoded from B0 §1.3 route scan)」——**proxy.py 已无任何路径表**（全文 51 行仅 404/501 边界）。
- 消费者全集（rg `shell_deny_list`，src+tests+deploy）：config.py:495（定义）、app.py:636,644（启动日志行 `shell_deny_list_enabled=%s`）——**无任何行为分支读它**。
- deny-list 实现确已删除：rg `shell|pty|deny` 全 src 无路径表残余；tests/test_proxy.py:116-127 钉死 `/session/{sid}/shell`、`/pty`、`/api/pty/x` → **404** thin_route_not_found（非 403 shell_not_allowed）且上游零接触。
- docs/specs/INTERFACE_MAP.md:149 仍描述该开关为活「Ops 开关」（「关闭后 deny-list 不生效，不构成安全保证」）——描述的行为已不存在。
- docs/ocmar/specs/2026-07-18-v1-b0-b1-design.md:137 与 ocmar/plans 为历史文档（可保留）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：3.0.0 删除 forwarder 时，deny-list 实现（路径表 + 403 `shell_not_allowed`）随之删除，但配置旋钮、启动日志广播、INTERFACE_MAP 行三处配套面漏删。
- 影响面：运维按 INTERFACE_MAP/日志以为存在第二道防线并可 break-glass 关闭——实际开关是纯 no-op（改 env 只改日志文本）；审计面产生「已启用 deny-list」的假信号。
- 触发条件/可达性：恒在（启动日志每次广播）；无 wire 影响。
## 契约/文档对照（规范轨引文）
- v3-contract §8.2-3.0.0「catch-all 关闭…WebSocket 仍 501 stub」（deny-list 随反代隐式退役，契约未单列该开关——无契约义务保留）。
- CHANGELOG [3.0.0] L1063「catch-all 反代关闭」+ proxy.py:16-24 docstring「Shell/PTY deny list … moved or deleted」。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——config.py:491-498 旋钮定义+注释、app.py:636/:644 启动日志、INTERFACE_MAP.md:149 假 Ops 行三处命中；rg `shell_deny_list` 全仓=3 处（config.py:495 定义 + app.py:636,644 日志）无行为分支；proxy.py 全文 51 行无路径表；tests/test_proxy.py:116-127 shell/pty→404 钉死——死配置残余成立。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「draft（A3/D03 产出）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 删除 config.py:491-498 旋钮 + app.py:636,644 日志字段 + INTERFACE_MAP.md:149 行（或改为历史注记）；若保留 env 兼容（已部署机器可能 set），可降级为 warning「ignored since 3.0.0」。
