# F-314：hub_types.py 公共类型聚合点评估——扇入 4 全部限于 sse 域，聚合合理（正向结论）
- 严重度：P3（informational——A11 任务 3 耦合度量确认项）
- 位置：src/oc_slimapi/sse/hub_types.py:1-419
- 类别：verification
- 置信度：high（import 图实测）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 扇入 4（import 图）：sse/global_hub.py、sse/hub.py(shim)、sse/registry.py、sse/tokenstream/hub.py——**全部在 sse 包内**；routes/events.py 经 sse.hub shim 间接消费 Subscriber/sse_frame。
- 内容四类（inventory 符号表）：哨兵/常量（STOP/_UNSET/IMMEDIATE/DEBOUNCE_SECONDS 等）、事件集合（SESSION_EVENTS/MESSAGE_EVENTS）、帧 helper（sse_frame/_upstream_line_bytes/_sanitize_error_message）、数据类型（DigestFields:~180-260/Subscriber:~264-346/SubscriberCapacityError）+ 归一化 helper（normalize_session_status——3.3.1 修复新增的跨 hub 共享点）。
- 模块级无可变状态（e1-05 卡 :151：全部不可变常量/哨兵/编译正则 + 两个 object 哨兵）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：hub_types 是 sse 域内「类型+协议常量」聚合点——不是全局上帝接口（对比典型反模式：全仓 types.py 吸引跨域耦合）。fan-in 封闭于包内 + 无状态 + 无行为依赖 = 聚合点健康。
- 3.3.1 修复把 normalize_session_status 放此处是正确落位（digest 与 token hub 双消费方均在 sse 域）。
- 影响面：无（正向确认）。
## 契约/文档对照
- 无涉。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：rg 重放：`rg -ln 'hub_types import' src/` → 扇入恰 4 = global_hub.py、registry.py、hub.py(shim)、tokenstream/hub.py——全部 sse 包内，与证据计数精确吻合；模块级无可变状态口径维持。
- 维持 P3 / confirmed（informational 正向结论）。原状态注记移入：draft（A11/D11 §5.2）。
## 建议方向（非实现）
- 保持；若 F-301 拆分落地后 tokenstream 域需要自己的类型模块，参照本文件模式建 `tokenstream/types.py`，勿把 tokenstream 专属类型再堆进 hub_types（守住「域类型在域内」的现有边界）。
