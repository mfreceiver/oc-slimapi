# F-141：tokenstream/frames.py 复制 `sse_frame`/`_now_ms` 的「import 环」论证失效——hub_types 叶子化后可直连去重
- 严重度：P3（初判，按 §3.3 决策表待 Phase 2/3 复核定级）
- 类别：smell（结构债）
- 位置：src/oc_slimapi/sse/tokenstream/frames.py:19-40；对照 src/oc_slimapi/sse/hub_types.py:11-25（叶子事实）
- 置信度：high（A3 import 图实证，D03 §2.3 附带；e1-05 hub 卡疑问 4 同判）
- 快照：0b836e7
- 状态：confirmed
## 证据
- frames.py:25-36：注释称复制是刻意的——「a runtime `from .hub import _now_ms` here would still create a cycle」；论证据 hub.py（shim re-export registry/global_hub，再 TYPE_CHECKING 引 tokenstream）。
- hub_types.py 实际 import 集（:11-25）：仅 stdlib + orjson + `oc_slimapi.logging_config` + `.replay_wire.V4_RESYNC_REASONS`；replay_wire 仅 import `.replay_log` + stdlib——**hub_types 是叶子**，frames.py `from ..hub_types import sse_frame, _now_ms` 不构成环（环论证针对的是 hub.py shim 而非 hub_types）。
- 后果面：两个 `sse_frame` 函数对象与两个 `_now_ms`（hub_types vs frames）并存，且 **两个 STOP 哨兵**（hub_types.py:30 vs frames.py:19-21，e1-05 疑点 1 已记录跨体系误比较隐患）；tests/test_v3_sse_meta.py:43-44 需并排导入双哨兵佐证分叉已知。
## 分析（机制、影响面、触发条件、可达性）
- 机制：lite-v2 拆分时为避环选择复制；后续 hub_types 已叶子化（拆分完成），复制的理由过期但未被回收。
- 影响面：纯结构债——wire 字节格式目前 byte-identical（注释自证共享 orjson），但两份实现将来任何一份单独改动即产生帧格式分叉（SSE 帧是契约 §7 冻结面）。
## 契约/文档对照（规范轨引文）
- v3-contract §7（SSE 帧形冻结——两份 `sse_frame` 都是该冻结的实现载体，分叉即契约风险）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——frames.py:19-40 复制注释（import cycle 论证）逐字命中；hub_types.py import 集实读为 stdlib+orjson+logging_config+replay_wire（叶子）——「环论证针对 hub.py shim 而非 hub_types」的判断成立。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「draft（A3/D03 产出）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- frames.py 改 `from ..hub_types import _now_ms, sse_frame`（删本地副本与过期注释）；STOP 双哨兵是否合并需单独评估（控制面 vs token 流终结语义耦合风险），至少应在两定义处加互指警告注释。
