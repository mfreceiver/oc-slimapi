# F-005：deploy 残留废弃 env OC_SLIMAPI_SERVER_API_VERSION=2（warning+忽略）+ config 死错误路径点名旧 env
- 严重度：P3（初判，按 §3.3 决策表待 Phase 2/3 复核定级）
- 类别：ops
- 位置：deploy/oc-slimapi.service:32；src/oc_slimapi/config.py:796-806（废弃 warning 路径）、config.py:828-830 附近（钉扎校验）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- deploy/oc-slimapi.service:32：`Environment=OC_SLIMAPI_SERVER_API_VERSION=2`（与 :33 `OC_SLIMAPI_ACCEPTED_CLIENT_VERSIONS=2,2` 共同构成 F-004 的 crash-loop 面）。
- src/oc_slimapi/config.py（SERVER_API_VERSION 废弃路径）
- 佐证材料：config-census.md
- 【A3 补 2026-08-20】精确路径：config.py:796-806——`if os.getenv("OC_SLIMAPI_SERVER_API_VERSION") is not None:` → warning "OC_SLIMAPI_SERVER_API_VERSION is deprecated and ignored …"（不参与任何视图判定）；常量钉扎 `server_api_version: int = SERVER_API_VERSION`（config.py:436，源自 versioning.py:38 `SERVER_API_VERSION = 4`）。
- 【A3 补】生产实况（本机只读核对 `systemctl --user cat oc-slimapi`）：生产 unit **不含** :32/:33 两行（已较 repo 模板清理）且 override 启用了 actions（L82）——即该残留仅影响「按 repo 模板新部署」路径，与 operations.md 声称已清理的矛盾定位于 **repo 模板滞后**而非生产实况。
## 分析（机制、影响面、触发条件、可达性）
- 【A3 补】机制：4.0.0 起 `server_api_version` 常量钉扎（S-B04），env 仅触发 warning；但同文件 :33 的 `ACCEPTED_CLIENT_VERSIONS=2,2` 在 config.py:817-819 fail-closed 校验下是 RuntimeError（F-004 crash-loop）——本条（P3 warning 面）与 F-004（P1 启动失败）是同两行 env 的两层后果。
- 影响面：按模板部署必得一条 warning + 随即 F-004 崩溃；单独 set 本 env（无 :33）则仅日志噪音。
## 契约/文档对照（规范轨引文）
- CHANGELOG [4.0.0]（S-B04 双版本窗口）；operations.md:92-94「已清理」声明 vs repo 模板滞后（F-020 文档链同源）。
## 复核记录（Phase 3）
- 【A14 文档面复核 2026-08-20】锚点复核：deploy/oc-slimapi.service:32-33 两行仍在（本轮 sed 实证）；operations.md:92-94「模板不再示例」不实陈述在 D14 表记为 D4（P1，引用本条 + F-004，不另立）。文档链侧同源项已分流：develop.md:23-24 把同两 env 列常规 knob（归 F-020/D8，develop 入口按表设置 :33 等价 env 同样 startup-fatal）。本条 P3（warning 面 + 模板滞后）维持；终态（含 deploy 对账逐行裁决）归 A13。
- **终态 confirmed / P3**（Phase 3 归一；A13 待裁项由本条收口——deploy 三方对账已完成）。锚点与机制复核（2026-08-20）：deploy/oc-slimapi.service:32 `Environment=OC_SLIMAPI_SERVER_API_VERSION=2` 在库（本轮 grep 复核）；config.py:796-803 warning 路径直读（「deprecated and ignored … pinned to 4」）+ :436 `server_api_version: int = SERVER_API_VERSION` 常量钉扎——**「warning+忽略」定性精确**（与 F-004 的 :33 致命行分界清晰）；生产 unit（`systemctl --user cat` 只读）不含 :32/:33——「repo 模板滞后而非生产实况」结论复核成立。三方对账（repo 模板 / operations.md:92-94 / 生产 unit）：模板残留 :32 + operations 声称「模板不再示例」不实 + 生产已清理——ops 模板滞后 + 文档不实陈述双缺陷成立。原状态行括注「A3 证据链闭合 + A14 文档面复核；deploy 三方对账终裁归 A13」由本节承接。
## 建议方向（非实现）
- 删 deploy/oc-slimapi.service:32-33 两行（F-004 一并闭环）；warning 路径保留一两个版本周期作迁移提示后移除。
