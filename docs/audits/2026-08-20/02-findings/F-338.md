# F-338：sse_observability._emit 双 `except Exception: pass`——SSE 生命周期观测丢失零日志零计数
- 严重度：P3（观测丢失无信号；SSE 观测链自身无健康面）
- 类别：defect（observability）
- 位置：src/oc_slimapi/sse_observability.py:83-114（两段静默吞异常 :107-108,113-114）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- sse_observability.py:107-108：`write_sse_lifecycle_log` 调用包裹 `except Exception: pass`；:113-114：`ledger.record_sse_lifecycle` 同款——对比同链路的 middleware `_record`（traffic_accounting.py:343-344,418-419,434-435 三段均 `logger.warning`），此处连 warning 都没有。
- 调用点（events.py:182,242；token_stream.py:252,307）在 generator 内——注释自认「never break the stream」是设计取向，但静默程度超出必要（warning 不影响流）。
## 分析
- 机制：open/close 行写失败或 ledger bump 失败完全静默——`sseActive` 存量与生命周期行从这一刻起不一致（open 丢 = 低估；close 丢 = 高估 + 永久悬挂 open），且无任何线索。
- 影响面：SSE 观测数据完整性不可自证；§9.4 carry-in 对账的孤儿/悬挂解释空间增大（无法区分「真孤儿」与「观测丢失」）。
- 触发条件：access logger disabled 后仍连 SSE（此时每条 lifecycle 写入都进 except）——**该场景必现而非罕见**：ACCESS_LOG_ENABLED=false 时 `_emit` 的日志半边永远走 pass 分支（ledger 半边仍工作）。
## 契约/文档对照
- v3-contract §9.1（SSE 生命周期行）+ §9.2（sseActive 四维）：观测位设计存在；实现将其降级为 best-effort 且无诊断面。手册 §5.1（:179）声称「close 行必达（open/close 配对不泄漏）」——在 logger disabled/写失败时该承诺不成立，文档口径强于实现。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 sse_observability.py:107-108 与 :113-114（`except Exception: pass` ×2，行号逐字吻合）打开核对一致；对照 traffic_accounting.py:343-344/:418-419 三段均 warning——静默程度超出必要的对比成立。
- 维持 P3 / confirmed。原状态注记移入：verified（A13/D13 §1.3 #8；Phase 3 终态待定）。
## 建议方向（非实现）
- 两处 `pass` → `get_logger("sse_observability").warning(...)`（与 middleware 姿态对齐，仍不 raise）；手册 §5.1 的「close 行必达」补条件（logger enabled 前提）；可选：_emit 内加进程级丢失计数器进 metrics（与 F-337 元观测块合并）。
