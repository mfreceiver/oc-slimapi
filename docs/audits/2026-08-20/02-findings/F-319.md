# F-319：恒真断言——test_traffic_ledger.py:588 `assert … or True` 使该行永不失败
- 严重度：P3（单个断言失效；用例的可执行部分「must not raise」仍有效）
- 类别：test
- 位置：tests/test_traffic_ledger.py:588（`TestStashUpIn` 类 `test_no_scope_attribute_is_safe_noop`）
- 置信度：high（布尔短路直读；全仓唯一 `or True` 命中）
- 快照：0b836e7
- 状态：confirmed
## 证据
```python
def test_no_scope_attribute_is_safe_noop(self):
    class NoScope:
        pass
    req = NoScope()
    stash_up_in(req, 100)  # must not raise
    assert not hasattr(req, "scope") or req.scope is None or True
```
- `… or True` 恒真：无论 `hasattr` 结果如何断言必过。rg `or True\b` 全 tests/ 仅此 1 处；`assert True` 全仓 0 处。
## 分析（机制、影响面、触发条件、可达性）
- 意图大概是「无 scope 属性 = 安全 no-op」的宽松表述，但落成永真式后，`stash_up_in` 若在 no-scope 路径上写入/抛错以外的行为变化（如意外创建 scope）不会被此行捕捉（该具体场景下 hasattr 仍为 False，前半已为真——真正失效的是对未来实现漂移的表达力；当前实现的保护由 `# must not raise` 的执行语义承担）。
## 契约/文档对照（规范轨引文）
- 无（纯函数级防御测试）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 test_traffic_ledger.py:588 打开核对逐字一致：`assert not hasattr(req, "scope") or req.scope is None or True`——`or True` 恒真；`rg 'or True\b' tests/` 仅此 1 复现。
- 维持 P3 / confirmed。原状态 verified。
## 建议方向（非实现）
- 改为 `assert not hasattr(req, "scope")`（no-op 语义：不创建 scope）或直接删除该断言保留执行不抛错语义。
