# F-321：flaky 面复核——148 处真实墙钟 sleep（串行合计约 86s）+ 3 文件以 monotonic 计时参与行为断言（高负载假失败风险）
- 严重度：P3（时间虚拟化纪律总体良好：25 处 sleep(0) 让步 + 22 处 patch/FakeClock 注入 + 27 处真实时钟读取中仅少数入断言）
- 类别：test
- 位置：全 tests/ 计量（A12 复跑）：`sleep(` 182 处 = 25 处 `sleep(0)` + **148 处 >0 真延迟**（合计 ≈85.9s）+ 9 处注入形；真延迟 Top：10.0s×2（test_batch3_lifecycle.py:502,545 被取消路径）、5s×7（子进程 argv）、4.0s（test_actions.py:949）、3.5s/2.2s（test_full_absorb.py:241,164）、3.0s（test_transform.py:385）、1.2s×5（catalog/etag/fingerprint TTL）
- 置信度：high（rg 全量重提取 + 与 test-census §2 交叉一致）
- 快照：0b836e7
- 状态：confirmed
## 证据
1. 三类计数（A12 口径）：**[真延迟] 148 处 / [可控注入] 22 处**（counting_sleep/fast_sleep/FakeClock/_FrozenClock/_CountingDateTime）**/ [真实时钟读入断言] 6 处**（test_b1b_sweep_shadow.py:149-151 夹逼、test_post_actions_v4.py:406-409,604-608 夹逼、test_transform.py:358-390 与 test_full_absorb.py:180-197 与 test_messages_routes.py:338-340 monotonic 参与行为断言、test_globalhub_retired_gate.py:325 时间戳构造）。
2. 与 census §2 对照：census 计 191 行 sleep 证据 / 45 行注入面 / 27 处真实时钟——A12 按函数体收紧口径后为 148/22/6+3；方向一致，无矛盾。
3. random/urandom 13 处全为安全用法（不可压缩体构造、shuffle 鲁棒性、urandom 重试循环），v4_fixture.py:86-87 显式规避 hash() 随机化（正面样例）。
## 分析（机制、影响面、触发条件、可达性）
- 最重串行真延迟 >30s 集中在 test_actions/test_full_absorb/test_transform/test_catalog_cache/test_leased_singleflight（census §2.5 结论维持）；monotonic 行为断言（transform 线程池占用时长、absorb 预算窗、health 阻塞时长）在 CI 机器高负载时可能假失败——这是唯一的真 flaky 向量（其余真延迟是确定性等待，最多拖慢不失败；少数 0.05-0.3s 竞态窗口在极端调度下有脆弱性，census §2.1 已列）。
## 契约/文档对照（规范轨引文）
- 无契约面。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：rg 重放：`rg -c 'sleep\(' tests/` 合计 → **182 处**，与证据总数 182（= 25 sleep(0) + 148 真延迟 + 9 注入形）自洽；10.0s×2 / monotonic 行为断言三处（test_transform.py:358-390 等）行号抽核一致。
- 维持 P3 / confirmed。原状态 verified。
## 建议方向（非实现）
- 三处 monotonic 行为断言放宽容差或改注入时钟；批量并发类文件（leased_singleflight/batch3）的 k×周期+0.05s 等待考虑统一 FakeClock 化（已有 22 处注入先例可复用）。可选：pytest 标记 slow 分离 >1s 用例。
