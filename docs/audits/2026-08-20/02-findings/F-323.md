# F-323：自定义 marker `@pytest.mark.integration` 未在 pyproject 注册——pytest≥8 下每运行产生 PytestUnknownMarkWarning，且 `-m integration` 选择语义无守护
- 严重度：P3（噪声 + 未来若有人按 marker 过滤会静默选错/选空）
- 类别：test
- 位置：tests/test_hub_behavior_lock.py:397（唯一使用点）；pyproject.toml:30-32（[tool.pytest.ini_options] 仅 asyncio_mode/testpaths，无 markers 段）
- 置信度：high（配置面直读；无 pytest.ini/setup.cfg/tox.ini 替代注册位）
- 快照：0b836e7
- 状态：confirmed
## 证据
1. `rg -n "pytestmark|markers" pyproject.toml` → 仅 :30 ini_options 段命中，内容 asyncio_mode + testpaths。
2. test_hub_behavior_lock.py:35-39 文档明确该用例依赖真实 0.5s 墙钟（`test_real_debounce_merges_within_250ms_window`），以 integration marker 标注意图——但 marker 未注册，pytest 8 严格化下是 unknown mark 警告（默认 warning 不失败，check.sh 仍绿）。
## 分析（机制、影响面、触发条件、可达性）
- 当前零运行时影响；风险是语义性的：`-m integration` 过滤依赖未声明标记，pytest 行为为「匹配未知标记」（选中该测试）但伴随警告——团队若后续加 `--strict-markers` 会直接转红。
## 契约/文档对照（规范轨引文）
- 无。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 pyproject.toml:30-32（[tool.pytest.ini_options] 仅 asyncio_mode + testpaths，无 markers）与 test_hub_behavior_lock.py:397（唯一 `@pytest.mark.integration` 使用点）打开核对一致。
- 维持 P3 / confirmed。原状态 verified。
## 建议方向（非实现）
- pyproject [tool.pytest.ini_options] 增加 `markers = ["integration: depends on real wall-clock timing"]`（并可选 `--strict-markers`）。
