# F-218：emitted_frames_total 计「投递尝试」而非「成功投递」——put() bool 返回值被全局忽略
- 严重度：P3
- 类别：observability
- 位置：src/oc_slimapi/sse/global_hub.py:486-488（心跳）、:593-599（业务帧）、:988-991（resync_all）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- hub_types.py:264-273 `Subscriber.put` 明确返回 bool（v6 §3.5 加性：True = 实际入队）；
- global_hub.py:593-599 `_emit_directory_frame`：`for subscriber ...: subscriber.put(...)` 返回值丢弃，随后 `emitted_frames_total += len(self.subscribers)`——closed/超大丢/溢出断连的订阅者也计入；
- :486-488 心跳同型（`put(frame)` 后 `+= len(self.subscribers)`），控制帧与业务帧混同计数器；
- :988-991 resync_all 同型。
- 消费面：registry.py:344-350 `snapshot_metrics` 直透 `emittedFramesTotal`（/slimapi/metrics）。
## 分析（机制、影响面、触发性）
- 机制：指标语义 = attempted ≠ emitted。慢客户端断连场景下计数系统性虚高；心跳帧（10s × N 订阅者）在长连接下主导计数，业务帧占比被稀释——省流审计（AGENTS.md 流量审计用途）用该数评估策展产出会失真。
- 无 wire 影响；纯观测口径问题。触发性：常态存在（每心跳 tick）。
## 契约/文档对照（规范轨引文）
- 契约未逐字冻结该计数器语义（§2/§9 观测位为形状冻结）；属语义与命名偏差。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——global_hub.py:593-599 put 返回值丢弃 + emitted_frames_total += len(self.subscribers) 命中；hub_types.py:264-273 put() bool 返回契约 docstring 命中。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「verified（A6/D06 2026-08-20；Phase 3 终审）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 采纳 put() bool：`emitted_frames_total += sum(1 for ... if put(...))`；心跳另立 `heartbeat_frames_total`（加性 metrics 键，走 CHANGELOG 观测加性）。
