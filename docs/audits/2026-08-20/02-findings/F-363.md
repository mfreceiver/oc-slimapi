# F-363：质量门禁描述三处滞后低估——release.md/AGENTS.md 仍称「pytest 最小集、compileall 后续可选」
- 严重度：P3
- 类别：docs
- 位置：docs/release.md:16（§0 表「质量门禁」行）、docs/release.md:152-161（§4）、AGENTS.md:73（硬规则括注「当前 = `pytest tests/`」）
- 置信度：high（三处原文 vs check.sh 实际三项，逐行对照）
- 快照：0b836e7
- 状态：confirmed
## 证据
- check.sh 实际执行三项检查：pytest（scripts/check.sh:21-22）+ 路由↔文档对账（:24-25，check_routes_doc.py）+ compileall（:27-28）；基线输出实证（logs/check-baseline.txt：「✅ 路由↔文档一致：54 条…」「==> compileall src」）。
- docs/release.md:16：§0 对照表质量门禁行写「`./scripts/check.sh`（`pytest tests/`）」——漏两项。
- docs/release.md:154-161（§4）：「最小集合（当前）：pytest…可选扩展（**后续**）：`compileall`、ruff/mypy、安装包可导入检查」——compileall 已是 enforced 现状而非「后续可选」。
- AGENTS.md:73：「必须 `./scripts/check.sh` 通过才算改动完成（当前 = `pytest tests/`）」——括注滞后（AGENTS.md:61 同表另一行倒是提到了对账门禁，自相不一致）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：check.sh 于早前版本扩展了对账+compileall（check.sh:6 用法注释已列三项），规范文档未回写。
- 影响面：文档读者（新 agent/运维）低估门禁范围；「可选扩展（后续）」清单与现状重叠（compileall），未来真加 ruff/mypy 时语义混乱；AGENTS.md 两处自相矛盾（:61 vs :73）。
- 无行为风险（门禁本身是超集——文档低估不影响 enforcement）。
## 契约/文档对照（规范轨引文）
- AGENTS.md 硬规则「改动校验必做…（当前 = `pytest tests/`）」（:73）与本仓「契约权威以文档为准」的纪律取向相悖——属入口索引文档自身漂移。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 release.md:16（质量门禁行「`./scripts/check.sh`（`pytest tests/`）」）与 check.sh:18-28（实际三项：pytest + check_routes_doc + compileall）打开核对一致；AGENTS.md:73「当前 = pytest tests/」同批在案——三处低估 + :61/:73 不一致实证。
- 维持 P3 / confirmed。原状态注记移入：verified（2026-08-20，D15 / A15）。
## 建议方向（非实现）
- release.md §0 表与 §4 改为列全三项（pytest + check_routes_doc + compileall）；AGENTS.md:73 括注改为「当前 = pytest + 路由↔文档对账 + compileall」。
