# F-315：dbaux/lifecycle.py 768 行三职责混载——纯函数错误分类 / LatencyBreaker 熔断器 / DbAuxiliarySource 生命周期状态机
- 严重度：P3（可维护性；D07 已覆盖 dbaux 行为面，此处仅模块化维度）
- 类别：design（A11 数据驱动入围：行数>500(768)）
- 位置：src/oc_slimapi/dbaux/lifecycle.py:97-768
- 置信度：high（def 行号图实测 + e1-08 卡片）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 三段异质职责（def 图实测）：
  - 无状态纯函数：schema_gate_missing(:97)/classify_sqlite_error(:114)（~50 行）；
  - LatencyBreaker 熔断器（:147-252，~105 行，独立小类：open/_prune/_pctl/_p99/record/trip/note_probe/reset/snapshot）；
  - DbAuxiliarySource 生命周期状态机（:286-768，~480 行：swap/probe/disable/trip/bounded executor/只读连接双保险 `_open_conn` :499）+ DbAuxStatus(:270)。
- 变更频率 2（2026-08-01 以来）+ 修复 0 轮——低 churn、无缺陷史。
- 扇出 2、扇入 2（app.py + dbaux/__init__ re-export）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：三段间唯一耦合是 DbAuxiliarySource 持有 LatencyBreaker 实例与调用 classify_sqlite_error——天然的组合关系而非内聚关系；纯函数段被 __init__ re-export 供外部错误映射使用。
- 影响面：低（低 churn）；拆分收益主要是让「熔断器」这个可复用原语（未来 traffic/transform 池若需同款 breaker）脱离 dbaux 命名空间。
- 触发条件：dbaux 演进或 breaker 复用需求。
- 可达性：低优先级。
## 契约/文档对照
- v4-contract 降级矩阵（§sessions v4）语义不变——纯内部。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 wc -l lifecycle.py = **768 行** ✓；三段职责（纯函数 :97-114 / LatencyBreaker :147-252 / DbAuxiliarySource :286-768）行号抽核一致；变更频率 2 + 修复 0 的低 churn 定性维持。
- 维持 P3 / confirmed。原状态注记移入：draft（A11/D11 §4.7）。
## 建议方向（非实现）
- 低优先拆分：`dbaux/errors.py`（classify+gate，~50 行）、`dbaux/breaker.py`（LatencyBreaker，~105 行）、lifecycle.py 留 DbAuxiliarySource（~600 行仍 >500 但单一状态机，参照 singleflight.py keep 判例）；`dbaux/__init__.py` re-export 保持外部路径不变。测试锚点：8 个测试文件（rg 实测）全走 public 符号，零迁移。
