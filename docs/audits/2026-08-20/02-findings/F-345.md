# F-345：记账三段 best-effort 无对账——access log 行先于 ledger 写（单侧行/账本不一致窗口）+ ledger 段统一异常标签 "record_upstream failed" 误导排障
- 严重度：P3（诊断噪音/一致性口径；无数据正确性错误——两库各自自洽）
- 类别：quality（observability 诊断性）
- 位置：src/oc_slimapi/middleware/traffic_accounting.py:319（先行写行）→ :346-419（后记账）,:418-419（标签）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- traffic_accounting.py:319-342 先写 access log 行，:346 起 ledger 五个 record 调用（downstream :370 / matrix :379 / expand :393 / degraded :401-406 / upstream :410-417）——两段独立 try；行成功+账本异常（:418-419）时行已落盘：该请求在 access log 有、在 metrics/snapshot 缺（反之亦然的顺序缺陷不存在——行先写）。
- :418-419 `logger.warning("record_upstream failed", …)` ——该 try 块含**全部五个** record 调用，标签只点名 upstream：排障按日志找 upstream 路径会走错方向。
- 对照：access log 段标签精确（:343-344 "write_access_log failed"）；degraded 计数器段独立且标签精确（:434-435）。
## 分析
- 机制：顺序（行先账后）+ 粗粒度 try + 误导标签三者叠加；单请求视角不一致窗口 = 该请求生命周期终点（无补偿/重放）。
- 影响面：低概率（record_* 纯内存操作，异常面极窄）；一旦发生，metrics.buckets 与 access log 聚合的长期对账出现不可解释偏差——而 §9.4 恰以两者对账为核心用法。
- 触发条件：ledger record 抛异常（锁内 deque/dict 操作理论异常）。
## 契约/文档对照
- 手册 §9.3 已声明三源「不同口径不直接对照」——但「同请求行/账本分裂」不在该声明覆盖内（那是跨源口径差，这是同源丢失）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 traffic_accounting.py:319（先 `write_access_log`）与 :418-419（`logger.warning("record_upstream failed", …)` 覆盖全部五个 record 调用的粗粒度 try）打开核对一致；对照 :343-344 精确标签成立。
- 维持 P3 / confirmed。原状态注记移入：verified（A13/D13 §1；Phase 3 终态待定）。
## 建议方向（非实现）
- :418 标签改 "ledger record failed (downstream/matrix/expand/degraded/upstream)" 或拆五段小 try（最小：改标签字符串）；可选在 warning 中带 bucket+path 便于对账定位；文档不必改（内部诊断面）。
