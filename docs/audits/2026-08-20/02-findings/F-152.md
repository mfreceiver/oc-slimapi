# F-152：405 错误体 code `method_not_allowed`（非 GET /slimapi/versions）无契约归宿——三契约（v2/v3/v4）与 CHANGELOG 均未载
- 严重度：P3（§3.3 contract-violation 行——措辞/未载类；状态码与 Allow 头已冻结，仅 body code 字面无主）
- 类别：contract
- 位置：src/oc_slimapi/selector.py:536-545（`json_response({"code": "method_not_allowed"}, status_code=405, headers={"Allow": "GET"})`）；docs/specs/v3-contract.md:49（§2「非 GET → 405 + `Allow: GET`，优先级高于一切」——未载 body code）；docs/specs/v4-contract.md:35（§2 同款）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- selector.py:540 `{"code": "method_not_allowed"}` + `Allow: GET`；
- 负向搜索：`rg -n 'method_not_allowed' docs/specs/v2-contract.md docs/specs/v3-contract.md docs/specs/v4-contract.md CHANGELOG.md` → 0 命中（v3 §2/v4 §2/§8.4 均只冻结状态码与 Allow 头；v2 无此端点语义）；
- 测试锁定：tests/test_terminal_matrix.py:201 `test_priority_405_over_everything`（断言 405 + Allow，**未断言 body code**——行为已锁定但 code 字面游离于规范外）。
## 分析（机制、影响面、触发条件、可达性）
- 三向对账「无主码」之一：实现发出、契约族（含 CHANGELOG）无一记载。客户端按 `{"code":...}` 惯例解析 405 body 时拿到的串无规范出处，未来改动无契约约束。
- 影响面：错误路径表示细节；触发条件 = 对 /slimapi/versions 发非 GET。
## 契约/文档对照（规范轨引文）
- v3 §2 / v4 §2（versions 405 优先级条款——只冻结状态+Allow）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——selector.py:536-545 405 `{"code":"method_not_allowed"}` + Allow: GET 命中。证据链成立，维持严重度与建议方向。
- P3 负向 rg 重放：`rg -n 'method_not_allowed' docs/specs/v2-contract.md docs/specs/v3-contract.md docs/specs/v4-contract.md CHANGELOG.md` → 0 命中（exit 1）——无主码成立。
- 状态行归一：confirmed（原「verified」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- v4-contract §2 表行补一句：405 body = `{"code":"method_not_allowed"}`（冻结字面）；或明确「body 形状不冻结」以关闭歧义。测试补 code 断言。
