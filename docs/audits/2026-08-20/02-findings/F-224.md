# F-224：双哨兵/双实现族（STOP×2、sse_frame×2、_now_ms×2）无一致性防护——跨体系误用即 TypeError / 字节漂移
- 严重度：P3
- 类别：quality（结构性腐化风险）
- 位置：src/oc_slimapi/sse/hub_types.py:30,105-107,110-111 vs src/oc_slimapi/sse/tokenstream/frames.py:19,25-36（:22-30）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- STOP：`hub_types.STOP`（:30）与 `tokenstream/frames.STOP`（:19，注释自述 "kept local to avoid a runtime import cycle"）是**不同 object()**；`sse/token_hub.py` shim 转发的是 tokenstream 版；
- `sse_frame`：hub_types.py:105-107 与 frames.py:25-36（复制实现，注释称 "Both copies share orjson so … byte-identical"）——无共享断言测试钉死；
- `_now_ms`：hub_types.py:110-111 与 frames.py:22-30 同型复制（后者注释自述防 import 环有意复制——但复制目标是 hub.py shim 而非叶子 hub_types，理由与可选方案有出入）；
- 误用面：`TokenSubscriber.put(控制面STOP)` → `frame is STOP` 为 False → `len(object())` TypeError（subscriber.py:397）；两份 `sse_frame` 一改一漏即 v3 字节漂移。当前生产无跨用（A6 rg 复核 routes/events.py:7 与 routes/token_stream.py:61-66 各取各的），测试 test_v3_sse_meta:43-44 同时导入两者佐证分叉已知。
## 分析（机制、影响面、触发性）
- 现行零行为缺陷；风险 = 演进期漂移（任一副本独立修改）+ 跨体系误用无类型防呆。frames.py 复制 `sse_frame` 的理由（import 环）针对 hub.py 而非叶子 hub_types——本可 import hub_types 消除一份。
## 契约/文档对照（规范轨引文）
- v3-contract §7「帧名/帧形零变化」——双实现是该冻结面的内部威胁点。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——hub_types.py:30 与 frames.py:19 两处 `STOP = object()`（不同对象）命中；frames.py:22-36 复制注释命中。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「verified（A6/D06 2026-08-20；Phase 3 终审）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- frames.py 改 import `hub_types.sse_frame/_now_ms`（叶子无环，注释已确认可选）；STOP 双哨兵可保留但补一个 `assert hub_types.STOP is not frames.STOP` 文档化测试 + 类型注解区分（NewType/独立类型名）。
