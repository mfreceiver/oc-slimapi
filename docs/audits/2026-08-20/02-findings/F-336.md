# F-336：retain 口径双偏差——「保留 N 天」实存 N+1 个日历日，且 access log 3 天 × snapshot 30 天窗错配使 §9.4 对账在 >4 天窗口系统性失真
- 严重度：P3（口径/文档缺口；对账工具在长窗失真）
- 类别：docs（ops 口径）
- 位置：src/oc_slimapi/access_log.py:560-575（prune 边界）；src/oc_slimapi/traffic_snapshot.py:79-101（同式）；deploy/oc-slimapi.service:45-46；docs/manual/traffic-accounting.md:252,257,363-365
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- access_log.py:562 `deadline = today - retain_days`；:575 `if file_date < deadline`（严格小于）——边界日（today-3）**保留**：retain_days=3 实存 4 个日历日文件（today 与 today-1/2/3）；traffic_snapshot.py:79-101 同式（30 → 31 天）。手册 :252/:257 与 operations.md:232/:233 均写「删除早于 N 天」未写边界保留——「保留 3 天」的直觉口径与实现差一天。
- 窗错配：deploy:46 ACCESS_LOG_RETAIN_DAYS=3 vs deploy:45 TRAFFIC_SNAPSHOT_RETAIN_DAYS=30——access log（矩阵明细源）只存 4 天，snapshot 存 31 天。
- 手册 §9.4（:363-365）跨日 carry-in 公式与 `aggregate_v3_observability` 对账依赖 open/close 行配对：窗口 >4 天后 access log 缺失，起点无 open 行 → 孤儿 close 只补计数不冲减（by design）+ 期初存量无来源 → sseActive/sseLive 在长窗系统性失真；手册未提示对账窗上限。
## 分析
- 影响面：①磁盘/合规口径差一天（低）；②「30 天 snapshot 支撑 ≥30 天对账」的隐含预期不成立——snapshot 帧本身有 v3 节（每 300s 全量），单侧趋势可用，但与 access log 的双向对账只在 4 天窗内有效。
- 触发条件：任何 >4 天窗的 §9.4 对账或「保留 N 天」合规核对。
## 契约/文档对照
- v3-contract §9.2（snapshot 聚合矩阵 ≥30 天）——契约只要求矩阵维度设计，不承诺 access log 对账窗；实现不违约，手册口径缺口。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 access_log.py:562（`deadline = today - retain_days`）+ :575（`if file_date < deadline` 严格小于 → 边界日保留）与 traffic_snapshot.py:79-101 同式（:82/:93）打开核对一致；deploy:45-46（SNAPSHOT=30 vs ACCESS=3）✓——N+1 日历日 + 窗错配实证。
- 维持 P3 / confirmed。原状态注记移入：verified（A13/D13 §2.3；Phase 3 终态待定）。
## 建议方向（非实现）
- 手册 §6/§9.3/§9.4 三处补：边界保留语义（N 天=N+1 日文件）+「carry-in 对账窗 ≤ ACCESS_LOG_RETAIN_DAYS+1」；如需 30 天全窗对账，生产应把 ACCESS_LOG_RETAIN_DAYS 提到 ≥30 或接受单侧（snapshot-only）趋势口径。
