# F-225：token flush_loop 看门狗重建无退避/预算——flush() 确定性异常 + 有消费者 = 10Hz CRITICAL 无限重建刷屏
- 严重度：P3
- 类别：risk
- 位置：src/oc_slimapi/sse/tokenstream/hub.py:443-476（`_on_flush_done` watchdog）
- 置信度：high（机制确定；当前不可达——flush() 调用链无抛错点）
- 快照：0b836e7
- 状态：confirmed
## 证据
- flush_loop :484-517 仅重抛 CancelledError——任何其他异常杀 task；
- watchdog :469-476：`logger.critical(...)` + `if self.has_consumers(): self._flush_task = None; self.start()`——**立即**重建（无退避、无 retry budget、无自终止）；
- 重建周期 = TOKEN_FLUSH_SECONDS(100ms) → 最坏 10Hz CRITICAL 日志 + 无限 task 创建/销毁循环；
- 当前不可达性核验（A6 逐点）：flush() → _fanout_frame → _replay_publish_token（:1389-1394 全吞）/ _deliver_logged → sub.put（返回 bool 不抛）/ events_tap → Subscriber.put（不抛）/ _drain_pending_session_resyncs → _fanout_resync → terminate/put（不抛）——现行调用链无抛错点，不变量靠约定不靠类型（docstring :424-428 自认 "defensive"）。
## 分析（机制、影响面、触发性）
- 触发：未来任一 flush 路径引入异常（新帧类型序列化、新 tap 回调、metrics 写点）即成 10Hz 风暴——日志洪水 + CPU 空转，且无升级信号（每条 CRITICAL 同型）。对照 GlobalHub 组 supervisor（global_hub.py:287-327）同型无退避，但 run/flush/heartbeat 三任务结构使其同理。
- 与 F-011（registry 拆除 task 无 except）同族：异步自愈组件缺 failure budget。
## 契约/文档对照（规范轨引文）
- 无契约面（内部生命周期）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——tokenstream/hub.py:469-476 watchdog logger.critical + `self._flush_task = None; self.start()` 立即重建（无退避）命中。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「verified（A6/D06 2026-08-20；Phase 3 终审）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 重建加指数退避（复用 run() 的 delay 模式）+ 连续失败阈值后降级（停重建 + 持久 CRITICAL 一条 + 依赖下次 first-attach start() 恢复）。
