# F-318：TDD 脚手架注释声称的 xfail(strict=False) 标记实际不存在——两文件头部与用例 docstring 描述已失效的标记状态
- 严重度：P3（无行为影响；误导维护者对测试语义的理解，且掩盖「修复已落地」这一事实）
- 类别：test
- 位置：tests/test_upstream_error_boundary.py:20-22,:46-47（「Every test in this file is marked xfail(strict=False)」）；tests/test_hub_behavior_lock.py:29,:1716,:1741（「marked xfail(strict=False)」「staying xfail until…」）
- 置信度：high（rg `pytest.mark.xfail|^pytestmark` 全 tests/ 零命中；check-baseline 绿色 + 断言为「修复后」期望值，证明标记确已移除而注释未同步）
- 快照：0b836e7
- 状态：confirmed
## 证据
1. `rg -n "pytest\.mark\.xfail|pytestmark" tests/` → **0 命中**；全文 xfail 字样仅存在于注释/docstring（上述 4 处 + test_upstream_error_boundary.py:20,:22）。
2. test_upstream_error_boundary.py 两个测试均断言 503 + `{"code": "upstream_unavailable"}` 精确体（:130-161），而文件头仍写「Currently uncaught → 500」「each test will xpass; the xfail markers can then be removed」——基线绿色（logs/check-baseline.txt rc=0）证明实现已修复、测试直绿，标记已删，注释全部过时。
3. test_hub_behavior_lock.py:1716 用例断言 `activeTransforms == 7`（spy 注入值），基线绿色证明 Batch2 公有 API 路由已落地；:1716/:1741 docstring 仍称「Today the registry reads pool._semaphore directly … stays xfail」。
## 分析（机制、影响面、触发条件、可达性）
- 读者会据注释推断这些测试当前 xfail/xpass 漂移状态，错误评估回归网强度；且「xfail(strict=False) 会吞掉回归」的心智模型与实际（直绿强断言）相反——实际状态比注释**更强**，属文档性过时而非保护缺失。
## 契约/文档对照（规范轨引文）
- 无契约面（纯测试内文档）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：rg 重放：`rg -n 'pytest\.mark\.xfail|pytestmark' tests/` → **0 命中**复现；xfail 字样仅存于注释/docstring（test_upstream_error_boundary.py:20/:22/:46-47、test_hub_behavior_lock.py:29/:1716/:1741——行号逐一核对吻合）。
- 维持 P3 / confirmed。原状态 verified。
## 建议方向（非实现）
- 删除/改写两文件头部与 :1716/:1741 的 xfail 叙述为「修复已落地，直绿回归锁」；一并清理 test_upstream_error_boundary.py:77 的 `VERSION_HEADERS = {"X-Slimapi-Version": "1"}` 陈旧语境注释（该头已退役不解读，用作无害 header 无妨，但注释宜说明）。
