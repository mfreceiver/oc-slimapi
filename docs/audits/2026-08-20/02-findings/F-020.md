# F-020：文档链式漂移：develop.md 钉值过时（3,3 vs 实际 3,4）、check_routes_doc L311 修复提示指向已退役 v2-contract、measure_token_overhead 代码锚点失效（sse_frame 已迁出 hub.py:110）
- 严重度：P3（A14 终判维持初判；docs 漂移行）
- 类别：docs
- 位置：docs/develop.md:23-24；scripts/check_routes_doc.py:311；scripts/measure_token_overhead.py:11-12,58 + scripts/measure_token_overhead.md:3
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- **develop.md:23-24**（配置速查表）：`OC_SLIMAPI_SERVER_API_VERSION`（默认 `3`）/ `OC_SLIMAPI_ACCEPTED_CLIENT_VERSIONS`（默认 `3,3`）列为常规 knob。实际：versioning.py:38 `SERVER_API_VERSION = 4`、:44 `(3, 4)`；两 env 均已弃用——前者设置仅启动 warning + 忽略（config.py:796-804，常量钉扎 config.py:436），后者设非 (3,4) 值 **startup RuntimeError**（config.py:817-822，validate() 于 lifespan 调用）。表内未标弃用状态（operations.md:92-94 已标）。〔docs-notes D8〕
- **check_routes_doc.py:311**（语义漂移分支的修复提示文案）：「修复：在该路由的 INTERFACE_MAP 行补齐对应错误码描述（与 `docs/specs/v2-contract.md` §7 对齐）」——错误码权威现为 v3-contract §8（v2 已于 3.0.0 退役）。gate 功能本身不受影响（仅提示文案指错权威文件）。
- **measure_token_overhead 陈旧代码锚点**：measure_token_overhead.py:11-12「mirroring `sse_frame()` from `src/oc_slimapi/sse/hub.py:110`」+ :58 注释「mirror hub.py:110 sse_frame」+ measure_token_overhead.md:3 同款——rg 实证 `sse_frame` 现位于 `src/oc_slimapi/sse/hub_types.py:105` 与 `src/oc_slimapi/sse/tokenstream/frames.py:33`，`sse/hub.py` 内**无**该函数（`rg "def sse_frame" src/` 全集两处，均非 hub.py）；hub.py:100-112 现为其他内容。
- 佐证材料：01-explore/docs-notes.md D8；03-reports/D14-docs-drift.md §1/§5。
## 分析（机制、影响面、触发条件、可达性）
- 机制：三处独立滞后——develop.md 配置表未随 4.0.0 弃用收编；check_routes_doc 提示文案写于 v2 时代；measure_token_overhead 的锚点随 sse_frame 模块重组（迁 hub_types/frames）失效。
- 影响面：develop.md 是配置调参入口——读者按表设 `OC_SLIMAPI_ACCEPTED_CLIENT_VERSIONS=3,3` 会直接 startup-fatal（与 F-004/F-005 的 deploy 残留同后果、不同入口）；check_routes_doc 提示误导修复方向；measure_token_overhead 锚点失效只影响维护者考古。
- 触发条件/可达性：develop.md 表被照抄即触发；后两处仅阅读面。
## 契约/文档对照（规范轨引文）
- CHANGELOG [4.0.0]（S-B04 双版本窗口 + env 弃用）；v3-contract §8（错误码权威）；AGENTS.md:75（SQLite/写域无关，版本双轨以 versioning.py 为准）。
## 复核记录（Phase 3）
- **终态 confirmed / P3**。锚点抽查（2026-08-20）：develop.md:23-24 两行 `OC_SLIMAPI_SERVER_API_VERSION`（默认 `3`）/ `OC_SLIMAPI_ACCEPTED_CLIENT_VERSIONS`（默认 `3,3`）在库（本轮 sed 直读）——与 versioning.py:38/:44 实际（4 / (3,4)）漂移命中；check_routes_doc.py:311 修复提示「与 docs/specs/v2-contract.md §7 对齐」在库（本轮 sed 直读）——权威已为 v3 §8，指错文件命中；measure_token_overhead 锚点（hub.py:110 已非 sse_frame 所在）A14 已 rg。三处文档漂移均无反证（无后续修订痕迹）。原状态行括注「A14」由本节承接。
## 建议方向（非实现）
- develop.md:23-24 两行标「已弃用（4.0.0）：SERVER_API_VERSION 设置仅 warning+忽略；ACCEPTED 非钉死值启动拒绝」或直接移出速查表（operations.md:92-94 已有权威说明）；check_routes_doc.py:311 文案改「与 docs/specs/v3-contract.md §8 对齐」；measure_token_overhead.{py,md} 锚点改 `sse/hub_types.py:105`（或 `tokenstream/frames.py:33`）。全部纯文档/注释级变更（check_routes_doc 文案改动不影响其校验逻辑，check.sh 照常）。
