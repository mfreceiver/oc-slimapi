# F-347：AGENTS.md 入口索引 4.0.0 未跟进族——ACCEPTED_CLIENT_VERSIONS 写 [3,3]、v3-only 表述、check.sh 描述失实、「当前不读 SQLite」自相矛盾
- 严重度：P3
- 类别：docs
- 位置：AGENTS.md:20,21,22,73,78,116（另 :113 design-v2「当前态设计」索引行关联）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- AGENTS.md:78（硬规则「版本双轨」）：「`ACCEPTED_CLIENT_VERSIONS`，当前 `[3,3]`」——实际 versioning.py:44 `ACCEPTED_CLIENT_VERSIONS: tuple[int, int] = (3, 4)`、:38 `SERVER_API_VERSION = 4`。
- AGENTS.md:21：「`/slimapi/**` **v3-only 选择器**（`?v=3`…）」；:22：「权威契约：v3-contract（唯一 wire 基准，**v3-only 终态**…）」；:116（文档索引）：「Wire 契约权威（**v3-only 终态**…）」——4.0.0 起 (3,4) 永久双版本窗口（v4-contract §0.3、CHANGELOG [4.1.0]）；且 :121-122 索引表又并列列出 v4-contract.md，文内自相矛盾。
- AGENTS.md:73（硬规则）：「必须 `./scripts/check.sh` 通过才算改动完成（**当前 = `pytest tests/`**）」——实际 check.sh = 三项：pytest + check_routes_doc.py + `compileall src`（scripts/check.sh:18-27）；:61 流程表描述为「pytest + 路由↔文档一致性 + release.md §质量门禁」亦不含 compileall、且 release.md 门禁非 check.sh 执行项。文内 :61 与 :73 两处描述互不一致且均与脚本不符。
- AGENTS.md:20：「当前**不读** opencode SQLite（v4 起经只读投影源 mode=ro 读…）」——自相矛盾句式：4.0.0 已发版且生产部署 dbaux（operations.md §7），读 SQLite 是现状；「当前不读…（v4 起…读）」括号自否。
- 关联：:113 索引行把 design-v2.md（头部「aligned with v2-contract.md」+「禁 SQLite」+「版本走必填请求头」，docs-notes S1）定位为「当前态设计（接口/骨架/部署）」。
## 分析（机制、影响面、触发条件、可达性）
- 机制：AGENTS.md 是全部 agent/开发流程的入口索引与硬规则载体；4.0.0 发版时未同步（check.sh 三项化更早发生，:73 从未跟上）。
- 影响面：所有依赖 AGENTS.md 的 agent 与新贡献者——版本窗事实错误会传导到发版判断（major 绑定规则以 ACCEPTED_CLIENT_VERSIONS 变更为铁律，错误基线值 [3,3] 可能误导「bump 到 (3,4) 需要 major」类推理）；check.sh 描述失实弱化质量门禁认知。
- 定级：入口文档事实错误但非运行时、且权威契约文件本身正确（F-151 辖契约内字面）——P3。
## 契约/文档对照（规范轨引文）
- versioning.py:38,44（(3,4)/4）；v4-contract §0（双版本原则）；scripts/check.sh:18-27；operations.md §7（dbaux 生产现状）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 versioning.py:38（`SERVER_API_VERSION = 4`）+ :44（`ACCEPTED_CLIENT_VERSIONS = (3, 4)`）对照 AGENTS.md:78（「当前 `[3,3]`」）与 AGENTS.md:73（「当前 = `pytest tests/`」）打开核对一致——入口索引事实错误实证；:21/:22/:116 v3-only 表述 + :61/:73 自相矛盾同批在案。
- 维持 P3 / confirmed。原状态注记移入：verified（A14）。
## 建议方向（非实现）
- :78 改「当前 `(3,4)`」；:21/:22/:116 的「v3-only 终态」改「(3,4) 双版本窗口（v4 差异面见 v4-contract.md）」；:73 与 :61 统一为 check.sh 实际三项；:20 改「经只读投影源 mode=ro 读（4.0.0 起），绝无写入」；:113 design-v2 索引行改「历史设计（v2 时代；现行契约见 v3/v4）」。纯文档变更。
