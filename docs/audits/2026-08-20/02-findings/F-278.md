# F-278：v4 sessions SQL 索引假设仅靠手工 eqp_matrix.py 实证——真库索引漂移无 CI 门禁，只靠运行时熔断兜底
- 严重度：P3（A9 初判；gap——面被消费方使用但运行时兜底存在（LatencyBreaker + 503 降级矩阵），非无保护裸奔）
- 类别：gap
- 位置：src/oc_slimapi/dbaux/projection.py:238-239（`ORDER BY s.time_updated DESC, s.id DESC` + `LIMIT ? + 1` + 可选 LIKE）；scripts/eqp_matrix.py:1-24（自述定位）；tests/v4_fixture.py:44-55,211-226；tests/test_eqp_matrix.py
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- scripts/eqp_matrix.py 模块头（:1-24）：48 组合 EQP 矩阵（archived 3 × parent 4 × cursor 2 × search 2）在**合成临时库**断言 planner 结构特征（SCAN/SEARCH/TEMP B-TREE，「不断言 EQP 全文案」）；真库模式（`--real-db`）为**数据采集不断言**（P50/P99 采样）。
- scripts/check.sh：仅 pytest + 路由↔文档一致性——eqp_matrix.py 不在门禁内（真库依赖本机路径，无法入 CI 可理解）。
- tests/test_eqp_matrix.py + tests/v4_fixture.py:226（`eqp = load_eqp_matrix()` 构造 fixture DDL）——测试固化的是 **SQL 文本 + 合成 DDL/EQP 行为**，不固化真库 opencode.db 的索引可用性。
- 运行时兜底：LatencyBreaker 60s 窗/10 样本/P99≥20ms trip/<10ms recover（src/oc_slimapi/dbaux/lifecycle.py:150-165）+ `busy_timeout=5000`（:302）+ 503 `auxiliary_unavailable` `Retry-After: 30`（src/oc_slimapi/routes/sessions.py:274,305）。
- 硬规则约束：sidecar 零 DDL（AGENTS.md「SQLite 写域」），索引建立属显式运维动作——即索引面完全在上游/运维控制下，sidecar 无法自卫。
## 分析（机制、影响面、触发条件、可达性）
风险形态：上游 opencode 版本 repoint 后真库 schema/索引变化（或运维建索引动作缺失/回滚），投影 SQL 退化为全表 SCAN + TEMP B-TREE 排序——大库下 P99 劣化 → 熔断打开 → v4 sessions 全部 503/降级（消费方可观察，但降级矩阵是冻结契约行为，非错误）。当前未发生（eqp_matrix 真库基线 ~0.015ms 量级，脚本自述）；属「假设无机械校验」缺口而非现行缺陷。
## 契约/文档对照（规范轨引文）
- v4-contract §4.2 降级矩阵（503 `Retry-After:30` / `degraded:true`）——运行时兜底与契约一致。
- AGENTS.md「上游对照常用路径」约定 repoint `current` 符号链接时需复核——eqp_matrix 重跑是该约定的自然组成但未写入清单。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 dbaux/projection.py:238-239 打开核对一致（`ORDER BY s.time_updated DESC, s.id DESC` + `LIMIT ? + 1`）；eqp_matrix 合成库断言 + 真库采集不断言的口径与 scripts/check.sh 不含该脚本的事实维持。
- 维持 P3 / confirmed（gap：假设无机械校验，运行时熔断兜底在案）。原状态 draft。
## 建议方向（非实现）
将「repoint `opencode-src/current` 时重跑 `scripts/eqp_matrix.py --real-db` 并比对 P99 基线」写入 AGENTS.md 上游对照清单或 docs/operations.md 升级 SOP；可选：dbaux 启动探测顺带对真库跑一次 EQP 并记入 health/metrics 观测位。
