# F-320：错误体断言强度两极——selector/providers 族精确整body断言，路由级 5xx/502 大量只断 code 子集（217 处 code-only vs 73 处整body）
- 严重度：P3（错误 body 的附加字段——hint/supported/queryDirectory/Retry-After 等——在路由级测试多为子集断言；wire 冻结面靠少数精确测试承载）
- 类别：test
- 位置：全 tests/ 计量：`resp.json() == {` 73 处（test_providers_projection_v4×14、test_terminal_matrix×13、test_selector×13、test_v4_dual_window×7、test_b4_allowlist×7 等）；`["code"] == ` 217 处（test_permissions/test_questions_routes/test_sessions_routes/test_children_routes/test_diff_routes/test_todo_routes 等路由级 503/502 族为主）
- 置信度：high（全仓 rg 计量 + 抽读核对）
- 快照：0b836e7
- 状态：confirmed
## 证据
1. 精确侧样例：test_terminal_matrix.py:122-123（`{"code":"unsupported_version","supported":[3,4]}` 整体）；test_v4_dual_window.py:271（RETIREMENT_BODY 逐键）；test_sessions_v4_matrix.py:271-277（auxiliary_unavailable + Retry-After:30 + 负向泄露断言）。
2. 子集侧样例：test_children_routes.py:280-292（upstream 5xx/网络错误仅 `["code"]=="upstream_unavailable"`）；test_permissions.py:294-346 同型——这些错误体的其余字段（如 sidecar 是否附 Retry-After、错误体 gzip 协商）无整body锁定。
3. 逐类抽查结论（A12 §断言强度）：error_unsupported_version/invalid_version_selector/directory 三态/directory_retired_in_v4/tokens_stream_retired_in_v4/method_not_applicable = **精确整body**；upstream_unavailable/upstream_http_\<N\>/transform_busy/response_too_large/request_too_large = **多数 code-only**（transform_busy 有 Retry-After 断言的例外：test_children_routes.py:347、test_directories_routes.py:412、test_sessions_routes.py:744）。
## 分析（机制、影响面、触发条件、可达性）
- 风险场景：上游错误映射层若给 503/502 body 添加/改名附加键（如未来加 hint），code-only 测试全绿而 ocdroid 侧解析受影响。契约对错误 body 形状的冻结度本身有限（v4 §8.1 只冻结 code 清单 + 优先级），故此为「测试弱于理想」而非「违约」。
## 契约/文档对照（规范轨引文）
- v3 §8.1/v4 §8.1：错误码清单与状态映射；v4 §4.2：503 恒 Retry-After:30（有精确锁定，test_sessions_v4_matrix.py:272）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：rg 重放（全仓计量）：`\.json\(\) == \{` → **73 处**、`F '["code"] == '` → **217 处**——与证据 73/217 **精确吻合**；精确侧/子集侧样例行号（test_terminal_matrix.py:122-123、test_children_routes.py:280-292 等）抽核一致。
- 维持 P3 / confirmed。原状态 verified。
## 建议方向（非实现）
- 对消费方直接解析的错误族（upstream_unavailable、upstream_http_\<N\>）在各路由专测文件补 1 条整body断言（每族一条即可，无需逐路由）；或在 errors.py 层加一条「所有注册码渲染形状」的表驱动精确测试（test_errors.py 已有雏形，扩为全码清单）。
