# F-287：read_groups 两条 v4 分支管线再实现共享读链骨架 + write_groups archive 自读 body 循环同款复制
- 严重度：P3
- 类别：smell（重复代码）
- 位置：src/oc_slimapi/routes/read_groups.py:247-368（_handle_providers_v4）、:455-517（_session_single_native_fallback）、:496-500；src/oc_slimapi/routes/write_groups.py:368-376（archive 自读循环）vs :135-144（_write_passthrough 循环）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- `_session_single_native_fallback`（read_groups.py:455-517）内嵌 stream_upstream→5xx drain→`raise_upstream_unavailable`→4xx 逐字→`read_with_cap`→413→offload→TransformBusy→busy_response 链——与 `_read_passthrough.read_passthrough_get`（_read_passthrough.py:194-243）逐段同构（~40 行重复）。
- `_handle_providers_v4`（read_groups.py:247-368）§12 十二步自成全链，fetch→status 映射→cap→pool→offload→emit 骨架 ~50 行与共享管线同构（差异：no-store 盖章、permit 后置——已冻结语义）。
- 同文件同码不同字段名：session fallback 413 用 `limit=`（read_groups.py:499），providers 413 用 `limitBytes=`（:334，§12.5.3 冻结）——同模块内同名错误双字段（关联 F-289）。
- write_groups.py:368-376 archive 路由复制 `_write_passthrough` 的 body+cap 循环（:135-144），注释自认「the SAME loop/order」——判界/顺序漂移风险双侧维护。
- 对照：sessions.py:266-269 v4 fork 重复系**注释在案的零接触策略**（zero-touch v3 pipeline）；read_groups v4 分支无此豁免注释亦无 v3 字节等价测试钉住。
- 佐证：03-reports/D10-code-quality.md §1.ii/§1 热区 2/5。
## 分析（机制、影响面、触发条件、可达性）
- v4 分支是 2026-08-19 修订新增面：复制时点近、尚未漂移，但 §12/§13 修订继续演化时共享管线修复（如错误体排水 cap 化，F-291）不会自动到达 v4 副本——**已发生一例：providers/单查回退的错误体读取各自内联，策略与 _read_error_body 不共享**。
- write archive 循环：单模块内 9 行 ×2，同判界同序；漂移后果=archive 与 PATCH 的 413 判界不一致（wire 可见）。
- 可达性：v4 面（?v=4 ∧ readiness 门控开）每请求走 v4 分支；archive 为 §16.2-c 便捷动作。
## 契约/文档对照（规范轨引文）
- v4-contract §12.5.2（十二步冻结序）、§13.2（fallback 列）、§16.2-c（archive 判据 octet 层冻结）——重构不得改评估序/字段名；合并方向须保序（§12 no-store/permit 后置为参数化差异）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 read_groups.py:333-334（providers 413 用 `limitBytes=`）与 :497-499（session 回退 413 用 `limit=`）打开核对一致——同模块同码双字段名实证；write_groups.py:363-376 archive 自读循环（注释自认 "the SAME loop/order"）对照 :135-144 成立。
- 维持 P3 / confirmed。原状态注记移入：draft（A10/D10 2026-08-20）。
## 建议方向（非实现）
- 扩展 `_read_passthrough`（status_map/error_fields/pool_after_fetch 钩子）或至少复用 `_read_error_body`；write 侧提 `_read_capped_body()` 助手供 _write_passthrough 与 archive 共用；顺带统一 session-fallback 的 413 字段名为 §12 形或注释指向 F-289。
