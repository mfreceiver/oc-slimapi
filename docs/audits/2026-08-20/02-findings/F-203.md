# F-203：sessions v3/v4 响应尾 orjson.dumps 双跑 + sha256 + json_response gzip 在事件循环上（envelope 小、纪律不一致）
- 严重度：P3
- 类别：smell/risk（事件循环上的序列化+哈希+压缩；现实量级小）
- 位置：src/oc_slimapi/routes/sessions.py:101-135（v3 `_finalize_sessions_response`）、:636-650（v4 `_v4_json_response`）；src/oc_slimapi/gzip_util.py:115-122（`json_response`）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- sessions.py:124-126：`identity = orjson.dumps(payload)` 后 `json_response(payload, ...)` 内部**再跑一次** `orjson.dumps`（gzip_util.py:117）——同 payload 双序列化（注释自认"sessions.py:101 … runs on the event loop … duplicated pass is the cost of keeping json_response untouched"；v4 侧 :636-638 同款口径）。
- `compute_etag`（sha256 over identity）与 `json_response` 的 `gzip.compress(level 6)`（gzip_util.py:121）均在 loop。
- 佐证材料：03-reports/D05-concurrency.md §6.2 N3。
## 分析（机制、影响面、触发条件、可达性）
- 机制：sessions envelope 为白名单投影（`skeleton_session`），量级 KB~低百 KB——双 dumps + sha256 + gzip 合计毫秒级以内，**现实影响小**；本条价值在纪律一致性：同为列表类路由，messages 有 worker（F-201 的例外）、catalog/providers 全 worker、sessions 全 loop，三种口径并存。
- 触发条件：任意 sessions 列表 200/304（gzip-capable 时含压缩）。
## 契约/文档对照（规范轨引文）
- 无 wire 面（响应字节不变）。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——sessions.py:122-128 identity=orjson.dumps 后经 json_response 内部再 dumps（gzip_util.py:117）双跑命中；gzip_util.py:115-122 json_response gzip L6 命中。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「confirmed（A5/D05 Phase 2；Phase 3 终审）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 与 F-201/F-202 整改合并考虑：统一"序列化/压缩/验证器哈希的线程归属"清单；`json_response` 至少可接受现成 identity bytes 避免 double-dumps。
