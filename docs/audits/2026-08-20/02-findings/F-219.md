# F-219：_check_pending_budget 的 had_subs 全局口径——无订阅 sid 的 pending 溢出仅 force-flush 静默丢帧、不逐出不回收（docstring 与实现语义偏差）
- 严重度：P3
- 类别：defect
- 位置：src/oc_slimapi/sse/tokenstream/hub.py:1848-1862（`_check_pending_budget`）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- :1853 `had_subs = self.subscriber_count > 0`——**全局**（所有 sid 之和，hub.py:360-375）；
- :1855 `self.flush()`（drain 全部 pending；无订阅 sid 的 delta 帧 `_deliver_logged` 返回 0，帧丢弃——published 语义已入 ReplayLog，但零订阅 token 域无消费者）；
- :1858 `if (not had_subs) or ...: candidates ... _evict_part_for_memory(...)`——逐出仅在**全局**无订阅时发生；
- docstring :1835-1844 自述 "if there were NO subscribers when the overflow was detected ... LRU-evict the oldest LivePart"——per-domain 语义 vs 全局实现偏差。
## 分析（机制、影响面、触发性）
- 机制：sid B（无任何 token 订阅者）持续产 delta、sid A（有订阅者）存在时——全局 `subscriber_count > 0` → 只 force-flush：B 的 delta 每 4MiB 阈值触发一次全量 flush（帧丢），B 的 LivePart 继续增长，仅靠 LIVE 4MiB/32 预算 LRU 兜底（该路径正确写 barrier+resync）；且 B 的每条 delta 反复触发跨 sid 全量 flush（:1855 flush() 排空所有 sid 的 pending）——放大为对 A 的额外 fanout 开销。
- 触发：未订阅会话的高频生成（v4 客户端断连后上游继续生成）+ 另一会话有活跃订阅。影响：B 侧流信息丢失（v4 重连靠 barrier/窗口兜底）+ 无谓 flush 开销；无内存泄漏（LIVE 预算兜底）。
## 契约/文档对照（规范轨引文）
- v4-contract §7.2 背压/重放语义不受影响（帧已 published 入日志）；属内部预算策略与自述文档的偏差。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——tokenstream/hub.py:1848-1862 had_subs = subscriber_count > 0（全局）+ flush() 全量排空 + 全局无订阅才逐出命中。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「verified（A6/D06 2026-08-20；Phase 3 终审）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- `had_subs` 改 per-sid 口径（`self._subs_by_sid.get(current_key[0])`）或在 docstring 载明全局口径的取舍；同时评估 force-flush 范围限缩为溢出 key 所属 sid。
