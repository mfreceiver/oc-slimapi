# F-140：qp_sweep 阶段 1 shadow 价值悬置——metrics 暴露零消费方、阶段 2 无排期无取消、`sweep` 桶恒空、`directory_source` 生产死参
- 严重度：P3（初判，按 §3.3 决策表待 Phase 2/3 复核定级）
- 类别：risk（机制设计安全，价值面悬置）
- 位置：src/oc_slimapi/qp_sweep.py:1-7,41,124-129；src/oc_slimapi/app.py:501-517；src/oc_slimapi/routes/metrics.py:40-44；src/oc_slimapi/traffic.py:109-110
- 置信度：high（A3 rg docs 消费侧为零 + 装配面实证，D03 §6.2）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 装配活：app.py:501-517（`qp_sweep_enabled` 默认 true，config.py:622-625；observer 接线 `global_hub.set_directory_observer`）；纯影子零上游 IO（qp_sweep.py:1-7 模块头）。
- 暴露面：`GET /slimapi/metrics` 的 `sweep` 块（routes/metrics.py:42-44，key 集 triggers_total/cold_hits/skips/budget_exhausted/est_bytes_total/known_directories）。
- 消费侧为零（rg 全 docs 除审计计划自身）：operations.md / traffic-accounting.md / INTERFACE_MAP.md:98 均只描述块存在，**无任何读取方法、jq 配方、告警阈值或决策流程**；告警建议（sweep skips 阈值）仍是 Phase 2 计划产物（docs/ocmar/plans/2026-08-20-v4-comprehensive-audit.md:709）。
- 阶段 2 状态：system-architecture-proposal-2026-08-17.md:165-166,L322 冻结了阶段 2 exit criteria（7 日窗公式、真实 sweep 请求计入）；CHANGELOG [3.3.0] L174「traffic bucket `sweep` 保留给阶段2 真实请求分类（阶段1 恒空）」——仓内无真实 sweep 代码（routes/metrics.py:40-41 "no HTTP sweep is issued"；traffic.py:109-110 的 `sweep` 桶无路由可命中，e1-13 疑点 2 同判）；无排期亦无取消记录。
- `directory_source` 生产死参：app.py:504-509 装配不传 → `_ingest_directory_source` 生产恒 no-op（qp_sweep.py:127）；仅 tests/test_b1b_sweep_shadow.py:368-383 注入（F-024 子项）。
## 分析（机制、影响面、触发条件、可达性）
- shadow 本身无缺陷级问题（零逃逸、日预算闸、seen_at 30 天驱逐——state-machines 卡 15 仅有低危疑点）；问题在**存在理由的兑现路径断裂**：产出（sweep 块）无人消费，后续（阶段 2）无排期——若永不做阶段 2，整套调度器 + reserved 桶 + 死参都是 F-024 同类遗留。
- 影响面：每进程常驻一个调度循环 + observer 回调路径 + metrics 块形状承诺（additive 契约面，删除需走 minor/major 判断）；认知负担（新维护者需理解 B1b 计划脉络）。
## 契约/文档对照（规范轨引文）
- CHANGELOG [3.3.0] L174（块/桶的加性承诺）；system-architecture-proposal §3.2a/L322（阶段 2 判据）——两文档均默认阶段 2 会来。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——qp_sweep.py:1-7 shadow-only 模块头、app.py:501-517 装配、routes/metrics.py:40-44 sweep 块、traffic.py:109-110 sweep 桶四锚点命中；rg `_shadow` 全 src 仅 traffic.py:109（无任何路由注册挂 /slimapi/_shadow/sweep）——桶恒空与生产死参论证成立。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「draft（A3/D03 产出）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- owner 二选一：①为阶段 2 排期并先补最小消费闭环（手册入口 + sweep skips/budget 告警阈值）；②声明放弃——届时 `sweep` 桶（traffic.py:109-110）、`directory_source` 参数、（若直投渲染已足够）shadow 整体进入退役清单。当前状态不应无限期悬置。
