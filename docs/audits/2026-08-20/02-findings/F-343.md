# F-343：桶归口径两处前缀陷阱——裸 `/slimapi/config` 落 other 桶（providers 语义丢失）、`/slimapi/sessionsfoo` 类过匹配归 sessions 桶（404 污染桶 errors4xx）
- 严重度：P3（对账口径陷阱；现实路由形态固定故影响低）
- 类别：defect（observability 口径）
- 位置：src/oc_slimapi/traffic.py:124（messages 前缀）,:141（sessions 前缀）,:151（config/ 带斜杠）,:190（other）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- traffic.py:151 `if path.startswith("/slimapi/config/"):` ——裸 `/slimapi/config`（无尾斜杠）不命中 → :190 `return "other"`：该 404 请求的观测离开 providers 语义域（现实唯一路由是 `/slimapi/config/providers`，裸路径是探测/错拼）。
- traffic.py:124 `path.startswith("/slimapi/messages")`、:141 `startswith("/slimapi/sessions")`——无尾斜杠/段界约束：`/slimapi/sessionsfoo`、`/slimapi/messagesbar` 归入 sessions/messages 桶（此类路径必然 404）→ 4xx 混入真实桶的 errors4xx，稀释桶错误率。
- 对照段严格实现：同文件 :203-222 `_expand_tail` 对 expand 路径做了段严格校验（空 sid/裸段不匹配）——证明仓库已有正确模式，前缀分支未对齐。
## 分析
- 机制：bucketize 前缀序中 messages/sessions 两分支用裸 startswith，其余多数分支（command/agent/questions 等）用「等于或带斜杠前缀」的正确形态——实现不一致。
- 影响面：`ratios.sessions`/`ratios.messages` 的错误率分母被探测流量污染；`other` 桶含语义上属 providers 域的 404。单用户部署量级小。
- 触发条件：客户端/扫描器请求畸形前缀路径。
## 契约/文档对照
- v3-contract §9.2（bucket 维度）未冻结具体归桶规则（ops 面）；手册 §3.2 桶表按真实路径描述（无畸形路径口径）——实现内部不一致为主。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 traffic.py:124（`path.startswith("/slimapi/messages")` 裸前缀）、:141（sessions 裸前缀）、:151（`startswith("/slimapi/config/")` 带斜杠）、:190（`return "other"`）打开核对一致——两处前缀陷阱 + 对照段（command :130 等号+斜杠形态、_expand_tail :122 段严格）成立。
- 维持 P3 / confirmed。原状态注记移入：verified（A13/D13 §1.2 行 #6 注记；Phase 3 终态待定）。
## 建议方向（非实现）
- 两分支改 `path == "/slimapi/messages" or path.startswith("/slimapi/messages/")`（与 command/agent 分支同款）；`/slimapi/config` 裸路径可归 providers 或保持 other 但在手册 §3.2 加一行畸形路径归桶说明。
