# F-018：pyproject 测试依赖 respx 全仓零引用（死依赖）
- 严重度：P3（A15 终判维持）
- 类别：quality
- 位置：pyproject.toml:21（`"respx>=0.22,<1"`）；src/ tests/ scripts/ 零代码引用
- 置信度：high（正/负向搜索 + 安装元数据三面实证）
- 快照：0b836e7
- 状态：confirmed
## 证据
- pyproject.toml:18-22 test extra 三项中 `respx>=0.22,<1`（:21）。
- 负向搜索（A15 复跑）：`grep -rn "respx" src/ tests/ scripts/` **零命中**；全仓命中仅 pyproject.toml 本体 + 审计工作区文档 + docs/ocmar/plans/2026-08-20-v4-comprehensive-audit.md（纯文字提及）。
- 安装面扩散实证：`src/oc_slimapi.egg-info/PKG-INFO:14`（`Requires-Dist: respx<1,>=0.22; extra == "test"`）与 `src/oc_slimapi.egg-info/requires.txt:9`——死依赖随 editable 安装元数据落地。
- 实际已装：logs/pip-list.txt（U0.3）`respx 0.23.1`（窗内合规但无消费者）。
- 上游 mock 方式旁证：tests 对 httpx 上游的模拟不经 respx（测试直接用 oc_slimapi 自身 httpx client + fixture；test_eqp_matrix.py 用本地 sqlite draft 库，v4_fixture.py:50-55）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：test extra 声明后从未被任何测试 import；每次 `pip install -e '.[test]'`（AGENTS.md:88、check.sh:15、docs/release.md:136 三处安装指令）仍会解析+安装 respx 及其依赖。
- 影响面：① 安装面/供应链无谓扩大（respx 0.23.1 实际占位，见 pip-list）；② 依赖窗口维护成本（`>=0.22,<1` 需随无消费者的演进无效跟进）；③ 误导后续贡献者以为测试栈含 httpx mock 层。
- 触发条件/可达性：无运行时影响（extra=test 只在开发环境安装）；纯仓库卫生问题。
- A12 交叉辖说明：INDEX 原记主辖 A12/A15——A12（测试有效性）视角 = 无失效断言面（respx 不存在任何「本应由它 mock 而没 mock」的测试）；A15 视角 = 死依赖 + 元数据扩散。本终判以 A15 供应链口径落档。
## 契约/文档对照（规范轨引文）
- AGENTS.md 常用命令 `pip install -e '.[test]'`（:88）与 check.sh:15 提示——安装指令为权威入口，test extra 内容无文档承诺；无契约面。
- docs/release.md §6「禁止把 secret…提交进仓」等发版纪律与本项无关；无违规，仅卫生。
## 复核记录（Phase 3）
- **终态 confirmed / P3**。锚点抽查（2026-08-20）：pyproject.toml:21 `"respx>=0.22,<1"` 命中；本轮复跑 `rg respx src/ tests/ scripts/` → 零代码引用（负向搜索复现）；egg-info 元数据扩散面（A15 已核）。死依赖定性无反证（无任何间接消费者）。原状态行括注「2026-08-20，D15 / A15」由本节承接。
## A12 复核记录（Phase 2 · 2026-08-20）
- 独立复跑负向搜索（`rg respx tests/ src/ scripts/` 零命中）+ 确认全部上游模拟走 `httpx.MockTransport`（37 文件 63 处）与 conftest `upstream_factory`——与 A15 结论一致，**P3 维持、verified 认可**。A12 侧补充：respx 缺席对测试有效性零影响（无任何「应 mock 而 未 mock」缺口归因于它）；本条主辖终判以 A15 落档为准，A12 无追加发现。
## 建议方向（非实现）
- 从 pyproject.toml test extra 删除 respx 行（:21），下次 `pip install -e '.[test]'` 后 pip-list 自然收缩；或在测试中真正引入 httpx mock 需求时再回添（当前上游模拟另有机制，无需求迹象）。
