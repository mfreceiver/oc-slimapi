# F-244：snapshot() 公开返回明文 DB path——未来消费方泄面跟踪点
- 严重度：P3
- 类别：risk
- 位置：src/oc_slimapi/dbaux/lifecycle.py:707-727（`snapshot()` 的 :722-726 `path` 键）
- 置信度：high（代码 + 消费面 rg 复核，快照 0b836e7）
- 快照：0b836e7
- 状态：confirmed
## 证据
- lifecycle.py:722-726：`"path": self._resolution.path if isinstance(self._resolution, ResolvedPath) else None`——公开 API 返回明文 DB 路径。
- 当前唯一消费方 routes/metrics.py:51-69 显式白名单取键并丢弃 path（:48-50 注释 "resolved DB path is deliberately NOT echoed"）；tests/test_dbaux_metrics.py:112-115 断言 `"path" not in block` 且响应文本无 `.db`。
- 契约 §4.2「错误体不泄露 DB 路径」/ §8 同姿态——wire 面当前零泄。
## 分析
- 风险是前瞻性的：`snapshot()` 是设计给 B5 观测泳道的公开形状；未来任何新消费方（debug 路由、新指标块、异常详情）直接 `**snap` 展开即把本机路径带上网。防泄依赖每个消费方的自觉白名单，而非源头不产。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——lifecycle.py:720-727 snapshot() 返回 "path": self._resolution.path 明文键命中。证据链成立，维持严重度与建议方向。
- 严重度归一：原 P4（越出 P0-P3 刻度）→ P3（Phase 3 归一化）。
- 状态行归一：confirmed（原「verified（2026-08-20，D07 / A7，新发现——跟踪点，非现行泄露）」辖属/批次注记移入本记录）。

## 建议方向（非实现）
- 源头收敛：`snapshot()` 去 path（或改名 `snapshot_internal` / 拆分 `path` 到显式的运维专用方法）；metrics 注释同步简化。
