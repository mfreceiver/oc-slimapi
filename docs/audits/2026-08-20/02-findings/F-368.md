# F-368：无 lockfile / 无哈希固定——7 依赖全范围解析，重装不可复现（fastapi 0.115→0.139.2 漂移已发生）
- 严重度：P3
- 类别：risk（供应链/复现性）
- 位置：pyproject.toml:10-22（dependencies + test extra 全为范围窗口）；全仓无 lock/requirements/constraints（git ls-files grep 实证零命中）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- pyproject.toml:10-15：`fastapi>=0.115,<1`、`httpx>=0.28,<1`、`orjson>=3.10,<4`、`uvicorn>=0.34,<1`；:18-22 test：`pytest>=8,<9`、`pytest-asyncio>=0.25,<1`、`respx>=0.22,<1`——全部范围、无任何固定。
- 无锁定机制：`git ls-files | grep -iE "lock|requirements|constraints"` 仅命中 tests/test_hub_behavior_lock.py、tests/test_metrics_replay_block.py（文件名撞词，非依赖清单）；无 uv.lock/poetry.lock/requirements*.txt/pip-tools 产物。
- **漂移实证**：声明下界 fastapi 0.115（:11）→ 本机解析 0.139.2（logs/pip-list.txt，U0.3 存档）——跨 24 个 minor 版本；uvicorn 0.34→0.51.0、orjson 3.10→3.11.9 同理。
- 安装指令全部走范围解析：AGENTS.md:88、scripts/check.sh:15、docs/release.md:136 均为 `pip install -e '.[test]'`。
- 无 pip-audit/safety 等审计工具配置；无 `--require-hashes` 用法。
## 分析（机制、影响面、触发条件、可达性）
- 机制：范围窗口 + 无 lock = 每次安装时点重新解析；不同时间/机器得到不同依赖树（含传递依赖，本快照 18 项传递面见 D15 §5.1）。
- 影响面：① 换机重建 .venv 后行为漂移面（fastapi/pydantic minor 行为差）；② 供应链暴露窗口（PyPI 新 release 自动进入解析集，major 上界是唯一闸门）；③ 审计结论时效性（本审计的 pip-list 快照只代表 2026-08-16 安装时点，U0.3）。
- 缓解（P3 而非更高的理由）：单机 systemd 部署、venv 路径钉死（deploy/oc-slimapi.service:18）；不发 wheel、无 PyPI 发布面；唯一消费方 ocdroid 经 mTLS 专线对接本机实例；生产不重建环境则不触发。
- 触发：换机/重建 venv/未来扩为多机部署时显现。
## 契约/文档对照（规范轨引文）
- docs/release.md/AGENTS.md 均无 lockfile 政策条款（未承诺也未禁止）——属供应链流程空白而非违规；docs/develop.md 的开发备忘同样只给范围安装命令。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 pyproject.toml:10-15（fastapi/httpx/orjson/uvicorn 全范围窗口）+ :18-22（test extra 同）打开核对一致；全仓无 lock/requirements/constraints（rg 文件名撞词仅测试文件）复现——范围解析 + 无锁定实证。
- 维持 P3 / confirmed。原状态注记移入：verified（2026-08-20，D15 / A15）。
## 建议方向（非实现）
- 低成本方案：`pip freeze > docs/constraints-YYYY-MM.txt`（或 requirements/ 目录）归档审计时点解析结果，安装命令不变、仅作复现参照；若将来上 CI 再升级为真 lock（uv lock / pip-compile + --generate-hashes）。
