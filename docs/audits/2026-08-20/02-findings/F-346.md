# F-346：README.md 整体停留 v2 时代——权威指针指向已退役 v2-contract、quick-start 命令使用已删除头（照抄即 400）、catch-all/gzip 描述过时
- 严重度：P3
- 类别：docs
- 位置：README.md:3,5,29,35-38,41,56-57
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- README.md:3：「它只通过 HTTP 调 opencode legacy API（**不读 SQLite**）」——4.0.0 起 dbaux 只读 SQLite 生产启用（operations.md §7「功能随 4.0.0 生效（生产已部署）」；versioning 双窗口同期）。
- README.md:5：「权威 wire 契约为 `docs/specs/v2-contract.md`（`v1-contract.md` 已废弃移除）」——v2 契约已于 3.0.0 退役两个 major 周期；现行权威 = v3/v4-contract（AGENTS.md:22,121-122）。
- README.md:29（快速开始验证命令）：`curl --fail -H 'X-Slimapi-Version: 2' http://127.0.0.1:4097/slimapi/health`——该头已于 3.0.0 删除（v3 §1「出现不解读」）且命令**无 `?v=3`**：selector 对无 `v` 请求返回 400 `unsupported_version`（selector.py:622-634；supported=[3,4]）。照抄即失败。
- README.md:35-38：「范围（v2 / wire `X-Slimapi-Version: 2`）」整节 +「每个请求（含 SSE）必须带整数头 `X-Slimapi-Version: 2`」+「其他 HTTP path：流式反代至 127.0.0.1:4096（catch-all）」——头已删、catch-all 已关（未收编路径 404 `thin_route_not_found`，proxy.py:47-53）。
- README.md:41：token stream「**默认 gzip**（lever2，首个 SSE gzip 例外）」——v3 终态 SSE 恒 identity、不 gzip（v3 §7.7；INTERFACE_MAP §3.1 已注明 lever2 随 v3 终态移除）。
- README.md:56-57：文档表「v2-contract.md = Wire 契约权威」「design-v2.md = 当前态设计」——未列 v3/v4-contract 条目。
- 负向证据：README 全文无 `?v=`、无 v3-contract/v4-contract 引用（rg 复核）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：README 最后实质更新停在 v2 时代（≤2.x），3.0.0（头退役/catch-all 关闭/envelope）与 4.0.0（双版本/dbaux）两个单点事件均未跟进。
- 影响面：仓库门面（GitHub 落地页/新消费方/新 agent 的第一入口）。quick-start 验证命令直接失败会把读者引向错误排障方向；「权威=v2-contract」与 AGENTS.md 权威序直接冲突。
- 定级理由：与 F-157（CLIENT_CHANGES 五族滞后，A4 终判 P3）同刻度——纯文档缺陷、非运行时；但可见度全仓最高，故单列。
## 契约/文档对照（规范轨引文）
- AGENTS.md:22「权威契约：v3-contract（唯一 wire 基准…）」+ :121-122（v3+v4 并列）——README:5 与之矛盾。
- v3-contract §1（五头退役表）/§8.2（catch-all 关闭）/§7.7（SSE 恒 identity）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 README.md:3（「不读 SQLite」）、:5（「权威 wire 契约为 v2-contract.md」）、:29（`curl --fail -H 'X-Slimapi-Version: 2' …/slimapi/health` 无 `?v=`）打开核对逐字一致——照抄即 400（versioning (3,4)、该头不解读）成立；:35-38/:41/:56-57 同批核对在案。
- 维持 P3 / confirmed。原状态注记移入：verified（A14）。
## 建议方向（非实现）
- 按 v3/v4 现态重写 §范围 与 quick-start：`curl --fail 'http://127.0.0.1:4097/slimapi/health?v=3'`；文档表补 v3/v4-contract 行、v2-contract 标「≤2.x 历史」；「不读 SQLite」改「经只读投影源 mode=ro 读、绝无写入」；catch-all 描述改 404。纯文档变更，无需发版。
