# F-367：eqp_matrix 真库 schema 兼容门失败静默 exit 0——无 else 分支、无 WARNING
- 严重度：P3
- 类别：defect
- 位置：scripts/eqp_matrix.py:440-447（gate 计算）、:544-545（仅通过分支打印）、:557-560（照写报告并 return 0）
- 置信度：high（逐行 + 分支穷举）
- 快照：0b836e7
- 状态：confirmed
## 证据
- scripts/eqp_matrix.py:440-447：`gate_passes: not missing_session and not missing_project` 仅写入 report dict。
- :544-545：`if report["gate"]["gate_passes"]: print("  schema 门：通过…")` —— **无 else 分支**：gate 失败时无任何输出/告警。
- :557-559 报告照写（含 gate=false 数据），:560 `return 0`——进程退出码恒 0（真库模式）。
- 对照（任务前提修正）：真库**路径不存在**是响亮失败 `sys.exit("ERROR: real DB not found…")`（:427-428，非静默）；draft 断言模式失败也响亮（:553-554 `sys.exit("exit 1: …")`）。静默面**仅** schema 门。
- docstring :27 自辩「真库模式为数据采集不断言」——但 gate 失败连 WARNING 都没有，与「采集并记录」的意图也不符（采集到不兼容 schema 的库时使用者无感知）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：gate 语义从 B0 设计的「门」退化为「记录字段」。
- 触发：指向一个列缺失（上游 schema 演进）的真库跑 `--real-db`。
- 影响面：当前零 CI 面（check.sh 不跑它，tests 仅 importlib 复用其纯函数——tests/v4_fixture.py:50-55、test_eqp_matrix.py:15-17，不执行 CLI），风险**潜伏**；若将来按 F-278（A9）的诉求把它接入机械门禁，会把「schema gate 失败 = 绿灯」的语义一并带入。
- 与 F-278 关系：F-278 = 真库索引漂移**没有**机械门禁；F-367 = 这把尺自身有一处失效刻度（schema 维度）——互补不重复。
## 契约/文档对照（规范轨引文）
- docs/specs/design-v4-dbaux.md（B0 配套）将 schema 门定位为兼容性判断依据；sidecar 生产路径的 schema 门（dbaux）是独立实现且 fail-closed（open_failed），本项仅限 scripts/ 实证工具。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 eqp_matrix.py:440-447（gate_passes 仅入 report dict）、:544-545（`if report["gate"]["gate_passes"]: print(...)` 无 else 分支）、:557-560（报告照写 + 无条件 `return 0`）打开核对逐行一致——schema 门失败静默 + 恒 0 退出码成立；:427-428 路径不存在响亮失败的对照 ✓。
- 维持 P3 / confirmed。原状态注记移入：verified（2026-08-20，D15 / A15）。
## 建议方向（非实现）
- :544-545 加 else 分支：`print("  schema 门：失败…", file=sys.stderr)` 并对 `--real-db` 模式给非零退出（或至少 stderr WARNING）；报告 json 已含 gate 字段，改动只涉及 CLI 呈现层。
