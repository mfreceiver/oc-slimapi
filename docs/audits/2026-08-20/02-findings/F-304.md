# F-304：questions.py 与 permissions.py 复制粘贴双维护（归一化相似度 0.832、302 行匹配块）——修复已出现单边遗漏风险
- 严重度：P2（可维护性 + 已证实的修复不对称）
- 类别：design（重复代码，A11 数据驱动入围：两文件均行数>500）
- 位置：src/oc_slimapi/routes/questions.py:1-504 与 src/oc_slimapi/routes/permissions.py:1-526
- 置信度：high（difflib 归一化实测 + CHANGELOG 修复轨迹比对）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 归一化 diff（question↔permission 词元归一后）实测：相似度 **0.832**；>5 行匹配块 10 个共 **302 行**（79/69/45/33/29/12/9/8/7/11），约占各自非空非注释行（377/383）的 80%。
- 符号级镜像（inventory）：两文件同名符号 `_DirFetchFailure`、`_MAX_AGGREGATE_ITEMS`、`_directories_from_sessions`、`_pack_*_envelope`、`_fetch_*_for_dir`、`_collect_with_byte_budget`——仅 OBJ 名不同。
- 修复不对称实证：CHANGELOG 1.1.1/1.1.3（questions 发现机制两轮根治：/session→/project→/experimental/session?roots=true）与 1.1.4（questions 加 read_with_cap 内存防线）**只改 questions.py**；permissions 的目录发现同期走 discovery.py 共享路径（`from ..discovery import`，rg 实测 questions.py:... permissions.py 均引用 `fetch_global_root_sessions_raw`）——但 fan-out 聚合、字节预算、错误收集骨架仍是两份拷贝。1.5.0 B1 修复（去重共享内存不变数）则被迫同时触达两文件（CHANGELOG 1.5.0 Fixed）。
- 变更频率：questions.py 11 次（全仓并列第 6）vs permissions.py 3 次——高频修复侧与冻结侧漂移中。
## 分析（机制、影响面、触发条件、可达性）
- 机制：同构「目录发现 → per-dir fan-out → 字节预算聚合 → envelope 打包」管线被复制两份；一侧的修复/硬化不会自动到达另一侧，依赖人工记忆同步。
- 影响面：未来 permissions 侧缺陷概率升高（questions 历史上 4 轮缺陷修正是两文件共同命运的预演）；任何聚合语义演进双倍成本。
- 触发条件：任一聚合路由行为调整。
- 可达性：漂移已在发生（chg 11 vs 3）。
## 契约/文档对照
- v3-contract §5（questions/permissions envelope 形状各异：items/errors/authoritativeDirectories/discoveryComplete）——共享的是骨架与预算器，非 wire 形状；提取不改变两 envelope 的字段集。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核（质量线）：rg 重放（P2）：`rg -n '_DirFetchFailure|_MAX_AGGREGATE_ITEMS|_directories_from_sessions|_collect_with_byte_budget' questions.py permissions.py` → 镜像符号双侧同名同构复现（permissions.py:28/:46/:49/:426 ↔ questions.py 对应位）；CHANGELOG :502（1.1.1）/ :484（1.1.3）/ :474（1.1.4）questions 单侧修复轨迹打开核对一致。
- 维持 P2 / confirmed（修复不对称实证：chg 11 vs 3）。原状态注记移入：draft（A11/D11 §4.3）。
## 建议方向（非实现）
- 提取 `routes/_aggregate_fanout.py`（~250 行）：目录发现输入、`_collect_with_byte_budget` 字节预算器、并发 fan-out、per-dir 错误收集、`_DirFetchFailure`；两路由保留各自 envelope 打包器（`_pack_questions_envelope`/`_pack_permissions_envelope`）与字段映射（~120 行/文件）。
- 测试锚点：questions 2 测试文件 9 引用、permissions 2 文件 6 引用（rg 实测）——均走路由函数，共享层提取后原测试全部继续锚住行为，另需为共享预算器补直接单测（当前仅经两路由间接覆盖）。
