# F-227：_busy_sids 生产零读者死状态——每次 session.status 事件多付一次 prune
- 严重度：P3
- 类别：quality（死状态）
- 位置：src/oc_slimapi/sse/tokenstream/hub.py:283（声明）、:1056-1058/:1061/:1113/:2176（写点全集）
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- 写点全集：:1056-1058（busy 记账 + move_to_end + prune）、:1061（idle pop）、:1113（deleted pop）、:2176（reconnect clear）；
- 读者：**生产零命中**——`ttl_sweep` busy-guard 实读 `self._session_status.get(sid)`（:1190-1191），docstring :239-240 自称 "O(1) busy lookup mirror of _session_status" 的镜像从未被查；仅测试断言读（test_token_hub_lifecycle 等）；
- 维护成本：每次 busy 事件 `_prune_busy_sids()`（:2056-2059，cap 10k FIFO 扫描）+ 双表一致性负担（`_retire_session` 特意不清它、`on_session_deleted`/`on_upstream_reconnect` 要记得清——:1092-1096 注释自证其生命周期复杂度）。
## 分析（机制、影响面、触发性）
- 无行为影响（只写不读）；属 P1-21 加固时引入的冗余镜像。删除需同步删 4 个写点 + 1 个 prune + 测试断言。
## 契约/文档对照（规范轨引文）
- 无契约面。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——tokenstream/hub.py:283 声明命中；rg `_busy_sids` 写点全集=:1056-1058/:1061/:1113/:2176（+prune :2056-2059），生产读者零命中（仅 tests 断言）——只写不读成立。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「verified（A6/D06 2026-08-20；Phase 3 终审）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 删除 `_busy_sids` 及全部写点/prune/测试断言；或若保留镜像意图（未来 O(1) 查询），补真实读者并加一致性断言。
