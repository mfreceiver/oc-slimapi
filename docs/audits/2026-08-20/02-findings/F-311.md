# F-311：sessions.py 883 行 v3/v4 双投影路径并存——保持至 v3 退役窗口再减半（条件性 keep）
- 严重度：P3（可维护性，时间窗限定）
- 类别：design（A11 数据驱动入围：行数>500(883) + 扇出 Top10(14) + 变更频率 Top10(15) 三条全中）
- 位置：src/oc_slimapi/routes/sessions.py:1-883
- 置信度：high（符号清单 + D02 关联）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 双路径符号并存（inventory 符号表）：v3 侧 `_project_sessions`(:741 附近)/`_fetch_sessions_raw`/`_fetch_status_raw` 与 v4 侧 `_V4_ARCHIVED_STATES`/`_v4_allowlist_entries`/`_sessions_v4`/`_project_http_sessions_v4(_canonical)`/`_http_session_to_v4`——同文件两套投影实现。
- 变更频率 15（全仓第 4）；修复 3 轮（0.4.0 status 错误分裂、1.1.2 cap 防线、1.4.0 busy retry_after）均需双路径对照修改——双路径是修复成本的直接放大器。
- D02（v3 可淘汰性审计）已评估 v3 退役；wire 版本窗口当前 (3,4)。
## 分析（机制、影响面、触发条件、可达性）
- 机制：双版本窗口期的过渡态复杂度；现在拆文件会让退役时的删除操作横跨多文件。
- 影响面：退役前每次投影修改双份成本；退役后 ~40% 行数可删。
## 契约/文档对照
- v3-contract（v3 面）+ v4-contract（v4 面）并存即当前契约态。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核：主锚点 wc -l sessions.py = **883 行** ✓；v3/v4 双投影符号并存（`_V4_ARCHIVED_STATES`/`_sessions_v4`/`_project_http_sessions_v4(_canonical)` vs `_project_sessions` 族）rg 抽核在案。
- 维持 P3 / confirmed（时间窗限定 keep）。原状态注记移入：draft（A11/D11 §4.6）。
## 建议方向（非实现）
- 保持单文件至 v3 退役；退役时随删除自然减半（预计 ~500 行），再评估是否需要拆分。当前任何拆分投入都会被退役作废——机会成本为负。
