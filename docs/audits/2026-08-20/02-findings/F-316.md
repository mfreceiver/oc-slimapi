# F-316：35/53 路由的 v4 wire 面无任何 HTTP 级测试（?v=4 从未打到这些路由）；test-census「~35 路由全扫」声明口径失实
- 严重度：P3（§3.3 gap 行：v4 语义对这些路由 = 「与 v3 无差异」，由共享 selector 中间件 + 函数级集合差证明兜底；非硬不变量直锁面）
- 类别：test
- 位置：04-final/test-gap-matrix.csv（v4_face 行 ×35，四列全 NONE）；01-explore/test-census.md:135（「~35 路由全扫：A1 版本钉死/A2 selector/A3+ 逐路由 v4 面」）；tests/test_v4_dual_window.py:391-442
- 置信度：high（逐文件 rg `?v=4` 字面 + 语义变体全量核对）
- 快照：0b836e7
- 状态：confirmed
## 证据
1. 全 tests/ 仅 11 个文件出现 `v=4`/v4 selector 形态：test_sse_replay_wire、test_session_single_v4、test_method_boundary_v4、test_versions_readiness、test_expand_href_v4、test_selector、test_providers_projection_v4、test_post_actions_v4、test_sessions_v4_matrix、test_v4_dual_window、test_readiness_gating_integration。被 ?v=4 实际打到 HTTP 层的路由仅：sessions、session/{sid}、POST session 三条族、events、sessions/{sid}/stream、versions、messages（href/gating 面）、config/providers。
2. test_v4_dual_window.py 的「kept 30 路由」检查（:391-442 `test_directory_fork_is_set_difference`）是对 `sel._directory_consuming_for(path, 4)` 的**函数级**断言，不发起 HTTP 请求；HTTP 级 echo 仅 sessions / sessions/status / health / versions 四条（:86-102）。
3. 矩阵证据：53 条 v4_face 行中 35 条四列全 NONE（agent、command、file×3、vcs×3、find/file、api/session/active、global/health、health、ready、metrics、directories、permissions、questions、messages 列表本体、children、diff、todo、sessions/status（仅 echo）、actions×2、写组 12 条中 9 条、context 等）。
## 分析（机制、影响面、触发条件、可达性）
- v4 admission 由 SlimapiSelectorMiddleware 单点完成（stash wireView），路由侧仅 `wire_view_from_scope` 读取；对「行为不随 wire 变」的路由，v4 正确性 = 中间件正确性（已被 selector/terminal/dual-window 锁）+ 路由不误读 wire（无负面证据）。残留风险：某路由未来引入 wire 条件分支时无既有 v4 请求样例兜底，回归只能靠新增测试发现。
- test-census 的「~35 路由全扫」表述使覆盖广度被高估一个量级（函数级 ≠ HTTP 级）；影响后续审计/维护者对安全边际的判断。
## 契约/文档对照（规范轨引文）
- v4-contract §0.1（?v=3 逐字节不变——反向的 v4 面无逐路由承诺）；§5（退役集 = SET DIFFERENCE，仅 sessions 一条差异路由）。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核 + 反证搜索（任务专项）：`rg -l 'v=4' tests/` → 恰 11 文件，与证据清单逐一吻合；HTTP 层 ?v=4 实达路由面（sessions 族/events/stream/versions/messages href/config/providers 等）与 35/53 NONE 行口径维持。
- 反证搜索结果：唯一集合差测试 = test_v4_dual_window.py:391-442 `test_directory_fork_is_set_difference`——**函数级**断言 `sel._directory_consuming_for(path, 4)`（30 kept 代表路径 + sessions 唯一 retired），仅锁 directory 维度；另 test_selector.py 覆盖版本协商本身；**未发现任何 HTTP 层全路由 ?v=4 参数化扫描**（rg ALL_ROUTES/route list 零命中）。selector 层共享 + 函数级集合差足以锁「directory 消费面」的 v4 语义，但不等价 HTTP 级 wire 面测试——本条表述（函数级 ≠ HTTP 级、census 口径失实）无反证。
- 终判：**confirmed / 维持 P3**（严重度行已如实计入集合差兜底，无需降级表述）。原状态 verified。
## 建议方向（非实现）
- 若追求全面：在 test_v4_dual_window 增加一条参数化「全路由 ?v=4 冒烟」（每路由一次 200/预期错误断言）；最低成本修正 = test-census.md:135 措辞改为「函数级 kept-集断言 + 4 条 HTTP echo」。发 v5 前任何路由新增 v4 分支行为时，必须伴 per-route v4 测试（可入 CONTRIBUTING/check 清单）。
