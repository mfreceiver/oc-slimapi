# F-204：write_groups POST 回显 gzip 在事件循环上执行（回显体小）
- 严重度：P3
- 类别：smell（事件循环压缩；现实量级小）
- 位置：src/oc_slimapi/routes/write_groups.py:235
- 置信度：high
- 快照：0b836e7
- 状态：confirmed
## 证据
- write_groups.py:235：`encoded, coding = compress_if_beneficial(resp_body, accept_encoding)`——§10.b write 路由成功回显在路由协程压缩（注释 :230-233 自述"the sidecar re-encodes under its own gzip benefit gate"）。
- resp_body 上界 = `max_response_bytes`（64MiB cap-read）；POST 回显现实为小体（KB 级）。
- 佐证材料：03-reports/D05-concurrency.md §6.2 N4。
## 分析（机制、影响面、触发条件、可达性）
- 与 F-201/F-202/F-203 同族；write 回显无独立 worker 路径。量级最小（回显体小 + MIN_GZIP_BYTES 门），列档保持整改清单完整性。
## 契约/文档对照（规范轨引文）
- 无 wire 面。
## 复核记录（Phase 3）
- 2026-08-20 Phase 3 V1 复核（契约线+风险线批次；快照 0b836e7 未变由 manifest 保证）：主锚点抽查通过——write_groups.py:233-236 回显 compress_if_beneficial 命中（注释 :233-234 自述 re-encode）。证据链成立，维持严重度与建议方向。
- 状态行归一：confirmed（原「confirmed（A5/D05 Phase 2；Phase 3 终审）」辖属/批次注记移入本记录）。
## 建议方向（非实现）
- 并入 F-201 的"压缩下放"整改；或明确豁免（小体 inline）并注释阈值依据。
