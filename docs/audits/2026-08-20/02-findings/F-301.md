# F-301：sse/tokenstream/hub.py 2190 行上帝文件——单一类 59 方法承载累积/预算/flush/fanout/tombstone 五族职责
- 严重度：P2（可维护性；非运行时缺陷）
- 类别：design（模块化/上帝文件，A11 数据驱动入围 #1：行数全仓第一 + 扇出 Top10 + 变更频率 Top10 三条全中）
- 位置：src/oc_slimapi/sse/tokenstream/hub.py:214-2190（class TokenStreamHub 单类）
- 置信度：high（AST/行号实测 + e1-01 卡片全文精读）
- 快照：0b836e7
- 状态：confirmed
## 证据
- 行数 2190（全仓 71 个 src 文件第一，第二名 messages.py 1643）；顶层符号 8 个但类内 59 方法/属性（e1-01 卡片：1 类 + 8 模块级）。
- 类 docstring 自述 9 个核心容器（src/oc_slimapi/sse/tokenstream/hub.py:215-246：live_parts/_nontext_parts/_disabled_parts/_pending/_total_live_bytes/_total_pending_bytes/_session_status/_busy_sids/_pending_session_resinks/_subs_by_sid 实为 10 项）。
- 五族职责行段（grep "^    def " 实测）：
  - 后台 flush 生命周期：:421-617（start/_on_flush_done/stop/flush_loop/flush/flush_sid）；
  - ingest 事件族：:619-1166（on_part_updated/on_part_delta/on_message_removed/on_part_removed/_retire_message/finish_part/on_session_status/on_session_deleted/_retire_session/ttl_sweep）；
  - 订阅者接线：:1211-1370（attach_subscriber/detach_subscriber/has_subscriber）；
  - fanout/投递：:1371-1649（_replay_publish_token/_write_replay_barrier/_deliver_logged/_deliver_v3_only/_fanout_frame/_fanout_message_removed/_fanout_resync/_fanout_heartbeat/_emit_snapshot_or_truncated*）；
  - 内存预算/tombstone：:1650-2124（_reserve/_evict_part_for_memory/_check_pending_budget/_start_part/drop_part/_remember_disabled/_remember_nontext/_prune_bounded/_prune_*）。
- 变更频率 11 次（2026-08-01 以来，git log --name-only 聚合，全仓并列第 6）；CHANGELOG 修复涉及 3 次（0.6.0 O1 预算 re-entrancy、1.1.6 INV-4 session.deleted 终止、3.3.1 状态归一化对齐）。
- TODO 残留 2 处（inventory todo_markers：hub.py:663、:760「confirm live wire key casing」）。
## 分析（机制、影响面、触发条件、可达性）
- 机制：单文件同时是「事件累积器 + 双内存预算执行器 + 100ms flush 引擎 + SSE fanout 引擎 + tombstone/驱逐策略库」五个正交关注点；任何一族的修改都在同一 2190 行文件内进行，评审 diff 上下文互相干扰。
- 影响面：纯可维护性（churn×面积乘积全仓最大）；不改 wire 行为。
- 触发条件：任何 token stream 演进（预算调参、新帧型、订阅语义）都触达本文件。
- 可达性：已发生（0.6.0/1.1.6/3.3.1 三轮修复均在此文件内定位跨族交互）。
## 契约/文档对照
- v4-contract §13（token stream wire 面）不受影响——拆分为纯内部重构，public API（TokenStreamHub/apply_debug_budget_overrides）不变。
## 复核记录（Phase 3）
- 2026-08-20 V1 复核（质量线）：主锚点 wc -l hub.py = **2190 行**（全仓第一）✓；class TokenStreamHub 起 :214 ✓；awk 实测类内 def = **59** ✓；TODO :663/:760 ✓——三条数据驱动入围指标复核一致。
- rg 重放（P2）：`awk 'NR>214 && /^    (async )?def /{c++} END{print c}'` → 59，与证据「59 方法」精确吻合。
- 维持 P2 / confirmed（可维护性，非运行时缺陷）。原状态注记移入：draft（A11/D11 §4.1）。
## 建议方向（非实现）
- 沿仓库既有物理拆分先例（sse/hub.py → hub_types/global_hub/registry + 兼容 shim，sse/hub.py:1-13 docstring 自述；token_hub → tokenstream/ 包，sse/token_hub.py:1-4）拆为 5 模块包内组合：
  - `tokenstream/budgets.py`（~400 行：_reserve/_evict_part_for_memory/_check_pending_budget/_start_part/drop_part + _remember_*/_prune_* tombstone 族）；
  - `tokenstream/flush_engine.py`（~200 行：start/stop/flush_loop/flush/flush_sid/_on_flush_done + heartbeat tick）；
  - `tokenstream/ingest.py`（~550 行：on_*/finish_part/_retire_*/ttl_sweep）；
  - `tokenstream/fanout.py`（~430 行：_fanout_*/_deliver_*/_emit_snapshot_or_truncated*/_replay_publish_*）；
  - `hub.py` 壳（~350 行：容器 __init__ + property + 接线）。
- 迁移风险低：测试锚点 33 个测试文件、936 处引用（rg 实测，tests/ 对 hub/TokenStreamHub 的 import），且绝大多数经 public 类而非内部符号；保留 `TokenStreamHub` 类名与 `sse/token_hub.py` shim 即零测试迁移。
- 收益：五族职责各自 ≤550 行；预算族（CHANGELOG 0.6.0 O1 缺陷所在）可独立评审。
