# F-026：gzip_util.json_response 绕过 MIN_GZIP_BYTES/收益门——三路错误构造的全部错误体在小体时仍被压缩
- 严重度：P3（初判维持；纯 CPU/字节浪费，无正确性影响）
- 类别：smell
- 位置：src/oc_slimapi/gzip_util.py:110-123（json_response 无条件 gzip）对照 :75-107（compress_if_beneficial 三门）
- 置信度：high（A10 影响面全量核对后由 medium 升）
- 快照：0b836e7
- 状态：confirmed
## 证据
- json_response（gzip_util.py:117-123）：`if accepts_gzip(accept_encoding): body = gzip.compress(body, compresslevel=6)` ——**无 MIN_GZIP_BYTES（:25,64）尺寸门、无压缩收益比较**；compress_if_beneficial（:98-107）的三门（协商/≥64B/严格变小）只作用于成功体路径。
- 消费面（A10 全量）：错误构造三路全部经 json_response 渲染——
  1. `CodedHTTPException` handler（errors.py:44-52）→ json_response；
  2. `error_response`（gzip_util.py:126-144）→ json_response（19 个发射点：proxy/messages/_catalog_common/_read_passthrough/read_groups/write_groups）；
  3. selector/SSE 容量手写 json_response（selector.py:539,560,596,614,627；events.py:134；token_stream.py:177）。
  即 draft 所述「selector 小错误体」实为**全部 80 个错误发射点**共面。
- 典型体：版本门 400 `unsupported_version` ≈44B、`invalid_directory` ≈31B——gzip 固定开销（头尾 ~18B + deflate 框架）使压缩体**大于**原文（MIN_GZIP_BYTES=64 的注释 :20-24 自证该量级判断）。
- 佐证材料：01-explore/parts/e1-16-core-semantics.md；03-reports/D10-code-quality.md §2.1。
## 分析（机制、影响面、触发条件、可达性）
- 机制：json_response 与 compress_if_beneficial 并行实现 gzip 决策，前者落后于 P1-31 三门改造；错误路径全部走前者。
- 影响面：每错误响应多付 gzip CPU（事件循环上同步压缩，与 F-271 同面但体积极小）+ 传输负收益（压缩后更大仍标 Content-Encoding）。
- 触发条件：客户端 `Accept-Encoding: gzip` + 任意错误响应（高频 400/413/503 面）。
- 无 wire 违约：契约 §9 只要求「gzip 协商」，未冻结「必须压缩」；改走收益门后小体回 identity 仍是合法协商结果（Content-Encoding 缺席）。
## 契约/文档对照（规范轨引文）
- v3-contract §9（错误体 gzip 协商语义）；gzip_util.py:1-9 模块自述「Every gzip decision site MUST route through accepts_gzip」——本项不违反该句字面（json_response 确经 accepts_gzip），违反的是 P1-31 收益门精神。
## 复核记录（Phase 3）
- **终态 confirmed / P3**。锚点抽查（2026-08-20）：gzip_util.py:117-123 `json_response` 直读——`if accepts_gzip(accept_encoding): body = gzip.compress(body, compresslevel=6)` **无条件压缩**（无 MIN_GZIP_BYTES/收益门），与 :98-107 `compress_if_beneficial` 三门并行命中。纯 CPU/字节浪费、无 wire 违约（§9 只要求协商）定性复核认同。原状态行括注「A10/D10 2026-08-20」由本节承接。
## A10 复核记录（2026-08-20，D10）
- draft → **verified / P3 维持**，置信度 medium → high（影响面由 selector 单点扩为三路全错误体，均 rg 全量核对）。SSE/token 流已豁免（恒 identity，F-290 死链另计）。
## 建议方向（非实现）
- json_response 内联 compress_if_beneficial 的尺寸/收益门（保持函数签名与头合并顺序不变；≈44B 阈下回 identity 即可）；顺带统一两条 gzip 路径为单实现。
